---
phase: 01-repository-hygiene
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - REPO-01
must_haves:
  truths:
    - "README.md を開けばプロジェクトの正体（curl で Claude を実行する HTTP WebIF）が把握できる"
    - "前提依存（ht-mcp バイナリ、claude CLI が Max でログイン済み）が明示されている"
    - "ビルド手順（cargo build / cargo run）が記載されている"
    - "起動手順（.env 設定、起動コマンド、リッスンアドレス 127.0.0.1:8080）が記載されている"
    - "4 つの HTTP エンドポイント全てに curl での API 呼び出し例が掲載されている"
    - "ライセンス（MIT）が表記されている"
  artifacts:
    - path: "README.md"
      provides: "プロジェクト概要・前提・ビルド・起動・API 例・ライセンス"
      contains: "POST /prompt"
      min_lines: 80
  key_links:
    - from: "README.md"
      to: "LICENSE"
      via: "ライセンスセクションでの参照"
      pattern: "MIT"
    - from: "README.md"
      to: ".env.example"
      via: "起動手順での `.env` 設定ガイド"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "HT-PROTOCOL.md"
      via: "プロトコル仕様への参照リンク"
      pattern: "HT-PROTOCOL\\.md"
    - from: "README.md"
      to: "webif/Cargo.toml"
      via: "Cargo.toml の readme = \"../README.md\" 設定がこのファイルを指す"
      pattern: ""
---

<objective>
リポジトリ直下に `README.md` を作成し、初見の開発者が以下をワンストップで把握できる状態にする:
- プロジェクト概要（curl で Claude を実行する HTTP WebIF、Core Value: `-p` 回避 + Max サブスクリプション維持）
- 前提依存（`ht-mcp` バイナリ、`claude` CLI が既に Max でログイン済み）
- ビルド手順（Rust toolchain、`cargo build`）
- 起動手順（`.env` 設定、`cargo run`、リッスンアドレス）
- 4 つの HTTP エンドポイントの API 例（`POST /prompt`、`GET /turns/{id}`、`POST /command`、`POST /restart`）
- ライセンス（MIT）

Purpose: README は他人が本リポジトリに触れる際の最初の入口。Phase 1 の他の成果物（LICENSE、Cargo.toml メタデータ、.gitignore）に意味を与えるための「読み物」となる。
Output: `README.md`（リポジトリルート、日本語主体）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@HT-PROTOCOL.md

<!-- 既存ファイル: 現状は "hello ht!!" のみ。完全書き換え。 -->
@README.md
@.env.example
@webif/Cargo.toml

<!-- API 例の正確性のため、実際のハンドラ実装を読む -->
@webif/src/main.rs

<interfaces>
<!-- README の API 例セクションで再現する必要のある HTTP エンドポイント契約。
     webif/src/main.rs の関数シグネチャ・DTO 定義・println! 起動メッセージから抽出。
     README 執筆者はこの契約を直接使用し、ソースを再探索する必要はない。 -->

リッスンアドレス（webif/src/main.rs:551）:
  127.0.0.1:8080

POST /prompt
  - リクエスト DTO（PromptReq, webif/src/main.rs:398-407）:
      { "prompt": "<タスク文字列>",
        "fresh":  false,    // true なら /clear で文脈リセット
        "wait":   false }   // true なら完了まで待つ（同期、最大 700 秒）
  - 非同期レスポンス（既定、webif/src/main.rs:448）:
      { "turn_id": "20260523-103045-123", "status": "accepted" }
  - 同期レスポンス（wait:true）: read_turn() の戻り値:
      { "turn_id": "...", "status": "completed|timeout|failed", "result": "..." }

GET /turns/{turn_id}
  - turn_id は数字とハイフンのみ許可（webif/src/main.rs:457）
  - 実行中: { "turn_id": "...", "status": "running" }
  - 完了:   { "turn_id": "...", "status": "completed", "result": "..." }
  - 不在:   404

POST /command
  - リクエスト DTO（CommandReq, webif/src/main.rs:472-476）:
      { "text": "/clear" }  // スラッシュコマンドや生文字列を TUI に直接打鍵
  - レスポンス DTO（CommandResp, webif/src/main.rs:478-482）:
      { "sent": "<送信文字列>", "snapshot": "<TUI 画面の文字列スナップショット>" }

POST /restart
  - リクエストボディなし
  - レスポンス DTO（RestartResp, webif/src/main.rs:503-507）:
      { "status": "restarted", "session_id": "<新しい claude session ID>" }

環境変数（webif/src/main.rs:524-525、.env.example）:
  HT_MCP_PATH  ht-mcp バイナリへのパス。既定 "ht-mcp"（PATH 上を探索）
  優先順位: 実環境変数 > .env > 既定値

タイムアウト定数:
  TURN_TIMEOUT  300s（1試行あたり、webif/src/main.rs:39）
  MCP_TIMEOUT    30s（MCP 呼び出し1回、webif/src/main.rs:41）
  wait:true デッドライン  700s（webif/src/main.rs:433）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README.md を新規作成する（日本語主体、API 例 4 種を網羅）</name>
  <files>README.md</files>
  <read_first>
    - README.md（現状: `hello ht!!` のみ — 完全に上書きする）
    - CLAUDE.md（## Project セクション = README に転用する一次資料、## Architecture も参照）
    - .env.example（環境変数 `HT_MCP_PATH` の説明 — そのまま参照）
    - webif/Cargo.toml（クレート名 `ht-webif`、edition 2021）
    - webif/src/main.rs（**特に webif/src/main.rs:395-560** — DTO 定義、リッスンアドレス、4 ルートの起動メッセージ、タイムアウト定数）
    - HT-PROTOCOL.md（README から「詳細プロトコル仕様は HT-PROTOCOL.md を参照」と相互リンクする）
    - .planning/REQUIREMENTS.md（REPO-01）
    - .planning/ROADMAP.md（Phase 1 Success Criteria #1）
  </read_first>
  <action>
    リポジトリルート（`/home/parallels/workspaces/ht-mcp-sample/README.md`）の既存 `hello ht!!` を完全に置き換え、以下のセクション構成で日本語主体の README を書く。CLAUDE.md の「Language for Comments and Logs」規約に従い、見出し・本文ともに日本語、コードブロック内のシェルコマンドや JSON は ASCII。

    必須セクション（順序固定、`##` 見出し）:

    1. **タイトル + 1 行サマリ**: `# ht-webif` の下に「curl 1 発で Claude にタスクを送って結果を受け取れる、軽量な HTTP WebIF。」

    2. **## Core Value**: CLAUDE.md の Core Value をそのまま転載: 「`-p` を避けつつ curl で Claude を実行できる。」サブスクリプション課金を維持する設計動機を 2〜3 文で説明。

    3. **## アーキテクチャ概要**: CLAUDE.md の System Overview にあるデータフロー図（`[curl] --HTTP--> WebIF --MCP(stdio)--> ht-mcp --> ht --PTY--> claude`）を ASCII でそのまま記載。

    4. **## 前提依存**: 以下をリスト形式で:
       - **Rust toolchain**: edition 2021 をサポートする stable Rust（`rustup` でインストール）
       - **`ht-mcp` バイナリ**: 別途インストール済みで PATH 上にあるか、`HT_MCP_PATH` 環境変数で位置指定すること。プロジェクトでは `/home/parallels/.cargo/bin/ht-mcp` を想定（環境により異なる）
       - **`claude` CLI**: PATH 上にあり、**Max サブスクリプションで既にログイン済み**であること（`~/.claude/.credentials.json` の OAuth に依存）。**ANTHROPIC_API_KEY は使用しない**（Core Value の制約）
       - **POSIX-like OS**: ファイルの atomic rename を前提（HT-PROTOCOL v1.1 §6）

       重要: `~/.claude/.credentials.json` の **存在に依存することだけ言及**し、**ファイル内容や具体的な OAuth トークン形式は記載しない**（脅威モデル T-01-09 mitigate）。

    5. **## ビルド**: コードブロック:
       ```bash
       cd webif
       cargo build --release
       ```

    6. **## 起動**: 手順を箇条書きで:
       1. リポジトリルートで `cp .env.example .env` を実行し、必要に応じて `HT_MCP_PATH` を編集
       2. リポジトリルートで `cargo run --manifest-path webif/Cargo.toml`（または `cd webif && cargo run`）
       3. 起動成功時に `WebIF 起動: http://127.0.0.1:8080` が表示される
       4. ループバック専用（127.0.0.1）。外部から触りたい場合は SSH トンネルやリバースプロキシ経由

    7. **## API**: 4 サブセクションで `###` 見出しを切り、それぞれに curl 例 + 期待レスポンス JSON を載せる。**`<interfaces>` ブロックの契約を使用すること（再探索しない）**:

       - **### `POST /prompt` — ターン投入**: 非同期版（`{"prompt": "Rust とは何か 100 字で"}`）と同期版（`{"prompt": "...", "wait": true}`）の両方を示す。`fresh: true` の用途（/clear で文脈リセット）も言及。
       - **### `GET /turns/{turn_id}` — ターン状態照会**: running / completed / 404 の 3 ケースを示す。
       - **### `POST /command` — TUI に生文字列送信**: `{"text": "/clear"}` の例。スナップショット文字列が返ることを説明。
       - **### `POST /restart` — ht-mcp ごと再起動**: ボディなし POST。`{"status": "restarted", "session_id": "..."}` が返る。

    8. **## 環境変数**: 表形式（`| 変数 | 既定値 | 説明 |`）で `HT_MCP_PATH` を 1 行記載。`.env > 実環境変数` の優先順位を補足（実環境変数が優先）。

    9. **## ターン成果物**: `turns/` ディレクトリに `prompt-<turnId>.txt` / `result-<turnId>.txt` / `status-<turnId>.json` の 3 点セットが生成されることを 3 行で説明。詳細は HT-PROTOCOL.md にリンク。

    10. **## 制約**: 重要な技術的制約を箇条書き:
        - 同時 1 ターン（直列処理、`Arc<Mutex<Worker>>` で排他）
        - ループバック専用、認証なし（v2 で SEC-01 として bearer token 予定）
        - シングルホスト前提（WebIF / ht-mcp / claude が同一ファイルシステムを共有）
        - MCP 呼び出しタイムアウト 30 秒、ターンタイムアウト 300 秒、wait:true は最大 700 秒

    11. **## 詳細仕様**: 「HT-PROTOCOL の詳細（ターンID 発番規則、status ファイル形式、回復シナリオ）は [HT-PROTOCOL.md](./HT-PROTOCOL.md) を参照。」

    12. **## ライセンス**: 「MIT License — 詳細は [LICENSE](./LICENSE) を参照。」

    その他のルール:
    - 各 curl 例は実際にコピペで動く形式（実在しないオプションを書かない）
    - JSON は ASCII の半角ダブルクォート
    - エンドポイント記述では HTTP メソッド + パスをコードフォントで囲む
    - 「## API」セクション以下のすべての curl 例で `http://127.0.0.1:8080` を明示する（README 単体で動かせるように）

    脅威モデル対応: README に `~/.claude/.credentials.json` の **内容や OAuth トークン形式を書かない** ことで I（情報開示）を mitigate（存在依存だけ言及）。
  </action>
  <verify>
    <automated>test -f README.md && [ "$(wc -l < README.md)" -ge 80 ] && grep -q '^# ht-webif' README.md && grep -q '## Core Value\|## コアバリュー' README.md && grep -q '## 前提依存\|## 依存' README.md && grep -q '## ビルド' README.md && grep -q '## 起動' README.md && grep -q '## API' README.md && grep -q 'POST /prompt' README.md && grep -q 'GET /turns/' README.md && grep -q 'POST /command' README.md && grep -q 'POST /restart' README.md && grep -q '127.0.0.1:8080' README.md && grep -q 'ht-mcp' README.md && grep -q 'claude' README.md && grep -q 'HT_MCP_PATH' README.md && grep -q 'MIT' README.md && grep -q 'LICENSE' README.md && grep -q 'HT-PROTOCOL\.md' README.md && ! grep -q 'credentials\.json.*=\|eyJ\|sk-ant-' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` がリポジトリルートに存在し 80 行以上 — `test -f README.md && [ "$(wc -l < README.md)" -ge 80 ]`
    - タイトル行 `# ht-webif` が存在 — `grep -q '^# ht-webif' README.md`
    - 6 つの必須セクション見出しすべてが存在: Core Value / 前提依存（または依存） / ビルド / 起動 / API / ライセンス（タイトルは「ライセンス」または「License」） — 各 `grep -q` が exit 0
    - 4 つの HTTP エンドポイントすべてが文字列として登場: `POST /prompt`、`GET /turns/`、`POST /command`、`POST /restart` — 各 `grep -q` が exit 0
    - リッスンアドレス `127.0.0.1:8080` が明示されている — `grep -q '127.0.0.1:8080' README.md`
    - 前提依存に `ht-mcp` と `claude` の両方が言及されている — `grep -q 'ht-mcp' README.md && grep -q 'claude' README.md`
    - 環境変数 `HT_MCP_PATH` が表または本文に登場 — `grep -q 'HT_MCP_PATH' README.md`
    - ライセンスが MIT と明示され、LICENSE ファイルへの参照がある — `grep -q 'MIT' README.md && grep -q 'LICENSE' README.md`
    - HT-PROTOCOL.md への参照がある — `grep -q 'HT-PROTOCOL\.md' README.md`
    - **秘密情報が漏れていない**: credentials.json の中身や OAuth トークン文字列が含まれない — `! grep -qE 'credentials\.json.*=|eyJ[A-Za-z0-9_-]{10,}|sk-ant-[A-Za-z0-9_-]+' README.md`
    - curl コマンド例が少なくとも 4 つ存在（4 エンドポイント分） — `grep -c '^curl\| curl ' README.md` が 4 以上、または `grep -c 'curl.*http://127.0.0.1:8080' README.md` が 4 以上
    - 日本語の見出しが少なくとも 3 つ以上存在（CLAUDE.md の日本語規約準拠） — `grep -cE '^## .*[ぁ-んァ-ン一-龥]' README.md` が 3 以上
  </acceptance_criteria>
  <done>
    `README.md` がリポジトリルートに存在し、プロジェクト概要・Core Value・前提依存・ビルド/起動手順・4 エンドポイントの curl 例・環境変数・ライセンス・HT-PROTOCOL.md へのリンクを含む日本語主体のドキュメントになっている。`~/.claude/.credentials.json` の中身は記載されていない（存在依存のみ言及）。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 公開ドキュメント → 任意の閲覧者 | README は公開を前提とする境界。秘密情報の漏洩は不可逆 |
| README 記述内容 → 実バイナリ挙動 | ドキュメントと実装の乖離による誤った API 呼び出し（運用上の摩擦） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | I (情報開示) | README が `~/.claude/.credentials.json` の中身や OAuth トークン形式を露出させるリスク | mitigate | action で明示的に「ファイル内容や OAuth トークン形式は記載しない、存在依存だけ言及」と指示。acceptance: `grep -qE 'credentials\.json.*=\|eyJ\|sk-ant-' README.md` が一致行を返さないこと |
| T-01-10 | T (改ざん) / 運用ミス | README に書かれた API 仕様が実装と乖離（誤ったエンドポイント名・DTO フィールド・タイムアウト値） | mitigate | `<interfaces>` ブロックで webif/src/main.rs から実契約を抽出し、執筆者がソース再探索なしに正確な値を使えるようにする。acceptance: 4 エンドポイント全てが具体的 curl 例として登場 |
| T-01-11 | R (否認) | ライセンス記載と実 LICENSE の不一致 | mitigate | Plan 02 で LICENSE が MIT で確定済み。本 README も MIT を明示し、`./LICENSE` への直接リンクで読者が原典を確認可能にする |
| T-01-12 | I (情報開示) | `repository` URL や個人特定可能情報 | accept | README は GitHub 等での公開を前提とする文書。リポジトリ URL を載せることは想定内 |
</threat_model>

<verification>
1. `README.md` が repo root に存在し、80 行以上
2. 必須 6 セクション（Core Value / 前提依存 / ビルド / 起動 / API / ライセンス）がすべて存在
3. 4 つの HTTP エンドポイントすべてに curl + 期待レスポンスの例が掲載
4. 秘密情報（credentials.json 中身、OAuth トークン）が含まれない
5. LICENSE と HT-PROTOCOL.md へのリンクが存在
</verification>

<success_criteria>
- ROADMAP.md Phase 1 Success Criteria #1 を満たす: 「README.md を開くと、プロジェクト概要・前提依存（ht-mcp / claude CLI）・ビルド手順・起動手順・4 つの HTTP エンドポイントの API 例が確認できる」
- REPO-01 完了
</success_criteria>

<output>
完了後、`.planning/phases/01-repository-hygiene/01-03-SUMMARY.md` を作成する。
</output>
