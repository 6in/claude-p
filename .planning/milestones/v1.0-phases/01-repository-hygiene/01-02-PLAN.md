---
phase: 01-repository-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - webif/Cargo.toml
autonomous: true
requirements:
  - REPO-02
  - REPO-04
must_haves:
  truths:
    - "リポジトリ直下に LICENSE ファイルが存在する"
    - "LICENSE ファイルの内容が MIT ライセンスの正式テキストである"
    - "webif/Cargo.toml の license フィールドが SPDX 識別子 'MIT' を持つ"
    - "LICENSE ファイルの種類と Cargo.toml の license フィールドが一致する"
    - "cargo metadata で description / repository / license / authors / readme / edition の全フィールドが取得できる"
  artifacts:
    - path: "LICENSE"
      provides: "MIT ライセンスの全文（著作権者名・年込み）"
      min_lines: 18
    - path: "webif/Cargo.toml"
      provides: "クレートメタデータ（description / repository / license / authors / readme / edition）"
      contains: "license = \"MIT\""
  key_links:
    - from: "LICENSE"
      to: "webif/Cargo.toml license フィールド"
      via: "SPDX 識別子の一致（MIT）"
      pattern: "license = \"MIT\""
    - from: "webif/Cargo.toml readme フィールド"
      to: "リポジトリ直下の README.md（Plan 03 が作成）"
      via: "readme = \"../README.md\" の相対パス参照"
      pattern: "readme = \"\\.\\./README\\.md\""
---

<objective>
リポジトリ直下に `LICENSE` ファイル（MIT 全文）を配置し、`webif/Cargo.toml` のメタデータを充実させる（`description`、`repository`、`license = "MIT"`、`authors`、`readme = "../README.md"`、`edition = "2021"`）。LICENSE ファイルと Cargo.toml の license フィールドが一致することを保証する。

Purpose: LICENSE と Cargo.toml の license は **SPDX 識別子で結合された一対の表明**であり、両者の不一致は下流の依存者にとってライセンス上のリスクになる。同一プラン内で同時に決定・記述することで、一致を強制する。
Output: `LICENSE`（リポジトリルート、MIT 全文）、`webif/Cargo.toml`（メタデータ拡張）
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<!-- 現在のクレートメタデータ状態を確認するための参照 -->
@webif/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: LICENSE ファイル（MIT）を作成する</name>
  <files>LICENSE</files>
  <read_first>
    - .planning/REQUIREMENTS.md（REPO-02: LICENSE ファイル追加、MIT または Apache-2.0）
    - .planning/ROADMAP.md（Phase 1 Success Criteria #2: LICENSE ファイルが存在し、Cargo.toml の license フィールドと一致）
    - CLAUDE.md（プロジェクト概要 — このリポジトリは小さなブリッジ／ユーティリティ的 HTTP WebIF）
  </read_first>
  <action>
    リポジトリルート（`/home/parallels/workspaces/ht-mcp-sample/LICENSE`）に MIT ライセンスの正式全文を書く。SPDX 識別子は `MIT`。

    採用ライセンスは **MIT** に確定する（CONTEXT 指示: 「軽量グルー/ユーティリティクレートの慣例」「choose-one、planner がデフォルトで MIT を選ぶ」）。Apache-2.0 にはしない。

    LICENSE ファイルの 1 行目は **`MIT License`** とすること（`head -1 LICENSE` で SPDX 識別子 MIT と機械照合できるようにするため）。

    本文は OSI 公式の MIT 標準テンプレートをそのまま使用する。著作権行は:

    `Copyright (c) 2026 ht-webif contributors`

    年は 2026（today's date は 2026-05-23）、著作権者名は `ht-webif contributors`（個人名が確定していないため）。テンプレート全文は以下を含むこと:
    - `Permission is hereby granted, free of charge, to any person obtaining a copy`
    - `THE SOFTWARE IS PROVIDED "AS IS", WITHOUT WARRANTY OF ANY KIND`

    LICENSE は英文のまま（OSI 規定の標準テキストを改変しない）。本ファイルだけは CLAUDE.md の「Language for Comments and Logs」規約の対象外（ライセンス文の改変は法的に避けるべき）。

    脅威モデル対応: ライセンス表記の不一致（LICENSE と Cargo.toml で別物を主張）は STRIDE の R（否認）に類する下流リスク — Task 2 で Cargo.toml と一致させることで mitigate する。
  </action>
  <verify>
    <automated>test -f LICENSE && head -1 LICENSE | grep -q '^MIT License$' && grep -q 'Permission is hereby granted, free of charge' LICENSE && grep -q 'THE SOFTWARE IS PROVIDED "AS IS"' LICENSE && grep -q 'Copyright (c) 2026' LICENSE && [ "$(wc -l < LICENSE)" -ge 18 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` ファイルがリポジトリルート（`/home/parallels/workspaces/ht-mcp-sample/LICENSE`）に存在する — `test -f LICENSE`
    - 1 行目が `MIT License` である — `head -1 LICENSE` が `MIT License` を出力
    - MIT 標準文言の特徴的フレーズが含まれる — `grep -q 'Permission is hereby granted, free of charge' LICENSE` が exit 0
    - 免責文が含まれる — `grep -q 'THE SOFTWARE IS PROVIDED "AS IS"' LICENSE` が exit 0
    - 著作権年が 2026 — `grep -q 'Copyright (c) 2026' LICENSE` が exit 0
    - ファイルが空でなく行数が 18 行以上 — `[ "$(wc -l < LICENSE)" -ge 18 ]`
  </acceptance_criteria>
  <done>
    `LICENSE` がリポジトリルートに存在し、MIT 公式テンプレートの全文（permissions/conditions/disclaimer すべて）と `Copyright (c) 2026 ht-webif contributors` を含む。1 行目は `MIT License`。
  </done>
</task>

<task type="auto">
  <name>Task 2: webif/Cargo.toml にメタデータを追加する（LICENSE と SPDX 一致）</name>
  <files>webif/Cargo.toml</files>
  <read_first>
    - webif/Cargo.toml（現状: name、version、edition、dependencies のみ — description/repository/license/authors/readme が欠落）
    - LICENSE（同プラン Task 1 で作成済み — SPDX 識別子は MIT）
    - .planning/REQUIREMENTS.md（REPO-04: description、repository、license、authors、readme、edition）
    - .planning/ROADMAP.md（Phase 1 Success Criteria #4: cargo metadata で取得可能）
    - CLAUDE.md（Project セクション: 「ht-webif」、Core Value: 「`-p` を避けつつ curl で Claude を実行できる」）
  </read_first>
  <action>
    `webif/Cargo.toml` の `[package]` セクションに以下のフィールドを追加する（既存の name / version / edition は維持）:

    - `description = "curl 1発で Claude にタスクを送って結果を受け取れる軽量な HTTP WebIF。ht-mcp 経由で対話型 Claude TUI を駆動し、Max サブスクリプション課金を維持する。"`
      - 日本語の説明文。CLAUDE.md の冒頭サマリと整合させる。
    - `repository = "https://github.com/0hya6in/ht-mcp-sample"`
      - GitHub URL は user email（0hya6in@gmail.com）から GitHub username `0hya6in` を推測。リモートが未確定のため最も自然な候補を採用する。
    - `license = "MIT"`
      - **SPDX 識別子。LICENSE ファイル 1 行目の `MIT License` と必ず一致させること。**
    - `authors = ["ht-webif contributors"]`
      - LICENSE の著作権者と同一表記。
    - `readme = "../README.md"`
      - README.md はリポジトリルートに置く（Plan 03 が作成）。Cargo.toml は `webif/` 配下にあるため相対パスで親ディレクトリを指す。
    - `edition = "2021"`
      - 既に存在するため変更しない。**消さないこと。**

    任意の追加（推奨だが必須ではない）:
    - `keywords = ["claude", "mcp", "http", "axum"]`（4 個までという crates.io 制約に注意）
    - `categories = ["command-line-utilities"]`

    脅威モデル対応: LICENSE ファイルと `license` フィールドの不一致は本タスクで mitigate（acceptance: 機械的に一致を検証）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif && cargo metadata --format-version 1 --no-deps --offline 2>/dev/null | python3 -c "import sys, json; p = json.load(sys.stdin)['packages'][0]; assert p['description'], 'description missing'; assert p['repository'], 'repository missing'; assert p['license'] == 'MIT', f'license must be MIT, got {p[\"license\"]}'; assert p['authors'], 'authors missing'; assert p['readme'], 'readme missing'; assert p['edition'] == '2021', f'edition must be 2021, got {p[\"edition\"]}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `webif/Cargo.toml` に `description` キーが存在し、値が空文字列でない — `cargo metadata --format-version 1 --no-deps` の `.packages[0].description` が非 null かつ長さ 10 文字以上
    - `webif/Cargo.toml` に `repository` キーが存在 — `.packages[0].repository` が `https://` で始まる URL 文字列
    - `webif/Cargo.toml` に `license = "MIT"` が存在 — `.packages[0].license == "MIT"`
    - `webif/Cargo.toml` に `authors` キーが存在し、空配列でない — `.packages[0].authors` の長さが 1 以上
    - `webif/Cargo.toml` に `readme = "../README.md"` が存在 — `.packages[0].readme` が `../README.md` または `README.md` を含む
    - `webif/Cargo.toml` の `edition = "2021"` が維持されている — `.packages[0].edition == "2021"`
    - LICENSE 1 行目との SPDX 一致: `head -1 LICENSE` の出力 `MIT License` が `webif/Cargo.toml` の `license = "MIT"` の値 `MIT` と整合（`MIT License` の最初の単語が `MIT`）— `head -1 LICENSE | awk '{print $1}'` が `MIT` を出力 AND `grep -E '^license = "MIT"' webif/Cargo.toml` が一致行を返す
    - 既存の `[dependencies]` セクションが破壊されていない — `cargo metadata --offline` がエラーなく実行できる（依存解決失敗なら破壊された証拠）
  </acceptance_criteria>
  <done>
    `webif/Cargo.toml` が description / repository / license="MIT" / authors / readme="../README.md" / edition="2021" のすべてを持ち、`cargo metadata` で取得できる。LICENSE 1 行目 `MIT License` と `license = "MIT"` の SPDX が一致する。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本プロジェクト → 下流の依存者（このコードを利用・配布する第三者） | ライセンス表明が法的な利用条件として伝播する境界 |
| 本プロジェクト → crates.io / GitHub | メタデータが公開レジストリに登録される境界（将来的に publish される可能性を含む） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | R (否認) / 法的リスク | LICENSE ファイルと `Cargo.toml` license フィールドの不一致 | mitigate | 同一プラン内で両ファイルを同時に作成し、SPDX 識別子 `MIT` で機械的に一致を検証（Task 2 acceptance: `head -1 LICENSE` の先頭単語 = Cargo.toml `license` 値） |
| T-01-07 | I (情報開示) | `repository` URL に開発者個人を特定する情報が混入するリスク | accept | リポジトリホストの URL は本来公開される情報。著作権者名も `ht-webif contributors` という匿名集合表記にしており、個人特定情報を含まない |
| T-01-08 | T (改ざん) | LICENSE 本文の改変による法的有効性の喪失 | mitigate | LICENSE は OSI 公式 MIT テンプレートそのまま使用、本文の改変を action で禁止 |
</threat_model>

<verification>
1. `LICENSE` がリポジトリルートに存在し MIT 全文を含む
2. `webif/Cargo.toml` に 6 つのメタデータフィールドがすべて存在
3. `cargo metadata --format-version 1 --no-deps` で全フィールドが取得可能
4. LICENSE 1 行目と Cargo.toml `license` の SPDX 識別子が一致（共に MIT）
</verification>

<success_criteria>
- ROADMAP.md Phase 1 Success Criteria #2 を満たす: 「LICENSE ファイル（MIT または Apache-2.0）が存在し、webif/Cargo.toml の license フィールドと一致する」
- ROADMAP.md Phase 1 Success Criteria #4 を満たす: 「Cargo.toml に description、repository、license、authors、readme、edition が揃い、cargo metadata で取得できる」
- REPO-02 完了、REPO-04 完了
</success_criteria>

<output>
完了後、`.planning/phases/01-repository-hygiene/01-02-SUMMARY.md` を作成する。
</output>
