---
id: 03-03
phase: 03-testing-ci-automation
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - webif/src/mcp.rs
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "`cargo test --release` で `webif/src/mcp.rs` 末尾の `#[cfg(test)] mod tests` 内の MCP JSON-RPC 単体テストが全て green"
    - "`McpClient::from_streams` 経由で `request()` が書き出す JSON 行が `jsonrpc:\"2.0\"` / `id` / `method` / `params` を含むことを serde_json::Value で検証している"
    - "`next_id` が呼出ごとに +1 されることを検証するテストが存在する"
    - "`pub(crate) struct FakeMcp`（scripted VecDeque reply 方式）が定義され、`#[async_trait] impl Mcp for FakeMcp` を備える（Wave 3 の http テストから参照可能）"
  artifacts:
    - path: "webif/src/mcp.rs"
      provides: "末尾に co-located な #[cfg(test)] mod tests と pub(crate) FakeMcp"
      contains: "FakeMcp"
  key_links:
    - from: "webif/src/mcp.rs (mod tests)"
      to: "McpClient::request, McpClient::from_streams, trait Mcp"
      via: "tokio::io::duplex"
      pattern: "tokio::io::duplex"
---

<objective>
TEST-02「MCP クライアントの JSON-RPC リクエスト/レスポンス組み立て単体テスト」を実装する。`McpClient::from_streams`（Plan 03-01 で `#[cfg(test)] pub` 化済み、`next_id` 初期値 0）と `tokio::io::duplex` を組み合わせ、`request()` / `next_id` の挙動を hermetic にテストする。加えて、Wave 3 の HTTP 統合テスト（Plan 03-04）と Wave 1 の `trait Mcp` テストダブルから使う `pub(crate) struct FakeMcp` を `webif/src/mcp.rs` 末尾の `#[cfg(test)] mod tests` 内に定義する（D-07）。

Purpose: MCP transport の JSON-RPC envelope 組み立てロジックが、後の変更でリグレッションしないことを `cargo test` で検知できる。また、Wave 3 の HTTP テストが必要とする `FakeMcp` の供給源を兼ねる。
Output: `webif/src/mcp.rs` 末尾の `#[cfg(test)] mod tests` ブロック（FakeMcp + 3 〜 5 本の単体テスト）。`cargo test --release` で全 green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-testing-ci-automation/03-CONTEXT.md
@.planning/phases/03-testing-ci-automation/03-PATTERNS.md
@.planning/phases/03-testing-ci-automation/03-01-PLAN.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

@webif/src/mcp.rs
@webif/Cargo.toml

<interfaces>
<!-- Plan 03-01 後の trait Mcp と McpClient::from_streams の前提 -->

From webif/src/mcp.rs (after 03-01):
```rust
#[async_trait]
pub trait Mcp: Send {
    async fn handshake(&mut self) -> Result<()>;
    async fn create_claude_session(&mut self) -> Result<String>;
    async fn close_session(&mut self, session_id: &str) -> Result<()>;
    async fn send_keys(&mut self, session_id: &str, keys: &[String]) -> Result<()>;
    async fn submit_line(&mut self, session_id: &str, text: &str) -> Result<()>;
    async fn snapshot(&mut self, session_id: &str) -> Result<String>;
}

#[cfg(test)]
pub fn from_streams<W, R>(stdin: W, stdout: R) -> Self
where W: AsyncWrite + Send + Unpin + 'static, R: AsyncRead + Send + Unpin + 'static
// from_streams で作ったインスタンスは next_id: 0 で初期化される。
// よって最初の request 呼び出しで next_id += 1 された結果 id=1 が JSON に乗る。

pub async fn request(&mut self, method: &str, params: Value) -> Result<Value>
```

FakeMcp の構造（D-07 / PATTERNS.md lines 583-602）:
```rust
pub(crate) struct FakeMcp {
    pub create_session_replies: VecDeque<Result<String>>,
    pub snapshot_replies: VecDeque<Result<String>>,
    pub send_keys_log: Vec<(String, Vec<String>)>,
    // 必要に応じて submit_line_log / handshake_replies なども
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: FakeMcp 定義（pub(crate)、scripted reply queue 方式、Mcp 実装）</name>
  <files>webif/src/mcp.rs</files>
  <read_first>
    - webif/src/mcp.rs（trait Mcp の最終シグネチャ — Plan 03-01 完了後）
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §"FakeMcp の構造体パターン" / lines 577-605
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md D-06 / D-07 / D-25
    - .planning/codebase/TESTING.md lines 98-115（FakeMcp 推奨パターン）
    - CLAUDE.md（日本語コメント、anyhow 慣行）
  </read_first>
  <action>
    `webif/src/mcp.rs` の末尾に `#[cfg(test)] mod tests { ... }` ブロックを新規追加し（または Plan 03-01 で既にあれば追記）、以下を定義する。

    (a) mod 冒頭 import:
      ```
      use super::*;
      use std::collections::VecDeque;
      use tokio::io::duplex;
      ```

    (b) `pub(crate) struct FakeMcp` を定義（D-07 — `pub(crate)` で同一クレート内公開、Wave 3 の `http.rs` テストから `use crate::mcp::tests::FakeMcp;` で参照可能）:
      ```
      pub(crate) struct FakeMcp {
          pub handshake_calls: u32,
          pub create_session_replies: VecDeque<Result<String>>,
          pub close_session_log: Vec<String>,
          pub send_keys_log: Vec<(String, Vec<String>)>,
          pub submit_line_log: Vec<(String, String)>,
          pub snapshot_replies: VecDeque<Result<String>>,
      }
      ```

    (c) `impl FakeMcp` に `pub(crate) fn new() -> Self` を提供（全フィールドを空 / デフォルトで初期化）。

    (d) `#[async_trait] impl Mcp for FakeMcp` を実装:
      - `handshake`: `self.handshake_calls += 1; Ok(())`
      - `create_claude_session`: `self.create_session_replies.pop_front().unwrap_or_else(|| Err(anyhow!("create_claude_session scripted reply 切れ")))`
      - `close_session(session_id)`: `self.close_session_log.push(session_id.to_string()); Ok(())`
      - `send_keys(session_id, keys)`: `self.send_keys_log.push((session_id.to_string(), keys.to_vec())); Ok(())`
      - `submit_line(session_id, text)`: `self.submit_line_log.push((session_id.to_string(), text.to_string())); Ok(())`
      - `snapshot(session_id)`: `let _ = session_id; self.snapshot_replies.pop_front().unwrap_or_else(|| Err(anyhow!("snapshot scripted reply 切れ")))`

    日本語コメントで各メソッドの意図を 1 行ずつ記載（例: `// scripted reply キューから先頭を pop。空ならエラー。`）。

    注: `FakeMcp` を 「`tests` モジュール内に置く」のではなく **`mcp.rs` の本体に `#[cfg(test)] pub(crate) struct FakeMcp`** として置く案もあるが、CONTEXT D-07 は明確に「`mcp.rs` 末尾の `#[cfg(test)] mod tests` の中で定義」と指定しているので **`mod tests` 内に置く**。Wave 3 の `http.rs` テストからは `use crate::mcp::tests::FakeMcp;` で参照する（mod tests 自体に `#[cfg(test)]` がかかるので、参照側も `#[cfg(test)] mod tests` 内からのみアクセス可能 — これは仕様通り）。

    パス補足: `use crate::mcp::tests::FakeMcp;` は 同一クレート内の他モジュールの test mod から参照する場合に有効（test mod は `#[cfg(test)]` で同一 cfg 下にあるため）。
  </action>
  <verify>
    <automated>cd webif && cargo test --release --lib mcp::tests:: 2>&1 | tee /tmp/test-03-03-fakemcp.log && grep -c 'pub(crate) struct FakeMcp' src/mcp.rs && grep -c 'impl Mcp for FakeMcp' src/mcp.rs && cd webif && cargo build --tests 2>&1 | tee /tmp/build-tests-03-03.log</automated>
  </verify>
  <acceptance_criteria>
    - `webif/src/mcp.rs` に `#[cfg(test)] mod tests` ブロックが含まれる
    - `mod tests` 内に `pub(crate) struct FakeMcp` が定義されている
    - `FakeMcp` は最低限 `create_session_replies: VecDeque<Result<String>>` と `snapshot_replies: VecDeque<Result<String>>` を持つ
    - `impl FakeMcp { pub(crate) fn new() -> Self { ... } }` が定義されている
    - `#[async_trait] impl Mcp for FakeMcp` がすべての trait メソッド（handshake / create_claude_session / close_session / send_keys / submit_line / snapshot）を実装している
    - `cd webif && cargo build --tests` が exit 0
  </acceptance_criteria>
  <done>
    `FakeMcp` が `mcp.rs` の test mod に定義され、Wave 3 の http テストから `use crate::mcp::tests::FakeMcp;` で参照できる状態。
  </done>
</task>

<task type="auto">
  <name>Task 2: McpClient::request の JSON-RPC envelope と next_id 単体テスト</name>
  <files>webif/src/mcp.rs</files>
  <read_first>
    - webif/src/mcp.rs（特に `request` lines 67-82 と `write_message` / `read_response` / `next_id` の動作）
    - Plan 03-01 で追加された `#[cfg(test)] pub fn from_streams` の最終シグネチャ
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §"TEST-02 単体テストの題材" lines 147-152
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md D-05（duplex 経由のテスト方式）
    - .planning/codebase/TESTING.md lines 65-67（tokio::test）
  </read_first>
  <action>
    Task 1 の `mod tests` ブロックに以下の `#[tokio::test]` を追加する。`tokio::io::duplex(1024)` で in-memory な (writer, reader) ペアを 2 組作り、`McpClient::from_streams(client_stdin, client_stdout)` に渡す。テスト側は、client 側が「stdin に書き出した JSON 行」を test harness 側のリーダで読み、また test harness 側からは「擬似 ht-mcp 応答」を client の stdout に書き戻す。

    `duplex` の構造:
      - `let (client_writes_here, test_reads_here) = duplex(1024);` → `McpClient` が `stdin` として `client_writes_here` を持ち、テストは `test_reads_here` で client が送ったバイトを読める
      - `let (test_writes_here, client_reads_here) = duplex(1024);` → `McpClient` が `stdout` として `client_reads_here` を持ち、テストは `test_writes_here` でレスポンス JSON 行を書き込める

    Plan 03-01 で `from_streams` のシグネチャは `pub fn from_streams<W, R>(stdin: W, stdout: R) -> Self where W: AsyncWrite + Send + Unpin + 'static, R: AsyncRead + Send + Unpin + 'static` で確定。`tokio::io::DuplexStream` はこのバウンドを満たすので、テスト側からは `McpClient::from_streams(client_writes_here, client_reads_here)` で渡せる（Box 化不要）。

    (a) `#[tokio::test] async fn request_builds_jsonrpc_envelope_with_method_and_params()`:
      - duplex を 2 組作る
      - `let mut client = McpClient::from_streams(client_stdin, client_stdout);`
      - `tokio::spawn` で背景タスクを起こし、client.request("tools/call", json!({"name": "ht_take_snapshot"})) を実行
      - テスト harness 側 (test_reads_here) で client が書いた最初の行を `tokio::io::AsyncBufReadExt::read_line` で 1 行読み取り、`serde_json::from_str::<Value>(&line)` でパース
      - パース結果について検証:
        - `assert_eq!(parsed.get("jsonrpc").and_then(Value::as_str), Some("2.0"));`
        - `assert!(parsed.get("id").and_then(Value::as_i64).is_some());`
        - `assert_eq!(parsed.get("method").and_then(Value::as_str), Some("tools/call"));`
        - `assert!(parsed.get("params").is_some());`
        - `assert_eq!(parsed["params"]["name"].as_str(), Some("ht_take_snapshot"));`
      - クライアントタスクが応答待ちで停止しないよう、harness 側から擬似応答 `{"jsonrpc":"2.0","id":<同じid>,"result":{"content":[{"text":"snap-data"}]}}\n` を `test_writes_here` に書き込む
      - `tokio::time::timeout(Duration::from_secs(5), task).await` で client タスクの完了を待ち、結果を unwrap

    (b) `#[tokio::test] async fn next_id_increments_per_request()`:
      - 同じく duplex で client を作る（`from_streams` 経由なので `next_id: 0` で初期化される — Plan 03-01 acceptance で保証）
      - 1 回目の request 後、harness 側で 1 行目を読んで `id == 1` を確認
      - 2 回目の request 後、harness 側で 2 行目を読んで `id == 2` を確認
      - すなわち `id1 == 1`, `id2 == 2`, `id2 == id1 + 1`
      - （応答も都度書き戻す）

    (c) `#[tokio::test] async fn request_extracts_result_from_response()`:
      - request の戻り値が応答 JSON の `result` フィールドの clone を返すことを検証
      - 擬似応答: `{"jsonrpc":"2.0","id":1,"result":{"foo":"bar"}}\n`
      - `let v = client.request("dummy", json!({})).await.unwrap();`
      - `assert_eq!(v.get("foo").and_then(Value::as_str), Some("bar"));`

    日本語コメントで各テストの意図を 1 行ずつ記す（例: `// JSON-RPC envelope に jsonrpc/id/method/params が揃うことを検証`）。

    `cargo clippy --all-targets --release -- -D warnings` warning 0 維持。`unused_variables` を出さないため、duplex の不要な戻り値は `_` バインドする。

    `tokio::io::AsyncBufReadExt` を使うため import が必要なら追加。`BufReader` は本体 imports 済み（`use tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader, Lines}` lines 7）。

    実装上の注意:
      - `McpClient::from_streams` のシグネチャは Plan 03-01 で `pub fn from_streams<W, R>(stdin: W, stdout: R) -> Self where W: AsyncWrite + Send + Unpin + 'static, R: AsyncRead + Send + Unpin + 'static` を採用済み。テスト側は `DuplexStream` を直接渡せる。
      - duplex はバッファサイズ 1024 で十分（テストの JSON 行は 200 B 程度）。
      - 応答書き込み後は `flush()` を忘れない（writer を `&mut` で持っていれば `AsyncWriteExt::flush(&mut writer).await.unwrap();`）。
  </action>
  <verify>
    <automated>cd webif && cargo test --release --lib mcp::tests:: 2>&1 | tee /tmp/test-03-03-request.log && grep -E 'test result.*[1-9][0-9]* passed.*0 failed' /tmp/test-03-03-request.log && cd webif && cargo clippy --all-targets --release -- -D warnings 2>&1 | tee /tmp/clippy-03-03.log</automated>
  </verify>
  <acceptance_criteria>
    - `webif/src/mcp.rs` の `mod tests` に `fn request_builds_jsonrpc_envelope_with_method_and_params()` が存在
    - `webif/src/mcp.rs` の `mod tests` に `fn next_id_increments_per_request()` が存在（`id1 == 1`, `id2 == 2` の確認を含む — Plan 03-01 の `next_id: 0` 初期化前提）
    - `webif/src/mcp.rs` の `mod tests` に `fn request_extracts_result_from_response()` が存在
    - 3 テスト全て `#[tokio::test]` 属性付き
    - テストは `McpClient::from_streams` + `tokio::io::duplex` を使い、実プロセスを起動しない
    - `cd webif && cargo test --release` の出力に `test result.*[1-9][0-9]* passed.*0 failed` の行が存在する
    - `cd webif && cargo clippy --all-targets --release -- -D warnings` が exit 0
  </acceptance_criteria>
  <done>
    `McpClient::request` の JSON-RPC envelope 組み立てと `next_id` インクリメント、response の `result` 抽出が `tokio::io::duplex` ベースの hermetic テストでカバーされ、全 green。
  </done>
</task>

</tasks>

<verification>
TEST-02 のカバレッジ:
1. JSON-RPC envelope の jsonrpc / id / method / params フィールド組み立て
2. `next_id` が呼出ごとに +1 される（初期 0 → 1 → 2）
3. 応答 JSON の `result` フィールドが request の戻り値として返される
4. FakeMcp の用意（Wave 3 で消費）

検証コマンド:
1. `cd webif && cargo test --release` — `test result.*[1-9][0-9]* passed.*0 failed` がマッチする
2. `cd webif && cargo clippy --all-targets --release -- -D warnings` — exit 0
3. `grep -c 'pub(crate) struct FakeMcp' webif/src/mcp.rs` >= 1
4. `grep -c 'impl Mcp for FakeMcp' webif/src/mcp.rs` >= 1
5. `grep -c 'tokio::io::duplex\|use tokio::io::duplex' webif/src/mcp.rs` >= 1
</verification>

<success_criteria>
- FakeMcp が `pub(crate)` で `mcp.rs` の test mod に定義され、`Mcp` trait を実装する
- McpClient::request の JSON-RPC envelope を hermetic にテストできる（duplex 経由）
- `next_id` インクリメントと `result` フィールド抽出がテストでカバーされる
- `cargo test --release` の出力で `test result.*[1-9][0-9]* passed.*0 failed` がマッチする（少なくとも 1 つ passed かつ 0 failed）
- clippy warning 0 維持
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci-automation/03-03-SUMMARY.md` recording:
- 追加した FakeMcp の最終フィールド集合
- 追加した MCP request テスト 3 本の名前
- `McpClient::from_streams` のシグネチャが Plan 03-01 と整合したか確認結果（`<W, R>` 直渡しでテストが書けたか / Box 化が必要だったか）
- `cargo test --release` の最終結果
</output>
</content>
</invoke>