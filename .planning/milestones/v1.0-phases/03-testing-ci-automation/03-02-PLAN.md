---
id: 03-02
phase: 03-testing-ci-automation
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - webif/src/turn.rs
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "`cargo test --release` で `webif/src/turn.rs` 末尾の `#[cfg(test)] mod tests` 内の純粋関数テストが全て green"
    - "`build_prompt_body` がタスク本文・result パス・status パス・「【ht-webif 出力規約】」文字列を含むことを検証するテストが存在する"
    - "`read_turn` の 3 パターン（完全な done / garbled JSON → unknown / result ファイル不在 → 空文字）テストが存在する"
    - "turn_id formatter（`%Y%m%d-%H%M%S-%3f`）の形式検証テスト（19 文字・3 セグメント・各セグメント全数字）が存在する"
  artifacts:
    - path: "webif/src/turn.rs"
      provides: "末尾に co-located な #[cfg(test)] mod tests（純粋関数の単体テスト群）"
      contains: "#[cfg(test)]"
  key_links:
    - from: "webif/src/turn.rs (mod tests)"
      to: "build_prompt_body, read_turn"
      via: "use super::*"
      pattern: "use super::\\*"
---

<objective>
TEST-01「純粋関数の単体テスト」を実装する。対象は `webif/src/turn.rs` の `build_prompt_body`（完全な純関数）と `read_turn`（tempdir 入力に対する出力テスト）、および `chrono::Utc::now().format("%Y%m%d-%H%M%S-%3f")` の formatter 形式検証。D-06 の co-location 規約に従い、`webif/src/turn.rs` 末尾に `#[cfg(test)] mod tests` を追加する形式。`webif/tests/` 統合テストディレクトリは作らない。

Purpose: Phase 2 でリファクタした純粋関数の挙動が、以後の変更でリグレッションしないことを `cargo test` で検知できるようにする。これは ROADMAP Phase 3 Success Criterion #1 の中核。
Output: `webif/src/turn.rs` 末尾の `#[cfg(test)] mod tests` ブロック。`cargo test --release` で全テスト green。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-testing-ci-automation/03-CONTEXT.md
@.planning/phases/03-testing-ci-automation/03-PATTERNS.md
@.planning/phases/03-testing-ci-automation/03-01-PLAN.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

@webif/src/turn.rs
@webif/Cargo.toml

<interfaces>
<!-- テスト対象の純粋関数シグネチャ -->

From webif/src/turn.rs:
```rust
pub fn build_prompt_body(task: &str, result_path: &Path, status_path: &Path) -> String
pub async fn read_turn(turns_dir: &Path, turn_id: &str) -> Result<Value>
```

turn_id formatter のロケーション:
- `webif/src/http.rs:48` で `chrono::Utc::now().format("%Y%m%d-%H%M%S-%3f").to_string()` が呼ばれている
- 本 Plan ではこの文字列形式そのものを直接検証する小さなテストを `turn.rs` の mod tests 内に書く（または `chrono` を直接呼んでフォーマット結果をチェック）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: build_prompt_body と turn_id formatter の単体テスト追加</name>
  <files>webif/src/turn.rs</files>
  <read_first>
    - webif/src/turn.rs（既存純粋関数 build_prompt_body lines 21-35 全文と prompt 規約テキスト）
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`webif/src/turn.rs`（テスト例 lines 252-262）
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md §"Implementation Decisions" D-06 / D-11
    - .planning/codebase/CONVENTIONS.md §"Language for Comments and Logs"（日本語コメント / 英語識別子）
    - .planning/codebase/TESTING.md §"#[cfg(test)] mod tests" co-location パターン
  </read_first>
  <action>
    `webif/src/turn.rs` の末尾（EOF）に `#[cfg(test)] mod tests` ブロックを新規追加する。以下 2 テスト関数を含める。

    (a) `#[test] fn build_prompt_body_includes_task_and_paths()`:
      - `result_path = PathBuf::from("/tmp/result-X.txt")`、`status_path = PathBuf::from("/tmp/status-X.json")` を作る
      - `let body = build_prompt_body("やってほしいこと", &result_path, &status_path);`
      - `assert!(body.contains("やってほしいこと"));`
      - `assert!(body.contains("/tmp/result-X.txt"));`
      - `assert!(body.contains("/tmp/status-X.json"));`
      - `assert!(body.contains("【ht-webif 出力規約】"));`
      - 検証ポイント（D-11）: タスク本文・result パス・status パス・出力規約マーカー の 4 点を含む

    (b) `#[test] fn turn_id_formatter_matches_expected_shape()`:
      - `let id = chrono::Utc::now().format("%Y%m%d-%H%M%S-%3f").to_string();`
      - 形式は `YYYYMMDD-HHMMSS-mmm`（数字 8 桁 + `-` + 数字 6 桁 + `-` + 数字 3 桁）
      - 標準ライブラリで形式チェック（regex クレート追加禁止）:
        - `assert_eq!(id.len(), 19);` （8+1+6+1+3 = 19）
        - `let parts: Vec<&str> = id.split('-').collect(); assert_eq!(parts.len(), 3);`
        - `assert_eq!(parts[0].len(), 8); assert!(parts[0].chars().all(|c| c.is_ascii_digit()));`
        - `assert_eq!(parts[1].len(), 6); assert!(parts[1].chars().all(|c| c.is_ascii_digit()));`
        - `assert_eq!(parts[2].len(), 3); assert!(parts[2].chars().all(|c| c.is_ascii_digit()));`
      - 検証ポイント（D-11）: HT-PROTOCOL §3 turn_id 規格（UTC ミリ秒精度）に一致する形式を生成できる

    mod 冒頭で `use super::*;` を入れる。テスト関数名は snake_case でシナリオ記述形式（D-06 / PATTERNS.md §"#[cfg(test)] mod tests co-location 規約"）。

    各テストの冒頭に日本語コメントを 1 行付ける（CLAUDE.md / CONVENTIONS.md）。

    依存追加禁止: regex クレートは追加しない。標準ライブラリだけで形式チェックする（依存最小化、CONTEXT specifics line 159 の最小化方針に従う）。
  </action>
  <verify>
    <automated>cd webif && cargo test --release --lib turn::tests::build_prompt_body_includes_task_and_paths 2>&1 | grep -E 'test result.*[1-9][0-9]* passed.*0 failed' && cd webif && cargo test --release --lib turn::tests::turn_id_formatter_matches_expected_shape 2>&1 | grep -E 'test result.*[1-9][0-9]* passed.*0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `webif/src/turn.rs` 末尾に `#[cfg(test)] mod tests` ブロックが存在し、`use super::*;` を含む
    - `fn build_prompt_body_includes_task_and_paths()` が存在し、上記 4 つの assert を含む
    - `fn turn_id_formatter_matches_expected_shape()` が存在し、`YYYYMMDD-HHMMSS-mmm` 形式を検証する
    - `cd webif && cargo test --release` で当該 2 テストとも passed
    - `webif/Cargo.toml` に regex クレートが追加されていない
  </acceptance_criteria>
  <done>
    `build_prompt_body` と turn_id formatter の純粋関数テストが追加され、`cargo test --release` で green。
  </done>
</task>

<task type="auto">
  <name>Task 2: read_turn の 3 パターン単体テスト（done / unknown / result 不在）追加</name>
  <files>webif/src/turn.rs</files>
  <read_first>
    - webif/src/turn.rs（read_turn lines 97-109 — 特に `.unwrap_or_else(|| "unknown".to_string())` フォールバック分岐と `.unwrap_or_default()` の result 不在分岐）
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`webif/src/turn.rs`（read_turn 3 パターン解説）
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md D-11（read_turn 3 パターン規定）
    - .planning/codebase/TESTING.md lines 75-77（tempfile::tempdir 推奨）
  </read_first>
  <action>
    Task 1 で作った `#[cfg(test)] mod tests` ブロックに以下 3 つの async テストを追加する。`tempfile::tempdir` で一時ディレクトリを作り、`tokio::fs::write` で `status-<id>.json` と `result-<id>.txt` を事前配置してから `read_turn` を呼ぶ。

    モジュール冒頭に追加する import: `use tempfile::tempdir;`（test mod 内 import で副作用最小化）

    (a) `#[tokio::test] async fn read_turn_returns_done_when_status_complete()`:
      - `let dir = tempdir().unwrap();`
      - `let id = "20260525-000000-000";`
      - `tokio::fs::write(dir.path().join(format!("status-{id}.json")), "{\"status\":\"done\"}\n").await.unwrap();`
      - `tokio::fs::write(dir.path().join(format!("result-{id}.txt")), "回答本文").await.unwrap();`
      - `let v = read_turn(dir.path(), id).await.unwrap();`
      - `assert_eq!(v.get("turn_id").and_then(Value::as_str), Some(id));`
      - `assert_eq!(v.get("status").and_then(Value::as_str), Some("done"));`
      - `assert_eq!(v.get("result").and_then(Value::as_str), Some("回答本文"));`

    (b) `#[tokio::test] async fn read_turn_marks_unknown_when_status_garbled()`:
      - tempdir 作成
      - `let id = "X";`
      - `tokio::fs::write(dir.path().join(format!("status-{id}.json")), "not a json{{{").await.unwrap();`
      - `tokio::fs::write(dir.path().join(format!("result-{id}.txt")), "").await.unwrap();`
      - `let v = read_turn(dir.path(), id).await.unwrap();`
      - `assert_eq!(v.get("status").and_then(Value::as_str), Some("unknown"));` （`read_turn` の `.unwrap_or_else(|| "unknown".to_string())` フォールバックを検証）
      - 注: garbled = `serde_json::from_str` が Err を返すケース。`not a json{{{` が該当。

    (c) `#[tokio::test] async fn read_turn_returns_empty_result_when_result_file_missing()`:
      - tempdir 作成
      - `let id = "Y";`
      - `tokio::fs::write(dir.path().join(format!("status-{id}.json")), "{\"status\":\"done\"}\n").await.unwrap();`
      - `result-Y.txt` は **書かない** （`tokio::fs::read_to_string(&result_path).await.unwrap_or_default()` パスを検証）
      - `let v = read_turn(dir.path(), id).await.unwrap();`
      - `assert_eq!(v.get("status").and_then(Value::as_str), Some("done"));`
      - `assert_eq!(v.get("result").and_then(Value::as_str), Some(""));` （空文字フォールバック）

    各テスト前に日本語の `//` 説明コメント（例: `// 完全な done を返す happy path`、`// status JSON が壊れたら unknown にフォールバック`、`// result ファイル不在は空文字フォールバック`）。

    `serde_json::Value` は本体 imports 済みで `use super::*;` 経由で取り込まれる。`tempfile::tempdir` は test mod 専用 import。

    最終確認: `cd webif && cargo test --release` で 5 テスト（Task 1 の 2 本 + Task 2 の 3 本）全 passed。`cd webif && cargo clippy --all-targets --release -- -D warnings` で warning 0。
  </action>
  <verify>
    <automated>cd webif && cargo test --release 2>&1 | tee /tmp/test-03-02.log && grep -E 'test result.*[1-9][0-9]* passed.*0 failed' /tmp/test-03-02.log && cd webif && cargo clippy --all-targets --release -- -D warnings 2>&1 | tee /tmp/clippy-03-02.log</automated>
  </verify>
  <acceptance_criteria>
    - `webif/src/turn.rs` の `mod tests` 内に `use tempfile::tempdir;` が含まれる
    - `fn read_turn_returns_done_when_status_complete()` が存在する
    - `fn read_turn_marks_unknown_when_status_garbled()` が存在する
    - `fn read_turn_returns_empty_result_when_result_file_missing()` が存在する
    - 3 つのテストは全て `#[tokio::test]` 属性付き
    - `cd webif && cargo test --release` の出力に `test result.*[1-9][0-9]* passed.*0 failed` の行が存在する（少なくとも 1 つ passed かつ 0 failed）
    - `cd webif && cargo clippy --all-targets --release -- -D warnings` が exit 0
  </acceptance_criteria>
  <done>
    `read_turn` の 3 パターンが unit test でカバーされ、`cargo test --release` で green、clippy も warning 0。
  </done>
</task>

</tasks>

<verification>
TEST-01 のカバレッジ:
1. `build_prompt_body` — タスク・両パス・規約マーカーの 4 要素を含むことを検証
2. `read_turn` — done / unknown / result 不在 の 3 パターン
3. turn_id formatter — `%Y%m%d-%H%M%S-%3f` が `YYYYMMDD-HHMMSS-mmm`（19 文字、3 セグメント、全数字）になることを検証

検証コマンド:
1. `cd webif && cargo test --release` — `test result.*[1-9][0-9]* passed.*0 failed` がマッチする
2. `cd webif && cargo clippy --all-targets --release -- -D warnings` — exit 0
3. `grep -c '^#\[cfg(test)\]' webif/src/turn.rs` >= 1
4. `grep -c 'fn build_prompt_body_includes_task_and_paths\|fn turn_id_formatter_matches_expected_shape\|fn read_turn_returns_done\|fn read_turn_marks_unknown\|fn read_turn_returns_empty_result' webif/src/turn.rs` = 5
</verification>

<success_criteria>
- 5 つの単体テストが追加され、すべて `cargo test --release` で green
- D-06 の co-location 規約に従い、テストは `webif/src/turn.rs` 末尾の `#[cfg(test)] mod tests` 内
- `webif/tests/` 統合テストディレクトリは作られていない
- 新規依存（regex 等）が追加されていない
- clippy warning 0 を維持
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci-automation/03-02-SUMMARY.md` recording:
- 追加した 5 テストの名前一覧
- read_turn の `unknown` フォールバックを発火させた garbled 文字列の具体例
- `cargo test --release` の最終出力（test result 行）
- 依存追加なしで turn_id 形式チェックを実装できたこと
</output>
</content>
</invoke>