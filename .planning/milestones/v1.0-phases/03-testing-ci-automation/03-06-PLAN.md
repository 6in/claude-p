---
id: 03-06
phase: 03-testing-ci-automation
plan: 06
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [TOOL-02]
must_haves:
  truths:
    - "`.github/workflows/ci.yml` がリポジトリ直下の `.github/workflows/` 配下に存在する"
    - "ワークフローのトリガが `push: branches: [main]` と `pull_request: branches: [main]` の両方を含む"
    - "`runs-on: ubuntu-latest` 単一 OS でジョブを動かす"
    - "`actions-rust-lang/setup-rust-toolchain@v1` で `toolchain: stable`、`components: rustfmt, clippy` を取得する"
    - "`Swatinem/rust-cache@v2` で `workspaces: webif -> webif/target` のキャッシュを設定している"
    - "単一 `check` ジョブで `cargo fmt --all -- --check` → `cargo clippy --all-targets --release -- -D warnings` → `cargo test --release` を直列実行する"
    - "`defaults.run.working-directory: webif` でジョブ内 step が `cd webif` を省略できる"
    - "ワークフローが justfile を呼ばず、cargo を直接呼ぶ"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "push / PR トリガの単一 check ジョブ（fmt --check → clippy → test、release profile）"
      contains: "cargo clippy --all-targets --release -- -D warnings"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "webif/ (cargo project root)"
      via: "defaults.run.working-directory: webif"
      pattern: "working-directory: webif"
---

<objective>
TOOL-02「`.github/workflows/ci.yml` で push/PR 時に `cargo fmt --check`・`cargo clippy -- -D warnings`・`cargo test` を自動実行」を実装する。リポジトリ直下の `.github/workflows/` ディレクトリと `ci.yml` を新規作成する（`.github/` 自体が現状未存在）。D-16 〜 D-23 の決定を全て反映: ubuntu-latest 単一 / stable 単一 / Swatinem/rust-cache@v2 / 単一 `check` ジョブで直列実行 / `defaults.run.working-directory: webif` / justfile は呼ばない。

Purpose: 以後のコード変更で fmt / clippy / test のいずれかが失敗すると CI が赤くなる状態を作る（ROADMAP Phase 3 Success Criterion #4）。
Output: `.github/workflows/ci.yml`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-testing-ci-automation/03-CONTEXT.md
@.planning/phases/03-testing-ci-automation/03-PATTERNS.md
@CLAUDE.md

@webif/Cargo.toml

<interfaces>
<!-- 配置と前提 -->

リポジトリ root: `/home/parallels/workspaces/ht-mcp-sample/`
配置: `/home/parallels/workspaces/ht-mcp-sample/.github/workflows/ci.yml`
cargo project root: `webif/`（リポジトリ直下から見て）
Cargo.lock: `webif/Cargo.lock`（rust-cache のキー）

D-16 〜 D-23 の決定:
- D-16: `.github/workflows/` に配置（git root が親 ht-mcp-sample/）
- D-17: トリガ `push: branches:[main]` / `pull_request: branches:[main]`（フォーク PR の duplicate 抑制）
- D-18: ubuntu-latest 単一（macOS / Windows は v2）
- D-19: stable 単一、`actions-rust-lang/setup-rust-toolchain@v1` で `components: rustfmt, clippy`
- D-20: `Swatinem/rust-cache@v2` で `workspaces: webif -> webif/target`
- D-21: 単一 `check` ジョブで fmt → clippy → test 直列
- D-22: `defaults.run.working-directory: webif` で各 step の cd 省略
- D-23: CI から justfile を呼ばない（cargo 直接）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .github/workflows/ ディレクトリ作成 + ci.yml 新規作成</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`.github/workflows/ci.yml`（新規）lines 482-537（完全な YAML スケルトン提示済み）
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md §"CI ワークフロー" D-16 〜 D-23
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md "specifics" line 160（`cargo test --release` を採用する根拠）
  </read_first>
  <action>
    `.github/workflows/` ディレクトリと `ci.yml` をリポジトリ直下に新規作成する。Write tool で書く（heredoc 禁止）。

    YAML スケルトン（PATTERNS.md lines 490-528 のスケルトンに完全準拠）:

    - `name: CI`
    - トリガ:
      - `on.push.branches: [main]`
      - `on.pull_request.branches: [main]`
    - jobs:
      - `check`:
        - `name: fmt + clippy + test`
        - `runs-on: ubuntu-latest`（D-18）
        - `defaults.run.working-directory: webif`（D-22 — 各 step で `cd webif` 省略）
        - `steps`:
          1. `uses: actions/checkout@v4`
          2. `name: Install Rust toolchain` / `uses: actions-rust-lang/setup-rust-toolchain@v1` / `with: { toolchain: stable, components: rustfmt, clippy }` （D-19）
          3. `name: Cache cargo build` / `uses: Swatinem/rust-cache@v2` / `with: { workspaces: webif -> webif/target }` （D-20）
          4. `name: cargo fmt --check` / `run: cargo fmt --all -- --check`
          5. `name: cargo clippy` / `run: cargo clippy --all-targets --release -- -D warnings`（ROADMAP cross-cutting constraint 準拠）
          6. `name: cargo test` / `run: cargo test --release`（CI と release profile を揃える、CONTEXT specifics line 160）

    各 step の順序は必ず 「fmt-check → clippy → test」 の順とする（D-21）。並列ジョブ化はしない（D-21）。

    `justfile` を呼ぶ step は **追加しない**（D-23 — CI は cargo を直接呼ぶ）。

    YAML 内の `name:` や step 名は英語推奨（PATTERNS.md §"日本語コメント + 英語識別子" / GitHub UI が英語前提）。コメントは日本語可（任意、必須ではない）。

    その他 actions のバージョン:
    - `actions/checkout@v4`（GitHub 推奨の最新メジャー）
    - `actions-rust-lang/setup-rust-toolchain@v1`（D-19 指定）
    - `Swatinem/rust-cache@v2`（D-20 指定）

    YAML のインデントは半角スペース 2 個。`actions/checkout` / `setup-rust-toolchain` / `rust-cache` は必ず `uses:` キーで指定。

    ファイル末尾の改行を忘れない（POSIX text file 慣行）。
  </action>
  <verify>
    <automated>test -f /home/parallels/workspaces/ht-mcp-sample/.github/workflows/ci.yml && cd /home/parallels/workspaces/ht-mcp-sample && grep -q "name: CI" .github/workflows/ci.yml && grep -q "ubuntu-latest" .github/workflows/ci.yml && grep -q "actions-rust-lang/setup-rust-toolchain@v1" .github/workflows/ci.yml && grep -q "Swatinem/rust-cache@v2" .github/workflows/ci.yml && grep -q "working-directory: webif" .github/workflows/ci.yml && grep -q "cargo fmt --all -- --check" .github/workflows/ci.yml && grep -q "cargo clippy --all-targets --release -- -D warnings" .github/workflows/ci.yml && grep -q "cargo test --release" .github/workflows/ci.yml && grep -q "branches: \[main\]" .github/workflows/ci.yml && ! grep -q "just " .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` が存在する
    - `name: CI` が含まれる
    - トリガ `on:` に `push.branches: [main]` と `pull_request.branches: [main]` の両方が含まれる
    - 単一ジョブ `check:` が存在し、`runs-on: ubuntu-latest`
    - `defaults.run.working-directory: webif` が含まれる
    - step に `actions/checkout@v4` が含まれる
    - step に `actions-rust-lang/setup-rust-toolchain@v1` と `components: rustfmt, clippy` が含まれる
    - step に `Swatinem/rust-cache@v2` と `workspaces: webif -> webif/target` が含まれる
    - step `cargo fmt --all -- --check` が含まれる
    - step `cargo clippy --all-targets --release -- -D warnings` が含まれる
    - step `cargo test --release` が含まれる
    - step の順序は fmt-check → clippy → test
    - `just` コマンドを呼ぶ step が存在しない（D-23）
  </acceptance_criteria>
  <done>
    `.github/workflows/ci.yml` が配置され、push / PR トリガで ubuntu-latest 上の単一 `check` ジョブが fmt → clippy → test を直列実行する形になる。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: GitHub Actions の reachability 検証（人的確認）</name>
  <files>.github/workflows/ci.yml</files>
  <what-built>
    `.github/workflows/ci.yml` が新規配置された。push / pull_request (main) で単一 `check` ジョブが起動し、fmt-check → clippy → test を ubuntu-latest 上で直列実行する。
  </what-built>
  <how-to-verify>
    Phase 3 の commit を main に push、または PR を作って、GitHub Actions が起動し緑になることを確認する。

    1. リポジトリの GitHub UI で `Actions` タブを開く
    2. `CI` ワークフローが現れているか確認
    3. 直近の push / PR のジョブ `check` が緑になっているか確認
    4. （任意）試しに 1 行 fmt 違反を入れた branch を push し、CI が赤くなることを確認 → 元に戻す

    期待される結果:
    - Actions タブに `CI` ワークフローが表示される
    - push でも PR でもジョブが起動する
    - 通常状態（fmt / clippy / test 全部通る）で `check` ジョブが緑になる
    - 異常状態（fmt 違反など）を仕込むと `check` ジョブが赤くなる

    注: リモートに push せずに CI を確認する手段はない。`act` 等のローカルランナーは setup が重いので採用しない（オプション）。
  </how-to-verify>
  <resume-signal>
    Type "approved" if CI runs green on push/PR to main.
    Type "skip" if user wants to commit the workflow file without immediately verifying remote CI (verification deferred to first real push).
    Type the specific issue / Actions error if CI fails to start or returns errors not caused by code changes.
  </resume-signal>
</task>

</tasks>

<verification>
TOOL-02 のカバレッジ:
1. push / PR (main) トリガ
2. ubuntu-latest 単一 OS
3. stable toolchain + rustfmt + clippy
4. Swatinem/rust-cache キャッシュ
5. fmt-check → clippy → test の単一 `check` ジョブ直列実行
6. justfile を呼ばない（D-23）

検証コマンド:
1. `test -f .github/workflows/ci.yml`
2. `grep -c "cargo fmt --all -- --check\|cargo clippy --all-targets --release -- -D warnings\|cargo test --release" .github/workflows/ci.yml` = 3
3. `! grep -q '^\s*-\s*name:.*just\|run:\s*just' .github/workflows/ci.yml` (justfile を呼んでいないこと)
4. リモート push で CI が動作するかは人的検証
</verification>

<success_criteria>
- `.github/workflows/ci.yml` がリポジトリ直下に配置されている
- D-16 〜 D-23 のすべての決定が反映されている
- 単一 `check` ジョブで fmt-check → clippy → test を直列実行
- justfile を呼ばない（CI は cargo 直接）
- 人的検証で GitHub Actions が緑になることを確認（または first push で deferred）
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci-automation/03-06-SUMMARY.md` recording:
- 採用した actions のバージョン (checkout / setup-rust-toolchain / rust-cache)
- `defaults.run.working-directory: webif` の動作確認結果（first push 後の Actions UI）
- 異常系（fmt 違反など）で CI が赤くなる確認をしたか否か
- 全 Phase 3 plans (01..06) 完了後の `cargo build --release` / `cargo clippy --all-targets --release -- -D warnings` / `cargo test --release` の最終結果一覧
</output>
