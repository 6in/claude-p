---
id: 03-01
phase: 03-testing-ci-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webif/Cargo.toml
  - webif/src/mcp.rs
  - webif/src/worker.rs
  - webif/src/turn.rs
  - webif/src/http.rs
  - webif/src/main.rs
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03]
must_haves:
  truths:
    - "`webif/Cargo.toml` の `[dependencies]` に `async-trait = \"0.1\"` が含まれる"
    - "`webif/Cargo.toml` に `[dev-dependencies]` セクションが存在し、`tower = { version = \"0.5\", features = [\"util\"] }` と `tempfile = \"3\"` を含む"
    - "`webif/src/mcp.rs` に `#[async_trait] pub trait Mcp` が定義され、`McpClient` がこれを `impl Mcp for McpClient` で実装している"
    - "`webif/src/worker.rs` の `Worker` が `Worker<M: Mcp = McpClient>` でジェネリック化されている"
    - "`webif/src/http.rs` の `AppState` が `AppState<M: Mcp + Send + 'static = McpClient>` でジェネリック化され、`build_router` も型パラメータを受ける"
    - "`cargo build --release` が警告 0・エラー 0 で完了する"
    - "`cargo clippy --all-targets --release -- -D warnings` が警告 0・エラー 0 で完了する"
    - "`main.rs` は実質無変更（最低限、型推論で `Worker<McpClient>` が成立する）"
    - "`McpClient::from_streams` を呼んで作ったインスタンスの `next_id` 初期値は 0 である（03-03 の next_id テストの前提）"
  artifacts:
    - path: "webif/Cargo.toml"
      provides: "async-trait 本体依存 + dev-dependencies (tower, tempfile)"
      contains: "async-trait = \"0.1\""
    - path: "webif/src/mcp.rs"
      provides: "trait Mcp 抽象 + McpClient impl + #[cfg(test)] from_streams"
      contains: "pub trait Mcp"
    - path: "webif/src/worker.rs"
      provides: "Worker<M: Mcp = McpClient> ジェネリック構造体"
      contains: "Worker<M"
    - path: "webif/src/http.rs"
      provides: "AppState<M> + ジェネリック化された 4 ハンドラ + build_router<M>"
      contains: "AppState<M"
  key_links:
    - from: "webif/src/worker.rs"
      to: "webif/src/mcp.rs"
      via: "use crate::mcp::Mcp; M: Mcp バウンド"
      pattern: "use crate::mcp::Mcp"
    - from: "webif/src/http.rs"
      to: "webif/src/worker.rs"
      via: "Worker<M> を AppState<M> で保持"
      pattern: "Worker<M"
    - from: "webif/src/main.rs"
      to: "webif/src/worker.rs"
      via: "Worker::new で Worker<McpClient> をデフォルト型推論で取得"
      pattern: "Worker::new"
---

<objective>
Phase 3 のテスト基盤を載せる前提として、`webif/src/mcp.rs` に `trait Mcp` を抽出し、`Worker` / `AppState` / `build_router` をジェネリック化する（D-01 〜 D-05）。また `webif/Cargo.toml` に `async-trait` 本体依存と `[dev-dependencies]`（`tower`、`tempfile`）を追加する。`McpClient` 自身の挙動は変更せず、`#[cfg(test)] pub fn from_streams` テスト用コンストラクタの追加に留める。

Purpose: 後続の Wave 2/3 のテスト（TEST-01 / TEST-02 / TEST-03）はすべて trait Mcp と FakeMcp、tempfile、tower::oneshot を前提とするため、これらの土台がないと書けない。
Output: trait 抽出済み・ジェネリック化済みの `webif/src/{mcp,worker,turn,http,main}.rs` と、拡張済み `webif/Cargo.toml`。`cargo build --release` と `cargo clippy --all-targets --release -- -D warnings` がともに warning 0 で通る。

**Phase 3 では E2E テストを追加しない方針 (D-24)** のため、本プランで stdio I/O 経路を変更しても **実 ht-mcp + claude TUI による回帰確認は行わない**。代替として:
  (a) `cargo build --release` warning 0、
  (b) 03-03 の MCP JSON-RPC 単体テスト（FakeMcp + tokio::io::duplex で `from_streams` 経由で `request()` を駆動）が green、
  の 2 点で本番経路を間接的に検証する。実環境での回帰は merge 後の手動 curl smoke で確認する。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-testing-ci-automation/03-CONTEXT.md
@.planning/phases/03-testing-ci-automation/03-PATTERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

@webif/Cargo.toml
@webif/src/lib.rs
@webif/src/mcp.rs
@webif/src/worker.rs
@webif/src/turn.rs
@webif/src/http.rs
@webif/src/main.rs

<interfaces>
<!-- ジェネリック化前の主要シグネチャ。executor はこれらを起点に M: Mcp を伝搬させる。 -->

From webif/src/mcp.rs (現状の McpClient 主要メソッド — これらが trait Mcp の API):
```rust
pub async fn handshake(&mut self) -> Result<()>
pub async fn create_claude_session(&mut self) -> Result<String>
pub async fn close_session(&mut self, session_id: &str) -> Result<()>
pub async fn send_keys(&mut self, session_id: &str, keys: &[String]) -> Result<()>
pub async fn submit_line(&mut self, session_id: &str, text: &str) -> Result<()>
pub async fn snapshot(&mut self, session_id: &str) -> Result<String>
```

注: `request` / `call_tool` / `notify` / `write_message` / `read_response` は McpClient 内部詳細として trait 外に残す（D-02）。

From webif/src/worker.rs (ジェネリック化対象):
```rust
pub struct Worker { client: McpClient, pub session_id: String, ht_mcp_path: String }
```
→ `pub struct Worker<M: Mcp = McpClient> { client: M, pub session_id: String, ht_mcp_path: String }` に置き換える。

`Worker::boot` / `Worker::new` は `McpClient::spawn` を呼ぶため `impl Worker<McpClient>` 専用ブロックに隔離する。それ以外（`submit_line` / `snapshot` / `ensure_healthy` / `recreate` / `restart`）は `impl<M: Mcp + Send> Worker<M>` の汎用ブロックに入れる。

From webif/src/http.rs (ジェネリック化対象):
```rust
pub struct AppState { pub worker: Arc<Mutex<Worker>>, pub turns_dir: PathBuf, pub job_tx: mpsc::Sender<Job> }
pub fn build_router(state: Arc<AppState>) -> Router
```
→ `AppState<M: Mcp + Send + 'static = McpClient>` と `build_router<M: Mcp + Send + 'static>(state: Arc<AppState<M>>) -> Router` に伝搬。

From webif/src/turn.rs (シグネチャ伝搬):
```rust
pub(crate) async fn process_job(worker: &mut Worker, ...) -> Result<()>
pub async fn worker_loop(worker: Arc<Mutex<Worker>>, ...)
```
→ `process_job<M: Mcp + Send>(worker: &mut Worker<M>, ...)`、`worker_loop<M: Mcp + Send + 'static>(worker: Arc<Mutex<Worker<M>>>, ...)`。

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cargo.toml 拡張（async-trait 本体依存 + dev-dependencies）</name>
  <files>webif/Cargo.toml</files>
  <read_first>
    - webif/Cargo.toml（既存 [dependencies] ブロック lines 13-20 / [lib] [[bin]] lines 22-28）
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`webif/Cargo.toml`（依存追加の指示）
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md §"Implementation Decisions" D-04 / D-08 / D-25
  </read_first>
  <action>
    `webif/Cargo.toml` の `[dependencies]` ブロック末尾（既存 `dotenvy = "0.15"` の後）に `async-trait = "0.1"` を追加する（D-04 — trait Mcp の `#[async_trait]` は本体側で必要）。

    続いて `[dependencies]` の直後・`[lib]` の前に `[dev-dependencies]` セクションを新規追加し、`tower = { version = "0.5", features = ["util"] }`（D-08 — `oneshot` のため `util` feature 必須）と `tempfile = "3"`（D-25 — version pin は planner 裁量で "3" を採用）を追加する。

    `[lib]` / `[[bin]]` の二重定義は触らない。`webif/Cargo.lock` は cargo が自動更新する（コミット必要）。
  </action>
  <verify>
    <automated>cd webif && cargo build --release 2>&1 | tee /tmp/build-03-01.log && grep -q '^\[dev-dependencies\]' Cargo.toml && grep -q 'async-trait' Cargo.toml && grep -q 'tower' Cargo.toml && grep -q 'tempfile' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - `webif/Cargo.toml` の `[dependencies]` セクションに `async-trait = "0.1"` が含まれる
    - `webif/Cargo.toml` に `[dev-dependencies]` セクションが存在する
    - `[dev-dependencies]` に `tower = { version = "0.5", features = ["util"] }` が含まれる
    - `[dev-dependencies]` に `tempfile = "3"` が含まれる
    - `cd webif && cargo build --release` が exit 0
    - `webif/Cargo.lock` が更新済み（async-trait / tower / tempfile のエントリが追加されている）
  </acceptance_criteria>
  <done>
    `webif/Cargo.toml` に async-trait（本体）と dev-dependencies (tower, tempfile) が揃い、`cargo build --release` が通る。
  </done>
</task>

<task type="auto">
  <name>Task 2: mcp.rs に trait Mcp 抽出 + impl Mcp for McpClient + #[cfg(test)] from_streams</name>
  <files>webif/src/mcp.rs</files>
  <read_first>
    - webif/src/mcp.rs（現行 McpClient 構造体 lines 12-18 / spawn lines 20-33 / handshake〜snapshot lines 91-163）
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`webif/src/mcp.rs`（trait 抽出パターン）
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md §"Implementation Decisions" D-01 / D-02 / D-04 / D-05 / D-25
    - .planning/codebase/TESTING.md lines 98-115（trait 抽出推奨）
    - CLAUDE.md（snake_case / 日本語コメント / anyhow 慣行）
  </read_first>
  <action>
    `webif/src/mcp.rs` を編集する。本タスクの最終形は以下のとおり：

    **(a) imports 追加**

    既存 imports（lines 1-10）の末尾に `use async_trait::async_trait;` を追加する。

    **(b) trait Mcp の定義**

    既存 `McpClient` struct（lines 12-18）の **直前** に、`#[async_trait] pub trait Mcp: Send` を新規追加し、D-02 の API 粒度に合わせた 6 個の async メソッドシグネチャを宣言する：

    - `async fn handshake(&mut self) -> Result<()>`
    - `async fn create_claude_session(&mut self) -> Result<String>`
    - `async fn close_session(&mut self, session_id: &str) -> Result<()>`
    - `async fn send_keys(&mut self, session_id: &str, keys: &[String]) -> Result<()>`
    - `async fn submit_line(&mut self, session_id: &str, text: &str) -> Result<()>`
    - `async fn snapshot(&mut self, session_id: &str) -> Result<String>`

    シグネチャは既存 `McpClient` のメソッドと完全一致させる（`&mut self`、引数、戻り値の `Result<...>` まで）。`call_tool` / `request` / `notify` / `write_message` / `read_response` は trait 外（McpClient 内部詳細）。

    **(c) impl Mcp for McpClient ブロックの新設**

    既存 `impl McpClient` ブロックから、上記 6 メソッドを **`#[async_trait] impl Mcp for McpClient` ブロックに移送** する。残り（`spawn` / `write_message` / `read_response` / `request` / `notify` / `call_tool`）は `impl McpClient` に残す。`impl Mcp for McpClient` のメソッド本体は元のロジックをそのまま流用（`self.call_tool` / `self.request` 呼び出しはそのまま動く — 同一 struct の inherent method として残るため）。

    **(d) McpClient 構造体フィールドの型変更（最小変更で `from_streams` を実現するため）**

    現状の `McpClient` (lines 12-18):
    ```
    pub struct McpClient {
        #[allow(dead_code)]
        child: Child,
        stdin: ChildStdin,
        lines: Lines<BufReader<ChildStdout>>,
        next_id: i64,
    }
    ```

    最終形（本タスクで採用する設計）:
    ```
    pub struct McpClient {
        child: Option<Child>,                                              // 本番=Some, テスト=None
        stdin: Box<dyn tokio::io::AsyncWrite + Send + Unpin>,              // 本番=ChildStdin Box 化, テスト=duplex 片側
        lines: Lines<BufReader<Box<dyn tokio::io::AsyncRead + Send + Unpin>>>,
        next_id: i64,
    }
    ```

    - `pub` 可視性は維持（本番 API は破壊しない、struct を pub で出しているのは現状と同じ）
    - `child: Option<Child>` 化により、本番では `Some(child)` が入り `kill_on_drop(true)` の挙動はそのまま生きる（Drop 時に `Some` の場合のみ child が kill される — 既存挙動と同等）
    - `stdin` / `lines` を trait オブジェクトにすることで、本番（ChildStdin/ChildStdout）とテスト（tokio::io::duplex の片側）を同じ struct で扱える
    - `#[allow(dead_code)]` は `child` から外す（`Drop` 時に Option を参照されるため not dead）

    **(e) spawn の修正**

    既存 `spawn` (lines 20-33) を、新しいフィールド型に合わせて修正する：
    ```
    let stdin = child.stdin.take().ok_or_else(|| anyhow!("stdin が取れない"))?;
    let stdout = child.stdout.take().ok_or_else(|| anyhow!("stdout が取れない"))?;
    Ok(Self {
        child: Some(child),
        stdin: Box::new(stdin),
        lines: BufReader::new(Box::new(stdout) as Box<dyn tokio::io::AsyncRead + Send + Unpin>).lines(),
        next_id: 0,
    })
    ```

    **(f) from_streams テスト用コンストラクタの追加**

    既存 `spawn` の **直後** に以下を追加する：
    ```
    /// テスト時のみ使う直接コンストラクタ。
    /// tokio::io::duplex の片側ペアを渡して in-memory な MCP クライアントを作る。
    /// child は None（本番の kill_on_drop は不要）、next_id は 0 で初期化（03-03 の next_id テストが Some(1) を期待するため）。
    #[cfg(test)]
    pub fn from_streams<W, R>(stdin: W, stdout: R) -> Self
    where
        W: tokio::io::AsyncWrite + Send + Unpin + 'static,
        R: tokio::io::AsyncRead + Send + Unpin + 'static,
    {
        Self {
            child: None,
            stdin: Box::new(stdin),
            lines: BufReader::new(Box::new(stdout) as Box<dyn tokio::io::AsyncRead + Send + Unpin>).lines(),
            next_id: 0,
        }
    }
    ```

    **(g) 内部メソッド (write_message / read_response / request / notify / call_tool) は本体変更不要**

    `stdin: Box<dyn AsyncWrite + Send + Unpin>` は `AsyncWriteExt::write_all` / `flush` を介して既存の `self.stdin.write_all(...).await?` / `self.stdin.flush().await?` がそのまま動く（trait object 越しの dispatch、`Pin` 周りは Box が `Unpin` なので問題ない）。`self.lines.next_line().await?` も同様にそのまま動く。

    **(h) コメント慣行**

    新規追加部分には `//` または `///` で日本語の意図説明を残す（CLAUDE.md / CONVENTIONS.md）。例: `// 本番は Some(child)、テスト時は None`、`// trait Mcp は ht-mcp ツールラッパ層の API 粒度（D-02）`。
  </action>
  <verify>
    <automated>cd webif && cargo build --release 2>&1 | tee /tmp/build-03-01-mcp.log && grep -E 'Compiling|Finished' /tmp/build-03-01-mcp.log && grep -c '^pub trait Mcp' src/mcp.rs && grep -c '#\[async_trait\]' src/mcp.rs && grep -c 'fn from_streams' src/mcp.rs</automated>
  </verify>
  <acceptance_criteria>
    - `webif/src/mcp.rs` に `#[async_trait] pub trait Mcp` の宣言が含まれる
    - `webif/src/mcp.rs` に `#[async_trait]\nimpl Mcp for McpClient` ブロックが含まれる
    - `trait Mcp` のメソッドは `handshake` / `create_claude_session` / `close_session` / `send_keys` / `submit_line` / `snapshot` の 6 個に揃う（D-02 の粒度）
    - `webif/src/mcp.rs` に `#[cfg(test)] pub fn from_streams<W, R>(...)` が含まれ、シグネチャは `W: AsyncWrite + Send + Unpin + 'static` / `R: AsyncRead + Send + Unpin + 'static` を要求する
    - `from_streams` が初期化する `McpClient` は `next_id: 0` を持つ（コードから読み取り可能）
    - `McpClient` のフィールドは `child: Option<Child>` / `stdin: Box<dyn AsyncWrite + Send + Unpin>` / `lines: Lines<BufReader<Box<dyn AsyncRead + Send + Unpin>>>` / `next_id: i64` の 4 つになっている
    - `webif/src/mcp.rs` の `use async_trait::async_trait;` が import 済み
    - 既存の inherent 6 メソッド（handshake / create_claude_session / close_session / send_keys / submit_line / snapshot）の本体は `impl Mcp for McpClient` ブロックへ移送されており、それらが従来通り `cargo build --release` でコンパイルする（trait オブジェクト経由の stdin/lines アクセスに対しても `write_all` / `flush` / `next_line` がそのまま動く）
    - `cd webif && cargo build --release` が exit 0
  </acceptance_criteria>
  <done>
    trait Mcp が抽出され、McpClient はこれを実装する。テストから duplex 経由で MCP クライアントを作るための `from_streams` が `#[cfg(test)]` で公開され、その `next_id` 初期値は 0 である。
  </done>
</task>

<task type="auto">
  <name>Task 3: Worker / AppState / build_router / process_job / worker_loop / main.rs のジェネリック化伝搬</name>
  <files>webif/src/worker.rs, webif/src/turn.rs, webif/src/http.rs, webif/src/main.rs</files>
  <read_first>
    - webif/src/worker.rs（現行 Worker 構造体 lines 9-13 / impl Worker lines 15-92）
    - webif/src/turn.rs（現行 process_job lines 39 / worker_loop lines 77-94）
    - webif/src/http.rs（現行 AppState lines 21-25 / 4 ハンドラ / build_router lines 155-162）
    - webif/src/main.rs（Worker::new 呼び出し lines 19 / AppState 組み立て lines 33）
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`webif/src/worker.rs` / §`webif/src/turn.rs` / §`webif/src/http.rs` / §`webif/src/main.rs`
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md §"Implementation Decisions" D-03
  </read_first>
  <action>
    D-03 に従い、`Worker` → `AppState` → ハンドラ → `build_router` → `process_job` → `worker_loop` まで型パラメータ `M: Mcp` を伝搬させる。`Worker<M: Mcp = McpClient>` のデフォルト型を必ず入れ、`main.rs` を型注釈なしで動かせる状態にする。

    (a) `webif/src/worker.rs`:
      - `use crate::mcp::McpClient;` の隣に `use crate::mcp::Mcp;` を追加
      - `pub struct Worker { client: McpClient, ... }` を `pub struct Worker<M: Mcp = McpClient> { client: M, pub session_id: String, ht_mcp_path: String }` に変更
      - `impl Worker` ブロックを 2 つに分ける:
        1. `impl Worker<McpClient>` — `new` / `boot` / `spawn_session` / `restart` の 4 メソッド（`McpClient::spawn` を呼ぶため具象に依存）
        2. `impl<M: Mcp + Send> Worker<M>` — `submit_line` / `snapshot` / `ensure_healthy` / `recreate` の汎用 4 メソッド
      - `recreate` は `self.client.close_session` を呼ぶが、`Mcp` trait に `close_session` があるためコンパイル可能。

    (b) `webif/src/turn.rs`:
      - 既存 `use crate::worker::Worker;` の隣に `use crate::mcp::Mcp;` を追加
      - `process_job` のシグネチャを `pub(crate) async fn process_job<M: Mcp + Send>(worker: &mut Worker<M>, turns_dir: &Path, job: &Job) -> Result<()>` に変更
      - `worker_loop` のシグネチャを `pub async fn worker_loop<M: Mcp + Send + 'static>(worker: Arc<Mutex<Worker<M>>>, turns_dir: PathBuf, mut job_rx: mpsc::Receiver<Job>)` に変更
      - 本体ロジックは無変更

    (c) `webif/src/http.rs`:
      - 既存 `use crate::worker::Worker;` に追加して `use crate::mcp::{Mcp, McpClient};` を追加
      - `pub struct AppState` を `pub struct AppState<M: Mcp + Send + 'static = McpClient> { pub worker: Arc<Mutex<Worker<M>>>, pub turns_dir: PathBuf, pub job_tx: mpsc::Sender<Job> }` に変更
      - 4 ハンドラ全てに `<M: Mcp + Send + 'static>` 型パラメータを追加し、`State(state): State<Arc<AppState<M>>>` に変更
      - `pub fn build_router(state: Arc<AppState>) -> Router` を `pub fn build_router<M: Mcp + Send + 'static>(state: Arc<AppState<M>>) -> Router` に変更し、ルート登録部分は `prompt_handler::<M>` のようにターボフィッシュ注釈を付ける（または axum の型推論に任せる — まずは推論で試み、必要なら明示）
      - ハンドラ本体ロジックは無変更

    (d) `webif/src/main.rs`:
      - D-03 でデフォルト型を入れたため、理想的には **無変更** で `Worker::new(ht_mcp_path)` が `Worker<McpClient>` に推論されるはず。
      - もし型推論が効かず明示が必要なら、`use ht_webif::mcp::McpClient;` を import に追加し、line 19 を `let worker: Worker<McpClient> = Worker::new(ht_mcp_path).await?;` に書き換える。`AppState { worker, ... }` も同様にデフォルト型で `AppState<McpClient>` に推論されるはず。
      - 変更最小化を優先し、コンパイルが通るなら main.rs は触らない。

    (e) `cargo build --release` と `cargo clippy --all-targets --release -- -D warnings` の両方で warning 0 を達成すること。新規 `unused_imports` 等が出たら整理する。

    日本語コメント・スネークケース慣行は維持。
  </action>
  <verify>
    <automated>cd webif && cargo build --release 2>&1 | tee /tmp/build-03-01-generic.log && cargo clippy --all-targets --release -- -D warnings 2>&1 | tee /tmp/clippy-03-01.log && grep -c 'Worker<M' src/worker.rs && grep -c 'AppState<M' src/http.rs && grep -c 'build_router<M' src/http.rs</automated>
  </verify>
  <acceptance_criteria>
    - `webif/src/worker.rs` に `pub struct Worker<M: Mcp = McpClient>` が含まれる
    - `webif/src/worker.rs` に `impl<M: Mcp + Send> Worker<M>` 汎用 impl ブロックが含まれる（最低限 `submit_line` / `snapshot` / `ensure_healthy` / `recreate` を含む）
    - `webif/src/worker.rs` に `impl Worker<McpClient>` 具象 impl ブロックが含まれる（`new` / `boot` / `spawn_session` / `restart` を含む）
    - `webif/src/turn.rs` の `process_job` / `worker_loop` が `<M: Mcp + Send (+ 'static)>` 型パラメータ付き
    - `webif/src/http.rs` に `pub struct AppState<M: Mcp + Send + 'static = McpClient>` が含まれる
    - `webif/src/http.rs` の `build_router` が `<M: Mcp + Send + 'static>` 型パラメータ付き
    - `webif/src/main.rs` は `cargo build --release` がそのまま通る（型推論で `Worker<McpClient>` / `AppState<McpClient>` に解決される）
    - `cd webif && cargo build --release` が exit 0、warning 0
    - `cd webif && cargo clippy --all-targets --release -- -D warnings` が exit 0
  </acceptance_criteria>
  <done>
    型パラメータ M が `Worker` → `AppState` → ハンドラ → `build_router` → `process_job` → `worker_loop` に伝搬し、本番側は `Worker<McpClient>` をデフォルトで使う。リリースビルドと clippy が warning 0 で通る。
  </done>
</task>

</tasks>

<verification>
このプランは Phase 3 のテスト基盤の前提（trait Mcp / FakeMcp / from_streams / dev-dependencies）を確立するため、Wave 2/3 のテストが書ける状態にすることが完了条件。

検証:
1. `cd webif && cargo build --release` が exit 0 / warning 0
2. `cd webif && cargo clippy --all-targets --release -- -D warnings` が exit 0
3. `grep -c 'pub trait Mcp' webif/src/mcp.rs` >= 1
4. `grep -c 'Worker<M' webif/src/worker.rs` >= 1
5. `grep -c 'AppState<M' webif/src/http.rs` >= 1
6. `grep -c 'fn from_streams' webif/src/mcp.rs` >= 1
7. `webif/Cargo.toml` に `[dev-dependencies]` セクションが存在し、`tower` と `tempfile` が含まれる
8. `webif/Cargo.toml` の `[dependencies]` に `async-trait` が含まれる
</verification>

<success_criteria>
- trait Mcp が抽出され、McpClient はこれを実装する
- Worker / AppState / build_router / process_job / worker_loop がすべて `<M: Mcp>` でジェネリック化されている
- `McpClient::from_streams` が `#[cfg(test)] pub` で公開されており、`next_id: 0` で初期化する
- `webif/Cargo.toml` に `async-trait`（本体）と `[dev-dependencies]`（tower / tempfile）が追加されている
- `cargo build --release` と `cargo clippy --all-targets --release -- -D warnings` がともに warning 0 で通る
- `main.rs` は型推論でそのまま動作する（変更最小化）
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci-automation/03-01-SUMMARY.md` recording:
- trait Mcp の最終メソッド集合
- McpClient 内部の `child: Option<Child>` 化と stdin/lines の `Box<dyn ...>` 化など本番側に影響が出た変更点（影響範囲評価）
- `from_streams` の最終シグネチャ（`<W, R>` ジェネリック）と `next_id: 0` 初期化の確認
- main.rs に明示型注釈が必要だったか否か
- `cargo build --release` / `cargo clippy --all-targets --release -- -D warnings` の最終結果
</output>
</content>
</invoke>