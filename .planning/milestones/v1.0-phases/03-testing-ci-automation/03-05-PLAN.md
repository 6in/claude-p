---
id: 03-05
phase: 03-testing-ci-automation
plan: 05
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - justfile
autonomous: true
requirements: [TOOL-01]
must_haves:
  truths:
    - "リポジトリ直下 `ht-mcp-sample/justfile` が存在する"
    - "`just --list` で `build` / `run` / `test` / `fmt` / `clippy` / `clean` の 6 レシピが表示される"
    - "`just build` がリポジトリ直下から実行され、`webif/` で `cargo build --release` を呼ぶ"
    - "`just test` が `webif/` で `cargo test --release` を呼び、Wave 2/3 で追加したテスト全 green"
    - "`just clippy` が `webif/` で `cargo clippy --all-targets --release -- -D warnings` を呼び、warning 0"
  artifacts:
    - path: "justfile"
      provides: "6 レシピ: build / run / test / fmt / clippy / clean（リポジトリ直下配置）"
      contains: "cargo build --release"
  key_links:
    - from: "justfile"
      to: "webif/Cargo.toml"
      via: "set working-directory := 'webif' or each recipe runs in webif/"
      pattern: "working-directory|cd webif"
---

<objective>
TOOL-01「`justfile` または `Makefile` で開発コマンドを集約」を実装する。D-12 の決定通り `justfile`（Makefile ではなく）をリポジトリ直下 `ht-mcp-sample/justfile` に配置し、D-13 規定の 6 レシピ（`build` / `run` / `test` / `fmt` / `clippy` / `clean`）のみを提供する。リポジトリ直下から `just <recipe>` を実行すれば `webif/` で対応 cargo コマンドが走る形にする（D-14）。CI は justfile を呼ばない（D-23）。

Purpose: 開発者ローカルでのショートカットを一元化し、ROADMAP cross-cutting constraint（release profile での warning 0）と CI の cargo コマンドを揃える。
Output: リポジトリ直下 `justfile`。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-testing-ci-automation/03-CONTEXT.md
@.planning/phases/03-testing-ci-automation/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md

@webif/Cargo.toml

<interfaces>
<!-- 配置・実行ディレクトリ -->

リポジトリ root: `/home/parallels/workspaces/ht-mcp-sample/`
cargo project root: `/home/parallels/workspaces/ht-mcp-sample/webif/`
justfile 配置先: `/home/parallels/workspaces/ht-mcp-sample/justfile`

開発者の運用: リポジトリ直下から `just build` / `just test` / `just clippy` などを実行。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: justfile をリポジトリ直下に新規作成（6 レシピ）</name>
  <files>justfile</files>
  <read_first>
    - .planning/phases/03-testing-ci-automation/03-PATTERNS.md §`justfile`（リポジトリ直下、新規）lines 434-478（完全なスケルトン提示済み）
    - .planning/phases/03-testing-ci-automation/03-CONTEXT.md §"タスクランナー" D-12 / D-13 / D-14 / D-15 / D-23
    - .planning/codebase/CONVENTIONS.md（日本語コメント可）
    - CLAUDE.md（snake_case / 日本語コメント慣行）
  </read_first>
  <action>
    リポジトリ直下 `/home/parallels/workspaces/ht-mcp-sample/justfile` を新規作成する。Write tool で書く（heredoc 禁止）。

    必須要素:
    - 先頭に `set working-directory := 'webif'` を入れる（D-14 の選択肢 (a) を採用 — DRY で書き味が良い）
    - 6 レシピのみ（D-13 / D-15 — `default` / `check` / `cov` / `watch` 等は含めない）:
      1. `build` → `cargo build --release`
      2. `run` → `cargo run`
      3. `test` → `cargo test --release`（CONTEXT specifics line 160 — CI と profile を揃える）
      4. `fmt` → `cargo fmt --all`
      5. `clippy` → `cargo clippy --all-targets --release -- -D warnings`（ROADMAP cross-cutting constraint 準拠）
      6. `clean` → `cargo clean`

    各レシピの直前に日本語コメントを 1 行付ける（例: `# リリースビルド`、`# 開発実行（dev profile、.env を webif/ または親で読む）`、`# 全テスト（release profile、CI と揃える）` 等。PATTERNS.md lines 442-471 のスケルトンに従う）

    ファイル冒頭に短いヘッダコメント:
    ```
    # justfile — ht-webif 開発ショートカット
    # 全レシピは webif/ で cargo を呼ぶ。リポジトリ直下から `just <recipe>` で起動する。
    # CI からは justfile を呼ばない（cargo 直接呼び出し、D-23）。
    ```

    `default` / `check` / `cov` / `watch` レシピは追加しない（D-15）。CI 連携は別 Plan（03-06）で扱う。
  </action>
  <verify>
    <automated>test -f /home/parallels/workspaces/ht-mcp-sample/justfile && cd /home/parallels/workspaces/ht-mcp-sample && grep -c "^build:\|^run:\|^test:\|^fmt:\|^clippy:\|^clean:" justfile && grep -q "set working-directory := 'webif'" justfile && grep -q "cargo build --release" justfile && grep -q "cargo clippy --all-targets --release -- -D warnings" justfile && grep -q "cargo test --release" justfile</automated>
  </verify>
  <acceptance_criteria>
    - `/home/parallels/workspaces/ht-mcp-sample/justfile` が存在する
    - `justfile` の先頭に `set working-directory := 'webif'` が含まれる
    - レシピ `build:` が含まれ、コマンドは `cargo build --release`
    - レシピ `run:` が含まれ、コマンドは `cargo run`
    - レシピ `test:` が含まれ、コマンドは `cargo test --release`
    - レシピ `fmt:` が含まれ、コマンドは `cargo fmt --all`
    - レシピ `clippy:` が含まれ、コマンドは `cargo clippy --all-targets --release -- -D warnings`
    - レシピ `clean:` が含まれ、コマンドは `cargo clean`
    - `default` / `check` / `cov` / `watch` 等の余剰レシピが含まれない
    - 各レシピに日本語コメントが付いている
  </acceptance_criteria>
  <done>
    リポジトリ直下に 6 レシピのみの justfile が配置され、`set working-directory := 'webif'` で webif/ に降りて cargo を実行する形になっている。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: just <recipe> の動作確認（人的検証）</name>
  <files>justfile</files>
  <what-built>
    リポジトリ直下に `justfile`（6 レシピ: build / run / test / fmt / clippy / clean）が配置された。各レシピは `webif/` ディレクトリで対応 cargo コマンドを実行する。
  </what-built>
  <how-to-verify>
    `just` バイナリのインストール状況に依存するため、ユーザーに以下を実行して確認してもらう:

    1. `just` がインストールされているか確認: `which just`
       - インストールされていなければ `cargo install just` または `brew install just` 等で入れる
    2. リポジトリ直下から `just --list` を実行 → `build` / `run` / `test` / `fmt` / `clippy` / `clean` の 6 レシピが表示されるか
    3. `just build` を実行 → `cd webif && cargo build --release` 相当の動作で release ビルドが成功するか
    4. `just clippy` を実行 → `cd webif && cargo clippy --all-targets --release -- -D warnings` 相当で warning 0 で完了するか
    5. `just test` を実行 → Phase 3 で追加した全テストが green で完了するか

    期待される結果:
    - `just --list` の出力に 6 レシピ全てが見える（順番不問）
    - `just build` / `just clippy` / `just test` がいずれも exit 0
    - `just clean` が `webif/target` を削除する

    注: `just` 未インストール環境では Task 1 の自動検証で justfile の中身は確認済み。実環境テストはユーザー裁量。
  </how-to-verify>
  <resume-signal>
    Type "approved" if `just --list` shows 6 recipes and `just build` / `just test` / `just clippy` all succeed.
    Type "skip" if `just` is not installed locally (justfile content was verified in Task 1).
    Type the specific issue if any recipe fails.
  </resume-signal>
</task>

</tasks>

<verification>
TOOL-01 のカバレッジ:
1. リポジトリ直下に `justfile` が配置されている
2. 6 レシピ（build / run / test / fmt / clippy / clean）のみ
3. 各レシピが `webif/` で対応 cargo コマンドを呼ぶ
4. CI からは呼ばない（独立した存在）

検証コマンド:
1. `test -f justfile`
2. `grep -c "^build:\|^run:\|^test:\|^fmt:\|^clippy:\|^clean:" justfile` = 6
3. `grep "set working-directory := 'webif'" justfile`
4. `just --list`（環境依存）
</verification>

<success_criteria>
- リポジトリ直下に `justfile` が配置されている
- 6 レシピのみ（余剰なし）
- 各レシピが `webif/` で対応 cargo コマンドを呼ぶ
- `default` / `check` などの余剰レシピを含まない（D-15）
- 人的検証で `just --list` / `just build` / `just test` / `just clippy` が動作する（環境にインストールされていれば）
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-ci-automation/03-05-SUMMARY.md` recording:
- 採用した working-directory 方式（`set working-directory := 'webif'` 採用、または各レシピで `cd webif &&` 採用）
- ユーザー環境での `just --list` 動作確認結果（または `just` 未インストールで skip）
- 6 レシピ名一覧
</output>
