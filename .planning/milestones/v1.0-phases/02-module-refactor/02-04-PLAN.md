---
phase: 02-module-refactor
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - webif/src/http.rs
  - webif/src/lib.rs
  - webif/src/main.rs
autonomous: true
requirements:
  - CODE-01
  - CODE-02

must_haves:
  truths:
    - "`webif/src/http.rs` に `pub struct AppState`、`pub(crate) fn ise`、`pub struct PromptReq`、`pub struct CommandReq`、`pub struct CommandResp`、`pub struct RestartResp`、4 つのハンドラ（`prompt_handler`、`turn_handler`、`command_handler`、`restart_handler`）、`pub fn build_router(state) -> axum::Router` が存在する"
    - "`turn_handler` 内に **verbatim** で `if turn_id.is_empty() || !turn_id.chars().all(|c| c.is_ascii_digit() || c == '-')` のパストラバーサル防止チェックが残っている（ASVS V5 のみの security control）"
    - "`webif/src/lib.rs` の冒頭に `//!` doc コメント（main.rs lines 1-17 の architecture summary）が移送されている"
    - "`webif/src/main.rs` は wiring のみで ~30 行（`dotenvy::dotenv` / `Worker::new` / turns_dir / `Arc::new(Mutex::new)` / `tokio::spawn(worker_loop)` / `Arc::new(AppState{...})` / `build_router(state)` / `axum::serve`）"
    - "`main.rs` 内に `Router::new().route(...)` の直接ルーティング構築は **存在しない**（`build_router` 経由）"
    - "`cargo build --release` と `cargo clippy --all-targets --release -- -D warnings` が警告0・エラー0で完了する"
    - "起動した WebIF に対する 8 つの curl smoke test（RESEARCH.md §"Verification Strategy"）が全て期待結果を返す: POST /prompt (async/wait=true/fresh=true)、GET /turns/{id}（running 状態・存在しない・パストラバーサル拒否=HTTP 400）、POST /command、POST /restart"
  artifacts:
    - path: "webif/src/http.rs"
      provides: "AppState + ise + 4 DTOs + 4 ハンドラ + build_router"
      contains: "pub struct AppState"
    - path: "webif/src/lib.rs"
      provides: "doc コメント + 5 pub mod 宣言"
      contains: "//! ht-webif"
    - path: "webif/src/main.rs"
      provides: "wiring のみの薄い起点（~30 行）"
      contains: "axum::serve"
  key_links:
    - from: "webif/src/main.rs"
      to: "webif/src/http.rs"
      via: "use ht_webif::http::{AppState, build_router};"
      pattern: "use ht_webif::http::"
    - from: "webif/src/http.rs"
      to: "webif/src/turn.rs"
      via: "use crate::turn::{build_prompt_body, read_turn, Job};"
      pattern: "use crate::turn::"
    - from: "webif/src/http.rs"
      to: "webif/src/worker.rs"
      via: "use crate::worker::Worker;"
      pattern: "use crate::worker::Worker"
    - from: "webif/src/http.rs"
      to: "turn_handler のパストラバーサル防止"
      via: "ASCII allowlist on turn_id"
      pattern: "turn_id\\.chars\\(\\)\\.all"
    - from: "webif/src/http.rs"
      to: "build_router で4ルート登録"
      via: ".route() x 4"
      pattern: 'route\("/'
---

<objective>
最終層を切り出す: HTTP 層（`AppState` / `ise` / 4 DTO / 4 ハンドラ）を `main.rs` から `http.rs` へ移送し、追加で `pub fn build_router(state) -> axum::Router` を新規導入する。`main.rs` の architecture doc コメント（lines 1-17）を `lib.rs` に移送する。最後に `main.rs` を ~30 行の wiring 専用シムに縮小する。そして実行時の 8 curl smoke test で挙動完全保存を実証する。

Purpose: モジュール分割の最終仕上げ。`main.rs` は dotenv 読み込み・Worker 起動・turns_dir 作成・worker_loop spawn・router 構築・axum serve のみになる。HTTP 層は `http.rs` に完結し、Phase 3（テスト & CI）が `cargo test --lib` で純粋関数（`build_prompt_body`、`turn_id` validation など）をユニットテストできる土台が整う。

Output: `http.rs`（HTTP 全部 + `build_router`）、薄い `main.rs`（~30 行）、doc コメントが移送された `lib.rs`、そして 8 curl smoke test pass による behavior preservation の実証。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-module-refactor/02-RESEARCH.md
@.planning/phases/02-module-refactor/02-PATTERNS.md
@.planning/phases/02-module-refactor/02-03-PLAN.md
@webif/src/main.rs
@webif/src/lib.rs
@webif/src/config.rs
@webif/src/mcp.rs
@webif/src/worker.rs
@webif/src/turn.rs
@webif/src/http.rs
@CLAUDE.md
@HT-PROTOCOL.md

<interfaces>
<!-- webif/src/http.rs の公開 API -->
<!-- - pub struct AppState { pub worker: Arc<Mutex<Worker>>, pub turns_dir: PathBuf, pub job_tx: mpsc::Sender<Job> } -->
<!-- - pub(crate) fn ise<E: std::fmt::Display>(e: E) -> (StatusCode, String) -->
<!-- - pub struct PromptReq { prompt: String, #[serde(default)] fresh: bool, #[serde(default)] wait: bool } -->
<!-- - pub struct CommandReq { text: String } -->
<!-- - pub struct CommandResp { sent: String, snapshot: String } -->
<!-- - pub struct RestartResp { status: String, session_id: String } -->
<!-- - async fn prompt_handler(...)   — private; build_router 経由でのみ呼ばれる -->
<!-- - async fn turn_handler(...)     — private -->
<!-- - async fn command_handler(...)  — private -->
<!-- - async fn restart_handler(...)  — private -->
<!-- - pub fn build_router(state: Arc<AppState>) -> axum::Router  — main.rs が呼ぶ -->

<!-- 重要 — Plan 03 完了後の main.rs の状態（Task 1 の前提）: -->
<!-- Plan 03 Task 3 で `read_turn` 関数は **すでに main.rs から削除済み** で turn.rs へ移送されている。 -->
<!-- したがって Plan 04 Task 1 で http.rs に移送する HTTP 層には **`read_turn` の関数定義は含まれない**。 -->
<!-- HTTP ハンドラ内の `read_turn(...)` 呼び出しは Plan 03 Task 3 で既に `ht_webif::turn::read_turn(...)` 経由に変わっている。 -->
<!-- Plan 04 Task 1 では、http.rs を `crate::turn::read_turn` を `use` する形に再配線するだけでよい。 -->

<!-- 最終 main.rs の構造（目標 ~30 行） -->
<!-- - 1行目: 何もなし（doc コメントは lib.rs に移送した） -->
<!-- - use 文 5-6 個 -->
<!-- - #[tokio::main] async fn main() -> Result<()> { -->
<!--     dotenvy::dotenv().ok(); -->
<!--     let ht_mcp_path = load_ht_mcp_path(); -->
<!--     println!("ht-mcp パス: {ht_mcp_path}"); -->
<!--     let worker = Worker::new(ht_mcp_path).await?; -->
<!--     println!("claude セッション: {}", worker.session_id); -->
<!--     let turns_dir = std::env::current_dir()?.join("turns"); -->
<!--     tokio::fs::create_dir_all(&turns_dir).await?; -->
<!--     println!("ターンディレクトリ: {}", turns_dir.display()); -->
<!--     let worker = Arc::new(Mutex::new(worker)); -->
<!--     let (job_tx, job_rx) = mpsc::channel::<Job>(64); -->
<!--     tokio::spawn(worker_loop(worker.clone(), turns_dir.clone(), job_rx)); -->
<!--     let state = Arc::new(AppState { worker, turns_dir, job_tx }); -->
<!--     let app = build_router(state); -->
<!--     let addr = "127.0.0.1:8080"; -->
<!--     let listener = tokio::net::TcpListener::bind(addr).await?; -->
<!--     println!("WebIF 起動: http://{addr}"); -->
<!--     println!("  POST /prompt       ..."); （4行の起動ガイド） -->
<!--     axum::serve(listener, app).await?; -->
<!--     Ok(()) -->
<!--   } -->

<!-- 必要な use 文（最終 main.rs） -->
<!-- use anyhow::Result; -->
<!-- use std::sync::Arc; -->
<!-- use tokio::sync::{mpsc, Mutex}; -->
<!-- use ht_webif::config::load_ht_mcp_path; -->
<!-- use ht_webif::http::{AppState, build_router}; -->
<!-- use ht_webif::turn::{worker_loop, Job}; -->
<!-- use ht_webif::worker::Worker; -->

<!-- lib.rs の最終形 -->
<!-- //! ht-webif — curl で叩く薄い WebIF。 -->
<!-- //! ...（main.rs lines 1-17 の architecture summary block 全体）... -->
<!-- pub mod config; -->
<!-- pub mod http; -->
<!-- pub mod mcp; -->
<!-- pub mod turn; -->
<!-- pub mod worker; -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: webif/src/http.rs を完成させる（HTTP 全部 + build_router 新規追加）</name>
  <files>webif/src/http.rs</files>
  <read_first>
    - webif/src/main.rs（現状: Plan 03 後の状態。HTTP 層が main.rs の lines ~70-200 周辺に残っている。`// ── HTTP 層 ─` 銀行〜`restart_handler` 終端まで全て移送対象。`Router::new().route(...).with_state(state)` のルーティング構築も移送対象。**注意:** `read_turn` 関数は Plan 03 Task 3 で既に main.rs から削除され turn.rs へ移送済み — main.rs 内には存在しない。HTTP ハンドラ内の `read_turn(...)` 呼び出しは `ht_webif::turn::read_turn(...)` 経由に既になっている）
    - webif/src/turn.rs（Plan 03 完成形、`pub(crate) async fn read_turn` を含む — http.rs はここから `use crate::turn::read_turn;` で取得する）
    - webif/src/worker.rs（Plan 03 完成形、`pub session_id` と `pub(crate)` メソッドを import するため）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"Pattern 3: build_router helper"、§"Risk 5: AxumPath name collision"、§"Risk 7: Path-traversal guard"、§"Security Domain"）
    - .planning/phases/02-module-refactor/02-PATTERNS.md（§"webif/src/http.rs"、§"Boundary-Fragile Excerpts" 2 番）
  </read_first>
  <action>
    **前提:** Plan 03 Task 3 で `read_turn` は **既に main.rs から削除済みで turn.rs へ移送済み**。Plan 04 Task 1 で http.rs に新しく `read_turn` 関数を作成する必要は **ない**。http.rs は `use crate::turn::read_turn;` で `turn.rs` から関数をインポートして使うだけでよい。同様に、`Job` / `build_prompt_body` も turn.rs に既存。

    `webif/src/http.rs`（Plan 01 で作ったプレースホルダ）を以下の中身に置き換える。`main.rs` の HTTP セクション全体（read_turn を除く — `read_turn` は既に turn.rs にある）を verbatim 移送し、`build_router` 関数を新規に追加する。

    最終形の構造（順番厳守）:
    1. file-level コメント `// ── HTTP 層 ──────────────────────────────────────────────────────────`（main.rs の銀行コメント）
    2. 空行
    3. import ブロック:
       - `use anyhow::Result;`（ハンドラ内部で `Result<...>` 戻り値を使う）
       - `use axum::{extract::{Path as AxumPath, State}, http::StatusCode, routing::{get, post}, Json, Router};`（**`Path as AxumPath` のエイリアス必須** — `std::path::Path` との衝突回避）
       - `use serde::{Deserialize, Serialize};`
       - `use serde_json::{json, Value};`
       - `use std::path::PathBuf;`
       - `use std::sync::Arc;`
       - `use std::time::Duration;`（prompt_handler の `Duration::from_secs(700)`、command_handler の `Duration::from_millis(1500)` で使用）
       - `use tokio::sync::{mpsc, Mutex};`
       - `use tokio::time::Instant;`（prompt_handler の wait=true 待機で使用）
       - `use crate::turn::{build_prompt_body, read_turn, Job};` **← `read_turn` を turn.rs から import する（Plan 03 で turn.rs に既に存在する）**
       - `use crate::worker::Worker;`
    4. 空行
    5. `pub struct AppState { ... }`: main.rs の `AppState` 構造体を移送、宣言を `pub struct AppState`、全3フィールドを `pub worker:` / `pub turns_dir:` / `pub job_tx:` に変更
    6. `pub(crate) fn ise<E: std::fmt::Display>(e: E) -> (StatusCode, String) { ... }`: main.rs の `ise` 関数を verbatim 移送、可視性のみ `pub(crate) fn` に変更
    7. `#[derive(Deserialize)] pub struct PromptReq { ... }`: 構造体宣言を `pub struct PromptReq`、フィールドはそのまま（`#[serde(default)]` 属性も verbatim）
    8. `async fn prompt_handler(...)`: main.rs の `prompt_handler` を verbatim 移送（可視性は **private** のまま — `build_router` の `post(prompt_handler)` でしか参照されない）。`chrono::Utc::now().format(...)` の呼び出し、`build_prompt_body` 呼び出し、`state.job_tx.send(Job { ... })` 構築、`wait:true` ループ、`read_turn` 呼び出しは全て verbatim
    9. `async fn turn_handler(...)`: main.rs の `turn_handler` を verbatim 移送。**最重要**: 関数本体内のパストラバーサル防止ガード `if turn_id.is_empty() || !turn_id.chars().all(|c| c.is_ascii_digit() || c == '-') { return Err((StatusCode::BAD_REQUEST, "不正な turn_id".to_string())); }` を **1文字も変えず** 保持。コメント `// パストラバーサル防止: turn_id は数字とハイフンのみ` も verbatim
    10. `#[derive(Deserialize)] pub struct CommandReq { text: String }` を `pub struct CommandReq`、`text` フィールドは pub 不要（serde が deserialize するだけ）またはモジュール内のみ参照なので private 維持で OK
    11. `#[derive(Serialize)] pub struct CommandResp { sent: String, snapshot: String }`: フィールドは Serialize が `pub` でなくても serde が serialize するので private 維持で OK
    12. `async fn command_handler(...)`: main.rs の `command_handler` を verbatim 移送。`worker.ensure_healthy().await.map_err(ise)?;`、`worker.submit_line(&req.text).await.map_err(ise)?;`、`tokio::time::sleep(Duration::from_millis(1500)).await;`、`snapshot` の `.unwrap_or_else(|e| format!("(snapshot 取得失敗: {e})"))` フォールバック verbatim
    13. `#[derive(Serialize)] pub struct RestartResp { status: String, session_id: String }`
    14. `async fn restart_handler(...)`: main.rs の `restart_handler` を verbatim 移送
    15. **新規追加** `pub fn build_router(state: Arc<AppState>) -> Router { ... }`:
        - 中身: `Router::new().route("/prompt", post(prompt_handler)).route("/turns/{turn_id}", get(turn_handler)).route("/command", post(command_handler)).route("/restart", post(restart_handler)).with_state(state)`
        - これは main.rs にあった `Router::new()...` の4 .route 構築を関数化したもの

    **read_turn は http.rs に定義しない。** `use crate::turn::read_turn;` で turn.rs から呼ぶだけ（Plan 03 Task 3 で turn.rs に既存）。http.rs 内で `async fn read_turn` のような関数定義を書くと **重複定義エラー**になる。

    可視性ルール再掲: 4ハンドラ関数は `async fn` のまま private（build_router 経由でしか呼ばれない）。`AppState`・`PromptReq`・`RestartResp` などは `pub` （main.rs から型として参照される）。`ise` は `pub(crate)`。`build_router` は `pub`。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; grep -c '^pub struct AppState {' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '    pub worker: Arc<Mutex<Worker>>,' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '    pub turns_dir: PathBuf,' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '    pub job_tx: mpsc::Sender<Job>,' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^pub(crate) fn ise' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^pub struct PromptReq' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^async fn prompt_handler' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^async fn turn_handler' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^async fn command_handler' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^async fn restart_handler' src/http.rs | grep -q '^1$' &amp;&amp; grep -c '^pub fn build_router' src/http.rs | grep -q '^1$' &amp;&amp; grep -c "turn_id.chars().all" src/http.rs | grep -q '^1$' &amp;&amp; grep -c '不正な turn_id' src/http.rs | grep -q '^1$' &amp;&amp; grep -c 'use axum::extract::{Path as AxumPath, State}' src/http.rs | grep -q '^1$' &amp;&amp; ! grep -E '^(pub )?(async )?fn read_turn' src/http.rs &amp;&amp; cargo build --release --lib 2>&amp;1 | grep -E '^(error|warning)' | wc -l | grep -q '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^pub struct AppState {' webif/src/http.rs` returns `1`
    - `grep -c '    pub worker: Arc<Mutex<Worker>>,' webif/src/http.rs` returns `1`
    - `grep -c '    pub turns_dir: PathBuf,' webif/src/http.rs` returns `1`
    - `grep -c '    pub job_tx: mpsc::Sender<Job>,' webif/src/http.rs` returns `1`
    - `grep -c '^pub(crate) fn ise' webif/src/http.rs` returns `1`
    - `grep -c '^pub struct PromptReq' webif/src/http.rs` returns `1`
    - `grep -c '^pub struct CommandReq' webif/src/http.rs` returns `1`
    - `grep -c '^pub struct CommandResp' webif/src/http.rs` returns `1`
    - `grep -c '^pub struct RestartResp' webif/src/http.rs` returns `1`
    - `grep -c '^async fn prompt_handler' webif/src/http.rs` returns `1`
    - `grep -c '^async fn turn_handler' webif/src/http.rs` returns `1`
    - `grep -c '^async fn command_handler' webif/src/http.rs` returns `1`
    - `grep -c '^async fn restart_handler' webif/src/http.rs` returns `1`
    - `grep -c '^pub fn build_router' webif/src/http.rs` returns `1`
    - **`read_turn` は http.rs に定義しない（turn.rs から import するだけ）**: `grep -Ec '^(pub )?(async )?fn read_turn' webif/src/http.rs` returns `0`、`grep -c 'use crate::turn::' webif/src/http.rs` returns `1`（このいずれかに `read_turn` が含まれている）、または明示的に `grep -c 'read_turn' webif/src/http.rs` returns 2 以上（use 文 + 呼び出し箇所）
    - **CRITICAL — パストラバーサル防止 verbatim**: `grep -c 'turn_id.chars().all(|c| c.is_ascii_digit() || c == ' webif/src/http.rs` returns `1`、`grep -c "不正な turn_id" webif/src/http.rs` returns `1`、`grep -c 'StatusCode::BAD_REQUEST' webif/src/http.rs` returns `1`
    - `grep -c 'use axum::extract::{Path as AxumPath, State}' webif/src/http.rs` returns `1`（エイリアス必須）
    - `grep -c 'use std::path::Path' webif/src/http.rs` returns `0`（http.rs では std::path::Path は不要、PathBuf のみ）
    - `grep -c 'use crate::turn::' webif/src/http.rs` returns `1`
    - `grep -c 'use crate::worker::Worker;' webif/src/http.rs` returns `1`
    - 日本語文字列 verbatim:
      - `grep -c '不正な turn_id' webif/src/http.rs` returns `1`
      - `grep -c 'ジョブキューが閉じています' webif/src/http.rs` returns `1`
      - `grep -c 'wait タイムアウト' webif/src/http.rs` returns `1`
      - `grep -c 'は存在しません' webif/src/http.rs` returns `1`
      - `grep -c 'snapshot 取得失敗' webif/src/http.rs` returns `1`
    - build_router の4ルート全て登録: `grep -E 'route\("/prompt"' webif/src/http.rs | wc -l` returns `1`、`grep -E 'route\("/turns/' webif/src/http.rs | wc -l` returns `1`、`grep -E 'route\("/command"' webif/src/http.rs | wc -l` returns `1`、`grep -E 'route\("/restart"' webif/src/http.rs | wc -l` returns `1`
    - `cargo build --release --lib` exits 0 with no warnings
    - `cargo clippy --release --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `webif/src/http.rs` が `AppState`・`ise`・4 DTO・4 ハンドラ・`build_router` を保持し、パストラバーサル防止ガードが verbatim 残り、`build_router` で4ルートを登録できる状態になりライブラリ単体として警告なしでビルドできる。`read_turn` は http.rs に **再定義せず** turn.rs (Plan 03 で移送済み) から `use crate::turn::read_turn;` で参照する。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: lib.rs に doc コメントを移送し、main.rs を ~30 行の wiring シムに縮小</name>
  <files>
    webif/src/lib.rs,
    webif/src/main.rs
  </files>
  <read_first>
    - webif/src/main.rs（現状: Plan 03 後。HTTP 層が残り、`main()` 関数が `Router::new()...` でルート構築している。lines 1-17 の architecture doc コメントが冒頭にある）
    - webif/src/lib.rs（現状: Plan 01 で `pub mod` 宣言だけ。doc コメント未移送）
    - webif/src/http.rs（Task 1 完成形、`AppState` と `build_router` を import するため）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"Pattern 1: lib.rs + thin main.rs"、§"Example — src/main.rs (target state, ~30 lines)"）
    - .planning/phases/02-module-refactor/02-PATTERNS.md（§"webif/src/main.rs (thinned entry point)"、§"webif/src/lib.rs"、§"Boundary-Fragile Excerpts" 3 番）
  </read_first>
  <action>
    2 つの操作を順番に実施する。

    **A) `webif/src/lib.rs` に architecture doc コメントを移送する:**

    現在の `webif/src/lib.rs`（プレースホルダ doc コメント + 5 pub mod）を以下に置き換える:
    - 1 行目から 17 行目に main.rs lines 1-17 の `//!` doc コメントブロック全体を **verbatim** 移送（`//! ht-webif — curl で叩く薄い WebIF。` から `//!       ht-mcp 異常 → POST /restart。MCP 呼び出しは 30s でタイムアウトする。` までの 17 行）
    - 空行を 1 行
    - `pub mod config;`、`pub mod http;`、`pub mod mcp;`、`pub mod turn;`、`pub mod worker;`（アルファベット順）

    main.rs 側の lines 1-17 の doc コメントは Task で扱う「B) main.rs 縮小」で削除する（lib.rs に移送されたため main.rs には不要）。

    **B) `webif/src/main.rs` を ~30 行に縮小する:**

    `webif/src/main.rs` を以下の構造に書き換える（既存内容は全削除して新規書き直しで OK）:

    1. doc コメント（lines 1-17）は lib.rs に移送済みなので **削除**
    2. `// ── HTTP 層 ─` セクション銀行と `AppState` / `ise` / 4 DTO / 4 ハンドラ を **削除**（http.rs に移送済み）
    3. main 関数内の `Router::new().route(...).with_state(state)` を `build_router(state)` 呼び出しに置換
    4. 削除済みコードの use 文を整理: `use anyhow::Result;`、`use std::sync::Arc;`、`use tokio::sync::{mpsc, Mutex};`、`use ht_webif::config::load_ht_mcp_path;`、`use ht_webif::http::{AppState, build_router};`、`use ht_webif::turn::{worker_loop, Job};`、`use ht_webif::worker::Worker;` のみに整理
    5. `#[tokio::main] async fn main() -> Result<()> { ... }` 本体は以下の手順を verbatim 保持:
       - `dotenvy::dotenv().ok();`（main の最初の文）
       - `let ht_mcp_path = load_ht_mcp_path();`（`std::env::var("HT_MCP_PATH")...` の代わり）
       - `println!("ht-mcp パス: {ht_mcp_path}");`
       - `let worker = Worker::new(ht_mcp_path).await?;`
       - `println!("claude セッション: {}", worker.session_id);`
       - `let turns_dir = std::env::current_dir()?.join("turns");`
       - `tokio::fs::create_dir_all(&turns_dir).await?;`
       - `println!("ターンディレクトリ: {}", turns_dir.display());`
       - `let worker = Arc::new(Mutex::new(worker));`
       - `let (job_tx, job_rx) = mpsc::channel::<Job>(64);`
       - `tokio::spawn(worker_loop(worker.clone(), turns_dir.clone(), job_rx));`
       - `let state = Arc::new(AppState { worker, turns_dir, job_tx });`
       - `let app = build_router(state);`
       - `let addr = "127.0.0.1:8080";`
       - `let listener = tokio::net::TcpListener::bind(addr).await?;`
       - 起動ログ 5 行（`WebIF 起動: http://{addr}` + 4 行の API ガイド `POST /prompt`、`POST /prompt {wait:true}`、`GET /turns/{id}`、`POST /command`、`POST /restart`）を **verbatim** 保持
       - `axum::serve(listener, app).await?;`
       - `Ok(())`

    `Arc::new(Mutex::new(worker))` と `worker.clone()` の配線（RESEARCH.md Risk 3）は **1バイトも変更しない**。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; head -1 src/lib.rs | grep -q '^//! ht-webif' &amp;&amp; grep -c '^//! ' src/lib.rs | awk '$1 &gt;= 15 {exit 0} {exit 1}' &amp;&amp; grep -c '^pub mod ' src/lib.rs | grep -q '^5$' &amp;&amp; ! grep -q '^//! ht-webif' src/main.rs &amp;&amp; ! grep -q '^struct AppState' src/main.rs &amp;&amp; ! grep -q '^async fn prompt_handler' src/main.rs &amp;&amp; ! grep -q '^Router::new' src/main.rs &amp;&amp; ! grep -q 'Router::new()' src/main.rs &amp;&amp; grep -c '^use ht_webif::http::' src/main.rs | grep -q '^1$' &amp;&amp; grep -c 'build_router(state)' src/main.rs | grep -q '^1$' &amp;&amp; grep -c 'tokio::spawn(worker_loop' src/main.rs | grep -q '^1$' &amp;&amp; grep -c 'Arc::new(Mutex::new(worker))' src/main.rs | grep -q '^1$' &amp;&amp; grep -c 'dotenvy::dotenv' src/main.rs | grep -q '^1$' &amp;&amp; cargo build --release 2>&amp;1 | tee /tmp/build-04.log &amp;&amp; ! grep -E '^(error|warning)' /tmp/build-04.log &amp;&amp; cargo clippy --all-targets --release -- -D warnings 2>&amp;1 | tee /tmp/clippy-04.log &amp;&amp; ! grep -E '^(error|warning)' /tmp/clippy-04.log &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - **lib.rs 状態**:
      - `head -1 webif/src/lib.rs` の出力が `//! ht-webif — curl で叩く薄い WebIF。`
      - `grep -c '^//! ' webif/src/lib.rs` returns 15 以上（doc コメントブロックを移送完了）
      - `grep -c '^pub mod ' webif/src/lib.rs` returns `5`
      - `grep -E '^pub mod (config|http|mcp|turn|worker);$' webif/src/lib.rs | wc -l` returns `5`
    - **main.rs 状態**:
      - `grep -c '^//! ht-webif' webif/src/main.rs` returns `0`（doc コメントは lib.rs に移送した）
      - `grep -c '^struct AppState' webif/src/main.rs` returns `0`
      - `grep -c '^async fn prompt_handler' webif/src/main.rs` returns `0`
      - `grep -c '^async fn turn_handler' webif/src/main.rs` returns `0`
      - `grep -c '^async fn command_handler' webif/src/main.rs` returns `0`
      - `grep -c '^async fn restart_handler' webif/src/main.rs` returns `0`
      - `grep -c 'Router::new()' webif/src/main.rs` returns `0`
      - `grep -c '^use ht_webif::http::' webif/src/main.rs` returns `1`
      - `grep -c 'build_router(state)' webif/src/main.rs` returns `1`
      - `grep -c 'tokio::spawn(worker_loop' webif/src/main.rs` returns `1`
      - `grep -c 'Arc::new(Mutex::new(worker))' webif/src/main.rs` returns `1`
      - `grep -c 'worker.clone()' webif/src/main.rs` returns `1`
      - `grep -c 'dotenvy::dotenv' webif/src/main.rs` returns `1`
      - `grep -c '^#\[tokio::main\]' webif/src/main.rs` returns `1`
      - `grep -c '^async fn main' webif/src/main.rs` returns `1`
      - `grep -c '127.0.0.1:8080' webif/src/main.rs` returns `1`
      - **main.rs は 50 行以下**: `wc -l webif/src/main.rs` returns less than 50
    - **起動ログ verbatim**:
      - `grep -c 'WebIF 起動' webif/src/main.rs` returns `1`
      - `grep -c 'ターンディレクトリ:' webif/src/main.rs` returns `1`
      - `grep -c 'claude セッション:' webif/src/main.rs` returns `1`
      - `grep -c 'ht-mcp パス:' webif/src/main.rs` returns `1`
    - `cargo build --release` exits 0 with no `warning` or `error` lines
    - `cargo clippy --all-targets --release -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `lib.rs` が doc コメント + 5 pub mod 宣言を持ち、`main.rs` は ~30-50 行の wiring シムに縮小されている。`build_router(state)` 経由で router を構築し、`Arc<Mutex<Worker>>` 共有配線が verbatim 保たれ、`cargo build --release` と clippy が両方クリーンに通る。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 [BLOCKING]: 起動して 8 curl smoke test を実行し、4 エンドポイント全ての挙動が完全保存されていることを実証</name>
  <files>(none — runtime verification)</files>
  <read_first>
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"Verification Strategy" の Test 1〜Test 8）
    - HT-PROTOCOL.md（§4 ファイル仕様、§6 完了検知）
    - webif/src/main.rs（Task 2 完成形）
    - webif/src/http.rs（Task 1 完成形）
  </read_first>
  <action>
    Refactor 完了後の WebIF を実際に起動して、4 エンドポイントの挙動が pre-refactor と完全に同じであることを 8 curl test で実証する。

    **前提条件:** `ht-mcp` が `$HT_MCP_PATH`（または PATH 上）にあり、`claude` CLI がログイン済みで PATH 上にある。前提が満たせない環境では Test 1-2、4-7 はスキップ可能（status: skipped を SUMMARY に記録）、Test 3・Test 8 は status ファイル不要なので必ず実行する。

    **手順:**

    1. ターミナル A で WebIF をフォアグラウンド起動（プロジェクトルートから）:
       ```
       cd /home/parallels/workspaces/ht-mcp-sample
       cargo run --release --bin ht-webif
       ```
       起動ログに `ht-mcp パス: ...`、`claude セッション: ...`、`ターンディレクトリ: .../turns`、`WebIF 起動: http://127.0.0.1:8080` の 4 行が出ることを確認。

    2. ターミナル B で 8 curl test を順番に実行:

       **Test 1: POST /prompt (async)**
       ```
       curl -s -X POST http://127.0.0.1:8080/prompt \
         -H 'Content-Type: application/json' \
         -d '{"prompt":"echo hello"}'
       ```
       期待: HTTP 200、レスポンス body に `"turn_id"` と `"status":"accepted"` が含まれる。turn_id を変数 TURN_ID に保存。

       **Test 2: GET /turns/{id} — running または done 状態**
       ```
       curl -s -w "%{http_code}\n" http://127.0.0.1:8080/turns/$TURN_ID
       ```
       期待: HTTP 200、body の `"status"` が `"running"` または `"done"` のいずれか。

       **Test 3 [CRITICAL — security control]: GET /turns/{id} — パストラバーサル拒否**
       ```
       curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8080/turns/..%2F..%2Fetc%2Fpasswd"
       curl -s "http://127.0.0.1:8080/turns/abc.def"
       curl -s "http://127.0.0.1:8080/turns/abc/def"
       ```
       期待: 全て **HTTP 400** を返し、body に `"不正な turn_id"` が含まれる。
       追加検証:
       ```
       curl -s -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:8080/turns/abc"
       ```
       → 400（ASCII digit と hyphen 以外を含む）

       **Test 4: POST /prompt with wait=true**
       ```
       curl -s -X POST http://127.0.0.1:8080/prompt \
         -H 'Content-Type: application/json' \
         -d '{"prompt":"今すぐ短く hello と返答してください。","wait":true}'
       ```
       期待: HTTP 200、body に `"status":"done"` と `"result"` フィールドが含まれる（または timeout の場合は 504 — その場合は claude TUI 応答時間が原因の環境問題なので別途記録）。

       **Test 5: POST /prompt with fresh=true**
       ```
       curl -s -X POST http://127.0.0.1:8080/prompt \
         -H 'Content-Type: application/json' \
         -d '{"prompt":"fresh test","fresh":true}'
       ```
       期待: HTTP 200、`"turn_id"` と `"status":"accepted"`。

       **Test 6: POST /command**
       ```
       curl -s -X POST http://127.0.0.1:8080/command \
         -H 'Content-Type: application/json' \
         -d '{"text":"/help"}'
       ```
       期待: HTTP 200、body に `"sent":"/help"` と `"snapshot"` フィールド。

       **Test 7: POST /restart**
       ```
       curl -s -X POST http://127.0.0.1:8080/restart
       ```
       期待: HTTP 200、body に `"status":"restarted"` と `"session_id"`。

       **Test 8: GET /turns/{id} — 存在しないターン (HTTP 404)**
       ```
       curl -s -o /dev/null -w "%{http_code}\n" "http://127.0.0.1:8080/turns/99990101-000000-000"
       ```
       期待: HTTP 404、body に `"は存在しません"` が含まれる。

    3. 全 test 結果（HTTP コード・期待 body 部分一致）を SUMMARY に記録する。ターミナル A の WebIF を Ctrl+C で停止する。

    4. プロセス残留確認: `pgrep -af 'ht-mcp\|claude' | grep -v grep` で残骸が無いことを確認（`POST /restart` の `kill_on_drop` 維持を検証）。

    どれか1つでも期待結果と異なる場合は SUMMARY に詳細を記録し、原因を Task 1/Task 2 の不備として戻り修正する。Test 3 が落ちた場合は **path-traversal security regression** として即時 fix（acceptance_criteria 違反）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample &amp;&amp; cargo build --release 2>&amp;1 | grep -v -E '^(\s|Compiling|Finished)' | grep -E 'error|warning' | wc -l | grep -q '^0$' &amp;&amp; (cargo run --release --bin ht-webif &amp; SERVER_PID=$!; sleep 8; CODE_TRAV=$(curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8080/turns/abc"); CODE_NOTFOUND=$(curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8080/turns/99990101-000000-000"); kill $SERVER_PID 2>/dev/null; wait $SERVER_PID 2>/dev/null; echo "PATH_TRAVERSAL_CODE=$CODE_TRAV NOT_FOUND_CODE=$CODE_NOTFOUND"; [ "$CODE_TRAV" = "400" ] &amp;&amp; [ "$CODE_NOTFOUND" = "404" ]) &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` exits 0 with no warnings
    - WebIF が `cargo run --release --bin ht-webif` で起動し、stdout に `WebIF 起動: http://127.0.0.1:8080` を出す
    - **Test 3 (パストラバーサル拒否)** — `curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8080/turns/abc"` returns `400`（ASCII 英字を含むため拒否）
    - **Test 3 続き** — `curl -s "http://127.0.0.1:8080/turns/abc"` の body に文字列 `不正な turn_id` を含む
    - **Test 8 (not found)** — `curl -s -o /dev/null -w "%{http_code}" "http://127.0.0.1:8080/turns/99990101-000000-000"` returns `404`
    - **Test 1 (async POST /prompt)** — body に `"turn_id"` と `"status":"accepted"` を含み、HTTP 200
    - **Test 2 (GET /turns running)** — body の `"status"` が `running` または `done`、HTTP 200
    - **Test 6 (POST /command)** — body に `"sent"` フィールド、HTTP 200
    - **Test 7 (POST /restart)** — body に `"status":"restarted"` と `"session_id"`、HTTP 200
    - **Test 4 / Test 5** — 環境依存（claude TUI が応答する場合のみ完全 pass。応答しない場合は SUMMARY に `skipped: claude TUI environment dependency` と記録、blocking 解除）
    - **プロセス残留なし** — WebIF Ctrl+C 後、`pgrep -f ht-mcp | wc -l` returns `0`（kill_on_drop 維持確認）
    - 全 Test 結果が `.planning/phases/02-module-refactor/02-04-SUMMARY.md` に記録される
  </acceptance_criteria>
  <done>
    Refactor 後の WebIF が起動し、4 エンドポイント（POST /prompt、GET /turns/{id}、POST /command、POST /restart）が pre-refactor と完全に同じ挙動を返すことが 8 curl test で実証された。特に Test 3 のパストラバーサル拒否（HTTP 400 + `"不正な turn_id"`）が成功し、唯一の security control が refactor を生き延びたことを確認できた。プロセス残留なしで `kill_on_drop` も保たれている。Phase 2 の Success Criteria 1〜4 が全て満たされる。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → axum router | 外部の curl / スクリプトから JSON body と URL パスが届く。URL の `turn_id` セグメントが filesystem へ直結するためパストラバーサル攻撃の入口になりうる |
| `AppState` 共有境界 | 4 ハンドラ全てが `Arc<AppState>` 経由で同じ `Mutex<Worker>` と `Sender<Job>` を見る。共有が壊れると command/restart が worker_loop と別 Worker を操作する silent failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering / Information Disclosure | `turn_handler` のパストラバーサル防止ガード `!turn_id.chars().all(|c| c.is_ascii_digit() || c == '-')` が refactor で欠落すると、`GET /turns/../../etc/passwd` 相当の経路で `state.turns_dir.join(format!("status-{turn_id}.json"))` がディレクトリ脱出を許す可能性。これは本コードベース唯一の active security control（ASVS V5）| mitigate | Task 1 の acceptance_criteria で `grep -c 'turn_id.chars().all' webif/src/http.rs` を `1` 固定、`grep -c "不正な turn_id" webif/src/http.rs` を `1` 固定。Task 3 の Test 3 で実際に `curl /turns/abc` が HTTP 400 を返し body に `不正な turn_id` を含むことを runtime 検証する |
| T-02-04-02 | Spoofing / Tampering | DTO 構造体（`PromptReq`、`CommandReq`）の `#[serde(default)]` 属性が refactor で剥がれると、`{"prompt":"x"}` のような fresh/wait 省略 JSON が deserialize エラーとなり挙動が変わる。または `text` フィールドが任意型に変わると JSON 注入で予期せぬ動作 | mitigate | Task 1 で構造体定義を verbatim 移送。`grep -E '#\\[serde\\(default\\)\\]' webif/src/http.rs | wc -l` を 2 以上で確認（PromptReq の `fresh` と `wait` 用） |
| T-02-04-03 | Denial of Service | `Arc<Mutex<Worker>>` 配線が refactor で壊れて HTTP ハンドラと worker_loop が別 Worker を見ると、`POST /restart` で新セッションができても進行中のターンは旧セッションに固定され、claude TUI が wedge した時の回復が崩れる | mitigate | Task 2 の acceptance_criteria で `grep -c 'Arc::new(Mutex::new(worker))' webif/src/main.rs` を `1` 固定、`grep -c 'worker.clone()' webif/src/main.rs` を `1` 固定、`grep -c 'Worker::new' webif/src/main.rs` を `1` 固定。Task 3 で `POST /restart` が `"status":"restarted"` を返すことを runtime 確認 |
| T-02-04-04 | Information Disclosure | `command_handler` の snapshot fallback `.unwrap_or_else(|e| format!("(snapshot 取得失敗: {e})"))` が消えて `?` 伝播になると、`/quit` などセッションを終わらせるコマンド送信後に 500 を返すようになる（旧来は寛容に文字列返却していた） | mitigate | Task 1 で verbatim 移送ルール、`grep -c 'snapshot 取得失敗' webif/src/http.rs` を `1` 固定 |

備考: HTTP 認証は v1 範囲外（SEC-01 deferred to v2）。localhost binding `127.0.0.1:8080` は維持（外部公開しない）。
</threat_model>

<verification>
- `webif/src/http.rs` が AppState・ise・4 DTO・4 ハンドラ・build_router を保持
- `read_turn` の関数定義は `http.rs` に **含まれない**（turn.rs に Plan 03 で既に移送済み、http.rs は `use crate::turn::read_turn;` で参照のみ）
- パストラバーサル防止ガード（`turn_id.chars().all(|c| c.is_ascii_digit() || c == '-')`）が `http.rs` 内に verbatim
- `webif/src/lib.rs` が doc コメント（main.rs lines 1-17 から移送）+ 5 pub mod 宣言を持つ
- `webif/src/main.rs` が 50 行以下の wiring シムで、`build_router(state)` 経由で router を構築
- `cargo build --release` 警告0・エラー0
- `cargo clippy --all-targets --release -- -D warnings` 警告0
- 8 curl smoke test の全てが期待結果を返す（環境依存の Test 4/5 を除く）。Test 3（パストラバーサル拒否 = HTTP 400）と Test 8（not found = HTTP 404）は必須
- WebIF Ctrl+C 後に ht-mcp 残留プロセスなし（kill_on_drop 確認）
</verification>

<success_criteria>
- Phase 2 Success Criteria 1 達成: `webif/src/` 配下に `config`・`mcp`・`worker`・`turn`・`http` の5モジュール（+`lib.rs`）が存在し、`main.rs` は wiring（dotenv 読込・ルータ構築・サーバ起動）のみ
- Phase 2 Success Criteria 2 達成: `cargo build --release` が警告なく完了し、4 エンドポイントが既存と同一の挙動で応答する（同期/非同期、`fresh:true`、`wait:true`、パストラバーサル拒否 400、not found 404 を含む）
- Phase 2 Success Criteria 3 達成: HT-PROTOCOL v1.1 ターンファイル方式（`prompt-/result-/status-<turnId>`）が同じファイル名・同じディレクトリ（`turns/`）で動作する（curl test で turn_id レスポンスのフォーマット `%Y%m%d-%H%M%S-%3f` 維持確認）
- Phase 2 Success Criteria 4 達成: モジュール境界が明示的になり、`McpClient`（mcp.rs）、`Worker`（worker.rs）、Job/process_job/worker_loop（turn.rs）、HTTP ハンドラ（http.rs）、設定読込（config.rs）が個別ファイルに分かれている
- Phase 3 のテスト基盤の土台が完成: `cargo test --lib` でモジュール内純粋関数（`build_prompt_body`、`turn_id` validation、`config::load_ht_mcp_path` など）をユニットテストできる
</success_criteria>

<output>
完了後、以下を作成すること: `.planning/phases/02-module-refactor/02-04-SUMMARY.md`
SUMMARY には特に以下を記録すること:
- Test 1〜Test 8 の各 curl test の結果（HTTP コード、body の期待文字列マッチ、または skipped 理由）
- 最終 main.rs の行数
- 最終時点の `wc -l webif/src/*.rs` 全モジュールの行数
- パストラバーサル拒否（Test 3）が成功したこと — security control preservation の証拠
- プロセス残留なしの確認結果（kill_on_drop 維持）
- (`read_turn` が `http.rs` に再定義されていないことの確認: `grep -Ec '^(pub )?(async )?fn read_turn' webif/src/http.rs` returns `0`)
</output>
</output>
