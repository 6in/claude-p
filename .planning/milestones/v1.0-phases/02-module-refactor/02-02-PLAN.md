---
phase: 02-module-refactor
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - webif/src/config.rs
  - webif/src/mcp.rs
  - webif/src/main.rs
autonomous: true
requirements:
  - CODE-01
  - CODE-02

must_haves:
  truths:
    - "`webif/src/config.rs` に `TURN_TIMEOUT`、`MCP_TIMEOUT`、`load_ht_mcp_path()` が存在し、外部から呼び出せる"
    - "`webif/src/mcp.rs` に `McpClient` 構造体と全メソッド（spawn / write_message / read_response / request / notify / handshake / call_tool / create_claude_session / close_session / send_keys / submit_line / snapshot）が存在する"
    - "`webif/src/main.rs` から `TURN_TIMEOUT`・`MCP_TIMEOUT` の定数定義と `McpClient` 関連コードが削除され、代わりに `use ht_webif::config::...;`・`use ht_webif::mcp::McpClient;` などで再導入されている"
    - "`McpClient::spawn` が `.stderr(Stdio::inherit())` と `.kill_on_drop(true)` を両方持つ（ht-mcp shared-fate 維持）"
    - "`McpClient` が `child: Child` フィールドを `#[allow(dead_code)]` 付きで保持している（drop-kill のため）"
    - "`cargo build --release` と `cargo clippy --all-targets --release -- -D warnings` が警告0・エラー0で完了する"
  artifacts:
    - path: "webif/src/config.rs"
      provides: "TURN_TIMEOUT、MCP_TIMEOUT、load_ht_mcp_path()"
      contains: "pub(crate) const TURN_TIMEOUT"
    - path: "webif/src/mcp.rs"
      provides: "McpClient 構造体 + メソッド一式"
      contains: "pub(crate) struct McpClient"
    - path: "webif/src/main.rs"
      provides: "Worker / ターン処理 / HTTP 層は未移送のまま残る"
      contains: "struct Worker"
  key_links:
    - from: "webif/src/main.rs"
      to: "webif/src/mcp.rs"
      via: "use ht_webif::mcp::McpClient;"
      pattern: "use ht_webif::mcp"
    - from: "webif/src/main.rs"
      to: "webif/src/config.rs"
      via: "use ht_webif::config::{...};"
      pattern: "use ht_webif::config"
    - from: "webif/src/mcp.rs"
      to: "webif/src/config.rs"
      via: "use crate::config::MCP_TIMEOUT;"
      pattern: "use crate::config::MCP_TIMEOUT"
---

<objective>
リーフ層を切り出す: `config`（定数 + 環境変数読み込み）と `mcp`（McpClient stdio JSON-RPC クライアント）を `main.rs` から専用ファイルへ移送する。`Worker`・ターン処理・HTTP 層は `main.rs` に残したまま、これら2モジュールだけ先に独立させる。

Purpose: `config` と `mcp` は誰にも依存されない（`config` は inbound 0、`mcp` は `Worker` のみが import）リーフ依存。先に切り出すことで Plan 03（`worker.rs`）が `use crate::mcp::McpClient;`・`use crate::config::MCP_TIMEOUT;` を素直に書けるようになる。

Output: `config.rs`（定数 + `load_ht_mcp_path`）と `mcp.rs`（McpClient 一式）が完成し、`main.rs` がそれらを `use ht_webif::...` で参照する状態。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-module-refactor/02-RESEARCH.md
@.planning/phases/02-module-refactor/02-PATTERNS.md
@.planning/phases/02-module-refactor/02-01-PLAN.md
@webif/src/main.rs
@webif/src/lib.rs
@webif/src/config.rs
@webif/src/mcp.rs
@CLAUDE.md

<interfaces>
<!-- webif/src/config.rs の最終形（公開シンボル） -->
<!-- - pub(crate) const TURN_TIMEOUT: Duration = Duration::from_secs(300); -->
<!-- - pub(crate) const MCP_TIMEOUT: Duration = Duration::from_secs(30); -->
<!-- - pub fn load_ht_mcp_path() -> String -->

<!-- webif/src/mcp.rs の公開 API（pub(crate) for crate-internal use only） -->
<!-- - pub(crate) struct McpClient { #[allow(dead_code)] child: Child, stdin: ChildStdin, lines: ..., next_id: i64 } -->
<!-- - impl McpClient { -->
<!--     pub(crate) async fn spawn(program: &str) -> Result<Self> -->
<!--     async fn write_message(&mut self, msg: &Value) -> Result<()>   // private: McpClient 内部のみ -->
<!--     async fn read_response(&mut self, id: i64) -> Result<Value>    // private -->
<!--     pub(crate) async fn request(&mut self, method: &str, params: Value) -> Result<Value> -->
<!--     pub(crate) async fn notify(&mut self, method: &str, params: Value) -> Result<()> -->
<!--     pub(crate) async fn handshake(&mut self) -> Result<()> -->
<!--     pub(crate) async fn call_tool(&mut self, name: &str, arguments: Value) -> Result<String> -->
<!--     pub(crate) async fn create_claude_session(&mut self) -> Result<String> -->
<!--     pub(crate) async fn close_session(&mut self, session_id: &str) -> Result<()> -->
<!--     pub(crate) async fn send_keys(&mut self, session_id: &str, keys: &[String]) -> Result<()> -->
<!--     pub(crate) async fn submit_line(&mut self, session_id: &str, text: &str) -> Result<()> -->
<!--     pub(crate) async fn snapshot(&mut self, session_id: &str) -> Result<String> -->
<!--   } -->

main.rs 側で必要な変更:
  - 1-17 行目の doc コメントブロックは未変更で残す（Plan 04 で lib.rs に移送する）
  - 19-36 行目の use 宣言から、McpClient のみが使う型（Stdio、Child/ChildStdin/ChildStdout/Command、AsyncBufReadExt/AsyncWriteExt/BufReader/Lines）と config 定数を削除
  - 38-41 行目の TURN_TIMEOUT/MCP_TIMEOUT 定数定義を削除
  - 43-198 行目の `// ── MCP クライアント ─` 銀行と McpClient struct + impl ブロックを完全削除
  - 523 行目の `std::env::var("HT_MCP_PATH")...` を `ht_webif::config::load_ht_mcp_path()` 呼び出しに置換
  - 330 行目の `TURN_TIMEOUT` 参照は `ht_webif::config::TURN_TIMEOUT` に変更（Worker/turn 処理はまだ main.rs にいる）

main.rs に追加する use 文:
  use ht_webif::config::{self, TURN_TIMEOUT, load_ht_mcp_path};
  use ht_webif::mcp::McpClient;

注意: `dotenvy::dotenv().ok();` は main.rs に残す（config.rs に移してはならない — RESEARCH.md Risk/Pitfall）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: webif/src/config.rs を完成させる</name>
  <files>webif/src/config.rs</files>
  <read_first>
    - webif/src/main.rs（lines 38-41 の TURN_TIMEOUT / MCP_TIMEOUT 定義、line 525 の env::var 呼び出し）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"File: `src/config.rs` (new file, complete)"）
    - .planning/phases/02-module-refactor/02-PATTERNS.md（§"webif/src/config.rs (constants + env loader)"）
    - CLAUDE.md（日本語コメント、`///` doc コメント、`SCREAMING_SNAKE_CASE` 定数）
  </read_first>
  <action>
    `webif/src/config.rs`（Plan 01 で作ったプレースホルダ）を以下の中身に置き換える。コードは `main.rs` から移送する形を取るが、`main.rs` の元のコード本体への変更は Task 3 で行う（このタスクでは `config.rs` 単独で完成させる）。

    `config.rs` の構成:
    - file-level doc コメント: `//! 設定定数と環境変数読み込み。`
    - `use std::time::Duration;` のみ import
    - `/// 1ターンの最大待ち時間（1試行あたり）。` という `///` doc コメントの上で `pub(crate) const TURN_TIMEOUT: Duration = Duration::from_secs(300);`
    - `/// MCP 呼び出し1回のタイムアウト（ht-mcp の wedge 対策）。` の上で `pub(crate) const MCP_TIMEOUT: Duration = Duration::from_secs(30);`
    - `///` doc コメント `/// HT_MCP_PATH 環境変数を読む。存在しない場合は "ht-mcp" を既定値とする。` の上で `pub fn load_ht_mcp_path() -> String { std::env::var("HT_MCP_PATH").unwrap_or_else(|_| "ht-mcp".to_string()) }`

    `dotenvy::dotenv()` は config.rs に移送してはならない（RESEARCH.md Anti-patterns: `dotenvy::dotenv()` は `main` 関数の最初の文として残す）。

    `TURN_TIMEOUT` は `pub(crate)` であり `pub` ではない（クレート内のみが使う）。`MCP_TIMEOUT` も同様。`load_ht_mcp_path` は `pub`（`main.rs` という `[[bin]]` 別クレートから呼ぶため `pub` 必須）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; grep -q 'pub(crate) const TURN_TIMEOUT: Duration = Duration::from_secs(300);' src/config.rs &amp;&amp; grep -q 'pub(crate) const MCP_TIMEOUT: Duration = Duration::from_secs(30);' src/config.rs &amp;&amp; grep -q 'pub fn load_ht_mcp_path() -> String' src/config.rs &amp;&amp; ! grep -q 'dotenvy' src/config.rs &amp;&amp; cargo build --release --lib 2>&amp;1 | grep -v -E '^(\s|Compiling|Finished)' | grep -E 'error|warning' | wc -l | grep -q '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pub(crate) const TURN_TIMEOUT: Duration = Duration::from_secs(300);' webif/src/config.rs` returns `1`
    - `grep -c 'pub(crate) const MCP_TIMEOUT: Duration = Duration::from_secs(30);' webif/src/config.rs` returns `1`
    - `grep -c 'pub fn load_ht_mcp_path() -> String' webif/src/config.rs` returns `1`
    - `grep -c 'std::env::var("HT_MCP_PATH")' webif/src/config.rs` returns `1`
    - `grep -c 'dotenvy' webif/src/config.rs` returns `0`（dotenv は main.rs に残す）
    - `grep -c '^use std::time::Duration;' webif/src/config.rs` returns `1`
    - file-level doc コメント: `head -1 webif/src/config.rs` starts with `//!`
    - `cargo build --release --lib` exits 0 with no warnings（main.rs 側は未変更だがリブだけ単独でビルド可能）
  </acceptance_criteria>
  <done>
    `webif/src/config.rs` が `TURN_TIMEOUT`、`MCP_TIMEOUT`、`load_ht_mcp_path()` の3シンボルを提供する完成形になり、ライブラリ単体（`--lib`）として警告なしでビルドできる。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: webif/src/mcp.rs を完成させる（McpClient 全体移送）</name>
  <files>webif/src/mcp.rs</files>
  <read_first>
    - webif/src/main.rs（lines 43-198 の `// ── MCP クライアント ─` 銀行から `McpClient::snapshot` メソッド終端まで）
    - webif/src/config.rs（Task 1 で完成済み、`MCP_TIMEOUT` を import するため）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"Risk 2: kill_on_drop(true) semantics"、§"Risk 5: AxumPath name collision"）
    - .planning/phases/02-module-refactor/02-PATTERNS.md（§"webif/src/mcp.rs (MCP stdio client)"、§"Boundary-Fragile Excerpts" 1 番）
    - CLAUDE.md（日本語の `anyhow!` メッセージ・`.with_context` メッセージ・doc コメントを verbatim 保存）
  </read_first>
  <action>
    `webif/src/mcp.rs`（Plan 01 で作ったプレースホルダ）を以下の中身に置き換える。`main.rs` lines 43-198 から `McpClient` 構造体と impl ブロック全体を verbatim 移送する。

    最終形の構造（順番厳守）:
    1. file-level コメント `// ── MCP クライアント（newline 区切り JSON-RPC を直接話す最小実装）─────────`（main.rs:43 のセクション銀行を移送）
    2. 空行
    3. import ブロック（main.rs lines 24, 32-34 から該当分を抽出）:
       - `use anyhow::{anyhow, Context, Result};`
       - `use serde_json::{json, Value};`
       - `use std::process::Stdio;`
       - `use std::time::Duration;`（line 189 の `Duration::from_millis(500)` で使う）
       - `use tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader, Lines};`
       - `use tokio::process::{Child, ChildStdin, ChildStdout, Command};`
       - `use crate::config::MCP_TIMEOUT;`
    4. 空行
    5. `pub(crate) struct McpClient { ... }`: main.rs lines 45-51 のフィールド定義を verbatim 移送、構造体宣言の `pub` 修飾子だけ `pub(crate) struct McpClient` に変更。`#[allow(dead_code)]` 属性は `child: Child` フィールドの直前に保持する（drop-kill のため必須）
    6. `impl McpClient { ... }` ブロック: main.rs lines 53-198 の中身を全て verbatim 移送
       - メソッド可視性変更:
         - `spawn` → `pub(crate) async fn spawn`
         - `write_message`、`read_response` は private のまま（McpClient 内部のみ使用）
         - `request`、`notify`、`handshake`、`call_tool`、`create_claude_session`、`close_session`、`send_keys`、`submit_line`、`snapshot` は全て `pub(crate) async fn`
       - メソッド本体・引数・戻り値・エラーメッセージ（`anyhow!("ht-mcp が stdout を閉じた")`、`anyhow!("stdin が取れない")`、`anyhow!("stdout が取れない")`、`anyhow!("MCP error: {err}")`、`anyhow!("ht-mcp 応答タイムアウト ({}s)", MCP_TIMEOUT.as_secs())`、`anyhow!("セッション ID が取れない:\n{text}")`、`anyhow!("セッション ID が空")`、`.with_context(|| format!("ht-mcp の起動に失敗: {program}"))`）は全て verbatim
       - `spawn` 内の Command 構築は **必ず verbatim**: `.stdin(Stdio::piped())`、`.stdout(Stdio::piped())`、`.stderr(Stdio::inherit())`（コメント `// ht-mcp のログは端末にそのまま流す` ごと）、`.kill_on_drop(true)`（コメント `// WebIF 終了・再起動時に ht-mcp も道連れにする` ごと）の4チェーンを全て保つ
       - `MCP_TIMEOUT` は `crate::config::MCP_TIMEOUT` の use 経由で参照される（main.rs にあった `const` 定義は削除済み）
       - `///` doc コメントは main.rs にあった通りに verbatim 保存

    `Cargo.toml` には触らない。`main.rs` にも触らない（Task 3 で扱う）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; grep -c 'pub(crate) struct McpClient' src/mcp.rs | grep -q '^1$' &amp;&amp; grep -c '#\[allow(dead_code)\]' src/mcp.rs | grep -q '^1$' &amp;&amp; grep -c 'child: Child,' src/mcp.rs | grep -q '^1$' &amp;&amp; grep -c '.stderr(Stdio::inherit())' src/mcp.rs | grep -q '^1$' &amp;&amp; grep -c '.kill_on_drop(true)' src/mcp.rs | grep -q '^1$' &amp;&amp; grep -c 'use crate::config::MCP_TIMEOUT;' src/mcp.rs | grep -q '^1$' &amp;&amp; cargo build --release --lib 2>&amp;1 | grep -E '^(error|warning)' | wc -l | grep -q '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^pub(crate) struct McpClient {$' webif/src/mcp.rs` returns `1`
    - `grep -c '#\[allow(dead_code)\]' webif/src/mcp.rs` returns `1`
    - `grep -c '    child: Child,' webif/src/mcp.rs` returns `1`
    - `grep -c '\.stderr(Stdio::inherit())' webif/src/mcp.rs` returns `1`
    - `grep -c '\.kill_on_drop(true)' webif/src/mcp.rs` returns `1`
    - `grep -c 'use crate::config::MCP_TIMEOUT;' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn spawn' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn request' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn handshake' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn call_tool' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn create_claude_session' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn close_session' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn send_keys' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn submit_line' webif/src/mcp.rs` returns `1`
    - `grep -c 'pub(crate) async fn snapshot' webif/src/mcp.rs` returns `1`
    - `grep -c 'async fn write_message' webif/src/mcp.rs` returns `1` （private — `pub(crate)` 修飾子なし）
    - `grep -c 'async fn read_response' webif/src/mcp.rs` returns `1`
    - 日本語エラーメッセージが verbatim: `grep -c 'ht-mcp が stdout を閉じた' webif/src/mcp.rs` returns `1`、`grep -c 'stdin が取れない' webif/src/mcp.rs` returns `1`、`grep -c 'ht-mcp の起動に失敗' webif/src/mcp.rs` returns `1`、`grep -c 'セッション ID が取れない' webif/src/mcp.rs` returns `1`、`grep -c 'MCP error:' webif/src/mcp.rs` returns `1`、`grep -c 'ht-mcp 応答タイムアウト' webif/src/mcp.rs` returns `1`
    - `cargo build --release --lib` exits 0 with no warnings
    - `cargo clippy --release --lib -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    `webif/src/mcp.rs` が `McpClient` 構造体（`#[allow(dead_code)] child: Child` 付き）と全12メソッドを verbatim 保持し、`.kill_on_drop(true)` + `.stderr(Stdio::inherit())` も保たれた状態でライブラリ単体として警告なしでビルドできる。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 [BLOCKING]: main.rs から config / mcp の元コードを削除し use ht_webif::... で再導入、cargo build 警告0 確認</name>
  <files>webif/src/main.rs</files>
  <read_first>
    - webif/src/main.rs（全体: lines 1-561。lines 38-41 と 43-198 が削除対象、lines 19-36 の use ブロックが整理対象）
    - webif/src/config.rs（Task 1 完成形）
    - webif/src/mcp.rs（Task 2 完成形）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"Pitfall 4: use import blocks ... avoid over-importing"）
  </read_first>
  <action>
    `webif/src/main.rs` から以下を物理的に削除し、代わりに `ht_webif::` 経由の import を追加する:

    1. lines 38-41 の `TURN_TIMEOUT` / `MCP_TIMEOUT` 定数定義を **削除**（`config.rs` に移送済み）
    2. lines 43-198 の `// ── MCP クライアント ─` セクション銀行と `McpClient` struct + impl ブロック全体を **削除**（`mcp.rs` に移送済み）
    3. lines 19-36 の use ブロックを整理:
       - **削除する use**: `use std::process::Stdio;`（McpClient だけが使用）、`use tokio::io::{AsyncBufReadExt, AsyncWriteExt, BufReader, Lines};`（McpClient だけが使用）、`use tokio::process::{Child, ChildStdin, ChildStdout, Command};`（McpClient だけが使用）
       - **残す use**: `use std::path::{Path, PathBuf};`、`use std::sync::Arc;`、`use std::time::Duration;`、`use anyhow::{anyhow, Context, Result};`（Worker が `.with_context` を使う）、axum 系全部、`use serde::{Deserialize, Serialize};`、`use serde_json::{json, Value};`、`use tokio::sync::{mpsc, Mutex};`、`use tokio::time::Instant;`
       - **`Context` の必要性確認**: `Worker::boot` などが `.with_context` を呼ぶか確認 — main.rs 上で `Context` を使う箇所がなければ `Context` も削除可能（現状の Worker は使っていないので削除可能だが、Plan 03 で `Context` を再追加するため残しても良い。`unused_imports` 警告が出る場合は削除する）
    4. lines 19-36 のすぐ後ろに新規 use を追加:
       - `use ht_webif::config::{load_ht_mcp_path, TURN_TIMEOUT};`
       - `use ht_webif::mcp::McpClient;`
    5. line 525 の `std::env::var("HT_MCP_PATH").unwrap_or_else(|_| "ht-mcp".to_string())` を `load_ht_mcp_path()` に置換
    6. line 330 周辺の `Instant::now() + TURN_TIMEOUT` の参照は use 経由で解決される（書き換え不要）

    `dotenvy::dotenv().ok();`（line 524）は **絶対に変更しない**（main に残す）。`#[tokio::main]` 属性も残す。

    Worker / Job / worker_loop / process_job / build_prompt_body / read_turn / AppState / 全 HTTP ハンドラ / `main()` のロジックは **1行も変更しない**（Plan 03、04 で扱う）。

    削除後、`cargo build --release` と `cargo clippy --all-targets --release -- -D warnings` を実行し警告0・エラー0 を確認する。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; ! grep -q '^struct McpClient' src/main.rs &amp;&amp; ! grep -q '^const TURN_TIMEOUT' src/main.rs &amp;&amp; ! grep -q '^const MCP_TIMEOUT' src/main.rs &amp;&amp; grep -c '^use ht_webif::config::' src/main.rs | grep -q '^1$' &amp;&amp; grep -c '^use ht_webif::mcp::McpClient;' src/main.rs | grep -q '^1$' &amp;&amp; grep -c 'load_ht_mcp_path()' src/main.rs | grep -q '^1$' &amp;&amp; grep -c 'dotenvy::dotenv()' src/main.rs | grep -q '^1$' &amp;&amp; cargo build --release 2>&amp;1 | tee /tmp/build-02.log &amp;&amp; ! grep -E '^(error|warning)' /tmp/build-02.log &amp;&amp; cargo clippy --all-targets --release -- -D warnings 2>&amp;1 | tee /tmp/clippy-02.log &amp;&amp; ! grep -E '^(error|warning)' /tmp/clippy-02.log &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^struct McpClient' webif/src/main.rs` returns `0`
    - `grep -c '^impl McpClient' webif/src/main.rs` returns `0`
    - `grep -c '^const TURN_TIMEOUT' webif/src/main.rs` returns `0`
    - `grep -c '^const MCP_TIMEOUT' webif/src/main.rs` returns `0`
    - `grep -c '^use ht_webif::config::' webif/src/main.rs` returns `1`
    - `grep -c '^use ht_webif::mcp::McpClient;' webif/src/main.rs` returns `1`
    - `grep -c 'load_ht_mcp_path()' webif/src/main.rs` returns `1`
    - `grep -c 'std::env::var("HT_MCP_PATH")' webif/src/main.rs` returns `0`（移送済み）
    - `grep -c 'dotenvy::dotenv()' webif/src/main.rs` returns `1`（main に残す）
    - `grep -c '^#\[tokio::main\]' webif/src/main.rs` returns `1`
    - `grep -c '^struct Worker' webif/src/main.rs` returns `1`（Plan 03 で扱う、まだ main にいる）
    - `grep -c '^struct AppState' webif/src/main.rs` returns `1`（Plan 04 で扱う、まだ main にいる）
    - `grep -c '^async fn worker_loop' webif/src/main.rs` returns `1`（Plan 03 で扱う、まだ main にいる）
    - `grep -c '^async fn prompt_handler' webif/src/main.rs` returns `1`（Plan 04 で扱う、まだ main にいる）
    - `cargo build --release` exits 0 with no `warning` or `error` lines
    - `cargo clippy --all-targets --release -- -D warnings` exits 0
    - main.rs の総行数は約 410 行（元 561 行 - 削除約 160 行 + 追加 use 約 2 行 = 約 405-410 行）: `wc -l webif/src/main.rs` < 420 かつ > 380
  </acceptance_criteria>
  <done>
    `main.rs` から `McpClient` と config 定数の元コードが削除され、`use ht_webif::config::...;` と `use ht_webif::mcp::McpClient;` 経由で再導入されている。`Worker` 以降のコードは未変更で `main.rs` に残っている。`cargo build --release` と clippy `-D warnings` が両方クリーンに通る。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ht-mcp` 子プロセス境界 | `McpClient` が stdio パイプを通じて外部バイナリと JSON-RPC で会話する。drop-kill 規約が壊れると orphan プロセスが残る |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering / Denial of Service | `McpClient::spawn` の `.kill_on_drop(true)` が移送中に失われると、ht-mcp 子プロセスが孤立してファイルディスクリプタ・PID を蓄積し続け、いずれリソース枯渇 | mitigate | Task 2 の acceptance_criteria で `grep -c '\.kill_on_drop(true)'` を `1` 固定。Task 2 で `Child` フィールド名と `#[allow(dead_code)]` 属性を文字列マッチで検査 |
| T-02-02-02 | Information Disclosure | `.stderr(Stdio::inherit())` が `.stderr(Stdio::piped())` などに書き換わると、ht-mcp のエラーログがオペレータの端末に出なくなり、wedge 検知が遅れる | mitigate | Task 2 の acceptance_criteria で `grep -c '\.stderr(Stdio::inherit())'` を `1` 固定 |
| T-02-02-03 | Tampering | `tokio::sync::Mutex` 関連の use は本プランでは触らない（Worker と一緒に Plan 03 で扱う）が、`mcp.rs` 側で誤って `std::sync` を import するとコンパイラが先回りで catch する | accept | `mcp.rs` には `Mutex` を import しない（McpClient は Mutex を持たない）。`cargo build --release` でコンパイラが catch |

備考: パストラバーサル防止は `turn_handler` 内（Plan 04 で扱う）にあるため本プランの対象外。
</threat_model>

<verification>
- `webif/src/config.rs` が `TURN_TIMEOUT`、`MCP_TIMEOUT`、`load_ht_mcp_path()` の3シンボルを提供
- `webif/src/mcp.rs` が `McpClient` と12メソッドを保持し、`.kill_on_drop(true)` と `.stderr(Stdio::inherit())` が両方残っている
- `webif/src/main.rs` から `McpClient` と config 定数が完全に削除された
- `cargo build --release` 警告0・エラー0
- `cargo clippy --all-targets --release -- -D warnings` 警告0
</verification>

<success_criteria>
- 「リーフ層」が独立: `config.rs` と `mcp.rs` がそれぞれ自己完結し、`main.rs` は両者を `use ht_webif::...` で参照する
- 全エラーメッセージ・doc コメントが日本語 verbatim
- shared-fate 保証（`kill_on_drop` + `Child` フィールド保持 + `stderr inherit`）が保たれている
- Plan 03 はこの状態の `mcp.rs` から `McpClient` を import して `worker.rs` を構築できる
</success_criteria>

<output>
完了後、以下を作成すること: `.planning/phases/02-module-refactor/02-02-SUMMARY.md`
</output>
