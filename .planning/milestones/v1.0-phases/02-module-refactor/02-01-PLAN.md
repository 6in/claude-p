---
phase: 02-module-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webif/Cargo.toml
  - webif/src/lib.rs
  - webif/src/config.rs
  - webif/src/mcp.rs
  - webif/src/worker.rs
  - webif/src/turn.rs
  - webif/src/http.rs
autonomous: true
requirements:
  - CODE-01
  - CODE-02

must_haves:
  truths:
    - "`webif/Cargo.toml` に `[lib] name = \"ht_webif\" path = \"src/lib.rs\"` と `[[bin]] name = \"ht-webif\" path = \"src/main.rs\"` の2セクションが存在する"
    - "`webif/src/lib.rs` が存在し、`pub mod config; pub mod http; pub mod mcp; pub mod turn; pub mod worker;` の5モジュール宣言を含む"
    - "`webif/src/{config,mcp,worker,turn,http}.rs` の5ファイルがプレースホルダ（コンパイル可能な空モジュール）として存在する"
    - "`webif/src/main.rs` の中身は完全に未変更で 561 行のままである"
    - "`cargo build --release` が警告0・エラー0 で完了する"
    - "`cargo run --bin ht-webif` がこれまでと同じ起動ログを出力できる（ht-mcp 起動可能な環境において）"
  artifacts:
    - path: "webif/Cargo.toml"
      provides: "lib + bin ターゲット宣言"
      contains: "[lib]"
    - path: "webif/src/lib.rs"
      provides: "ライブラリクレートのモジュールルート"
      contains: "pub mod config"
    - path: "webif/src/config.rs"
      provides: "config モジュールのプレースホルダ"
    - path: "webif/src/mcp.rs"
      provides: "mcp モジュールのプレースホルダ"
    - path: "webif/src/worker.rs"
      provides: "worker モジュールのプレースホルダ"
    - path: "webif/src/turn.rs"
      provides: "turn モジュールのプレースホルダ"
    - path: "webif/src/http.rs"
      provides: "http モジュールのプレースホルダ"
  key_links:
    - from: "webif/Cargo.toml"
      to: "webif/src/lib.rs"
      via: "[lib] path = \"src/lib.rs\""
      pattern: "path\\s*=\\s*\"src/lib.rs\""
    - from: "webif/Cargo.toml"
      to: "webif/src/main.rs"
      via: "[[bin]] path = \"src/main.rs\""
      pattern: "path\\s*=\\s*\"src/main.rs\""
    - from: "webif/src/lib.rs"
      to: "webif/src/{config,mcp,worker,turn,http}.rs"
      via: "pub mod 宣言"
      pattern: "pub mod (config|mcp|worker|turn|http)"
---

<objective>
クレートスケルトンを作る。`webif/Cargo.toml` に `[lib]` と `[[bin]]` セクションを追加し、`webif/src/lib.rs` と 5 つの空モジュール（`config.rs`、`mcp.rs`、`worker.rs`、`turn.rs`、`http.rs`）を作成する。`main.rs` 本体は一切触らない。本プランは「ビルド可能な状態を保ったままモジュール器を用意する」だけのウォームアップで、コード移動は Plan 02 以降で行う。

Purpose: lib + bin の二重ターゲット構成（RESEARCH.md Pattern 1）を事前に確立しておくことで、以後の Plan 02-04 でシンボルを物理的に移動するときに `cargo build` が常に通る増分作業にできる。

Output: `[lib]`/`[[bin]]` 宣言を含む `Cargo.toml`、`pub mod` だけが書かれた `lib.rs`、各モジュールのプレースホルダファイル6本。`main.rs` は未変更。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-module-refactor/02-RESEARCH.md
@.planning/phases/02-module-refactor/02-PATTERNS.md
@webif/Cargo.toml
@webif/src/main.rs
@CLAUDE.md

<interfaces>
<!-- Cargo.toml に追加する2セクション（RESEARCH.md §"Cargo.toml Changes Required"より） -->
<!-- - [lib] name は Rust 識別子なのでアンダースコア (ht_webif) -->
<!-- - [[bin]] name は既存バイナリ名と一致させるためハイフン (ht-webif) -->

webif/Cargo.toml に追記:
  [lib]
  name = "ht_webif"
  path = "src/lib.rs"

  [[bin]]
  name = "ht-webif"
  path = "src/main.rs"

webif/src/lib.rs の中身（doc コメントは Plan 04 で main.rs から移送するため、本プランでは最低限）:
  //! ht-webif library crate. 詳細な doc コメントは Plan 04 で main.rs から移送する。
  pub mod config;
  pub mod http;
  pub mod mcp;
  pub mod turn;
  pub mod worker;

各プレースホルダモジュールは「空でコンパイル可能」であれば良い。
file-level の `//!` doc コメントを1行だけ書く（例: `//! MCP stdio JSON-RPC クライアント。Plan 02 で main.rs から移送する。`）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cargo.toml に [lib] + [[bin]] セクションを追加</name>
  <files>webif/Cargo.toml</files>
  <read_first>
    - webif/Cargo.toml（現状を確認: 13–20 行目に `[dependencies]` がある、`[lib]`・`[[bin]]` セクションは未存在）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"Cargo.toml Changes Required"）
    - .planning/phases/02-module-refactor/02-PATTERNS.md（§"webif/Cargo.toml (additive edit)"）
  </read_first>
  <action>
    `webif/Cargo.toml` の末尾（`[dependencies]` ブロックの後ろ）に以下2セクションを追記する。既存の `[package]` や `[dependencies]` の中身は1行も変更しない。

    追記する内容（RESEARCH.md §"Cargo.toml Changes Required" の通り）:
    - セクション1: `[lib]` `name = "ht_webif"` `path = "src/lib.rs"`（アンダースコア — Rust 識別子）
    - セクション2: `[[bin]]` `name = "ht-webif"` `path = "src/main.rs"`（ハイフン — `[package]` の `name` と一致）

    末尾の余分な空行は1行までに保つ。`[lib]` と `[[bin]]` の間に空行1つ。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; grep -c '^\[lib\]$' Cargo.toml | grep -q '^1$' &amp;&amp; grep -c '^\[\[bin\]\]$' Cargo.toml | grep -q '^1$' &amp;&amp; grep -q 'name = "ht_webif"' Cargo.toml &amp;&amp; grep -q 'name = "ht-webif"' Cargo.toml &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\[lib\]$' webif/Cargo.toml` returns `1`
    - `grep -c '^\[\[bin\]\]$' webif/Cargo.toml` returns `1`
    - `grep -E 'name\s*=\s*"ht_webif"' webif/Cargo.toml` matches exactly once
    - `grep -E 'name\s*=\s*"ht-webif"' webif/Cargo.toml` matches exactly once
    - `grep -E 'path\s*=\s*"src/lib.rs"' webif/Cargo.toml` matches exactly once
    - `grep -E 'path\s*=\s*"src/main.rs"' webif/Cargo.toml` matches exactly once
    - 既存の `[package]` と `[dependencies]` セクションの行は元のままで欠落・追加なし: `grep -c '^edition = "2021"' webif/Cargo.toml` returns `1`、`grep -c '^tokio = ' webif/Cargo.toml` returns `1`
  </acceptance_criteria>
  <done>
    `webif/Cargo.toml` に `[lib]`（`name = "ht_webif"`、`path = "src/lib.rs"`）と `[[bin]]`（`name = "ht-webif"`、`path = "src/main.rs"`）の2セクションが追加され、既存セクションは1バイトも変更されていない。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: webif/src/lib.rs と 5 つの空モジュールファイルを作成</name>
  <files>
    webif/src/lib.rs,
    webif/src/config.rs,
    webif/src/mcp.rs,
    webif/src/worker.rs,
    webif/src/turn.rs,
    webif/src/http.rs
  </files>
  <read_first>
    - webif/src/main.rs（doc コメントは移送しないことを再確認: lines 1-17 は Plan 04 で扱う）
    - .planning/phases/02-module-refactor/02-RESEARCH.md（§"File: `src/lib.rs` (new file, complete)"）
    - .planning/phases/02-module-refactor/02-PATTERNS.md（§"webif/src/lib.rs (re-export root)"）
    - CLAUDE.md（コメントは日本語、ファイル先頭に `//!` doc コメント1行）
  </read_first>
  <action>
    6つのファイルを新規作成する。中身は最小限で、コードは1行も移送しない（移送は Plan 02-04 の責務）。

    1. `webif/src/lib.rs`:
       - 1行目に file-level doc コメント `//! ht-webif library crate. 詳細な doc コメントは Plan 04 で main.rs から移送する。`
       - 空行を1行
       - 5つの `pub mod` 宣言: `pub mod config;`、`pub mod http;`、`pub mod mcp;`、`pub mod turn;`、`pub mod worker;`（アルファベット順）

    2. `webif/src/config.rs`:
       - 中身は file-level doc コメント1行のみ `//! 設定定数と環境変数読み込み。Plan 02 で main.rs から TURN_TIMEOUT / MCP_TIMEOUT / HT_MCP_PATH 読み込みを移送する。`

    3. `webif/src/mcp.rs`:
       - file-level doc コメント1行 `//! MCP stdio JSON-RPC クライアント。Plan 02 で main.rs から McpClient を移送する。`

    4. `webif/src/worker.rs`:
       - file-level doc コメント1行 `//! claude セッション寿命管理。Plan 03 で main.rs から Worker を移送する。`

    5. `webif/src/turn.rs`:
       - file-level doc コメント1行 `//! ターン処理（Job、worker_loop、process_job、build_prompt_body、read_turn）。Plan 03 で main.rs から移送する。`

    6. `webif/src/http.rs`:
       - file-level doc コメント1行 `//! HTTP ハンドラ・DTO・ルータ構築。Plan 04 で main.rs から移送する。`

    どのファイルにも `use` 文や struct 定義や fn 定義を書いてはならない（Plan 02-04 が「空のモジュールに移送する」前提で動く）。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; ls src/lib.rs src/config.rs src/mcp.rs src/worker.rs src/turn.rs src/http.rs &amp;&amp; grep -c '^pub mod ' src/lib.rs | grep -q '^5$' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f webif/src/lib.rs` exits 0
    - `test -f webif/src/config.rs` exits 0
    - `test -f webif/src/mcp.rs` exits 0
    - `test -f webif/src/worker.rs` exits 0
    - `test -f webif/src/turn.rs` exits 0
    - `test -f webif/src/http.rs` exits 0
    - `grep -c '^pub mod ' webif/src/lib.rs` returns `5`
    - `grep -E '^pub mod (config|http|mcp|turn|worker);$' webif/src/lib.rs | wc -l` returns `5`
    - `head -1 webif/src/config.rs` の最初の文字が `//!`
    - `head -1 webif/src/mcp.rs` の最初の文字が `//!`
    - `head -1 webif/src/worker.rs` の最初の文字が `//!`
    - `head -1 webif/src/turn.rs` の最初の文字が `//!`
    - `head -1 webif/src/http.rs` の最初の文字が `//!`
    - 各プレースホルダは10行以下: `wc -l webif/src/{config,mcp,worker,turn,http}.rs` の各行が10未満
    - `grep -E '^(use |struct |fn |impl )' webif/src/{config,mcp,worker,turn,http}.rs` returns 0 matches（コードは移送していない）
    - `webif/src/main.rs` は完全に未変更: `wc -l webif/src/main.rs` returns `562`（最終行の改行を含めて561行ファイル）または `561` (`wc -l` の数え方依存)。`grep -c '^fn main' webif/src/main.rs` returns `1`、`grep -c '^struct McpClient' webif/src/main.rs` returns `1`
  </acceptance_criteria>
  <done>
    `webif/src/lib.rs` が 5 つの `pub mod` 宣言を含み、`config.rs`・`mcp.rs`・`worker.rs`・`turn.rs`・`http.rs` が空のプレースホルダとして存在する。`webif/src/main.rs` は1バイトも変更されていない。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3 [BLOCKING]: ビルド検証 — lib + bin が警告なしでビルドできることを確認</name>
  <files>(none — read-only verification)</files>
  <read_first>
    - webif/Cargo.toml（Task 1 の追加結果）
    - webif/src/lib.rs（Task 2 の作成結果）
  </read_first>
  <action>
    `webif/` ディレクトリで `cargo build --release` を実行する。エラーが出ないこと、警告も出ないことを確認する。さらに `cargo build --release --bin ht-webif` と `cargo build --release --lib` の両方が個別にビルド可能であることを確認する。

    重要: この時点では `main.rs` は単一ファイル運用のまま（=従来通り `struct McpClient` などが `main` バイナリに含まれている）。`lib.rs` 側は空のモジュール宣言のみ。両方ターゲットを宣言したことで dead-code 警告などが出ないか、目視で確認する。

    `cargo clippy --all-targets -- -D warnings` も実行し、警告0で通ることを確認する（refactor 中は警告0を維持する方針）。

    エラーまたは警告が出た場合は Task 1 または Task 2 の不備（typo、`use` 残し、参照漏れ）を疑い、修正してから再ビルドする。
  </action>
  <verify>
    <automated>cd /home/parallels/workspaces/ht-mcp-sample/webif &amp;&amp; cargo build --release 2>&amp;1 | tee /tmp/build-01.log &amp;&amp; ! grep -E '^(error|warning)' /tmp/build-01.log &amp;&amp; cargo clippy --all-targets --release -- -D warnings 2>&amp;1 | tee /tmp/clippy-01.log &amp;&amp; ! grep -E '^(error|warning)' /tmp/clippy-01.log &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --release` exits 0
    - `cargo build --release` の stdout/stderr に `error` を含む行が0個
    - `cargo build --release` の stdout/stderr に `warning` を含む行が0個（`Compiling` や `Finished` は許容）
    - `cargo build --release --lib` exits 0
    - `cargo build --release --bin ht-webif` exits 0
    - `cargo clippy --all-targets --release -- -D warnings` exits 0
    - `target/release/ht-webif` バイナリが存在し実行可能: `test -x webif/target/release/ht-webif` exits 0
    - `target/release/libht_webif.rlib` が存在: `ls webif/target/release/libht_webif*.rlib` で1ファイル以上
  </acceptance_criteria>
  <done>
    `cargo build --release` および `cargo clippy --all-targets --release -- -D warnings` が警告0・エラー0 で完了し、lib と bin の両ターゲットが個別にビルド可能。`target/release/ht-webif` バイナリが生成されている。これより以降の Plan 02-04 はこのビルド可能状態を保ったまま増分にシンボル移送できる。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build config → 実行バイナリ | `Cargo.toml` の `[lib]` `[[bin]]` 宣言ミスは間違ったバイナリを生成しうるが、現段階ではロジック未移送のため挙動影響なし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `webif/Cargo.toml` — `[lib] name` を `ht-webif`（ハイフン）と誤記すると Rust 識別子規則違反で `cargo build` が即失敗 | mitigate | Task 1 の acceptance_criteria で `grep -E 'name\\s*=\\s*"ht_webif"'` をアンダースコア固定で検査。Task 3 の `cargo build --release` が即落ちることで検知される |
| T-02-01-02 | Denial of Service | `webif/src/lib.rs` のプレースホルダ作成漏れにより `pub mod foo;` が指すファイルが無い場合、`cargo build` 失敗で以後のプラン全体が停止 | mitigate | Task 2 の acceptance_criteria で 6 ファイル全ての `test -f` を確認、Task 3 で `cargo build --release` が成功することをゲートにする |

備考: 本プランはコード未移送のため、ランタイムの攻撃面は変化しない（パストラバーサル防止・kill_on_drop・stderr inherit などは全て `main.rs` 内で未変更）。HTTP 表面の脅威モデルは Plan 04 で扱う。
</threat_model>

<verification>
- `cargo build --release` が警告0・エラー0で完了する
- `cargo clippy --all-targets --release -- -D warnings` が通る
- `webif/src/main.rs` の中身が 1 バイトも変わっていない（`git diff webif/src/main.rs` が空、または `wc -l` が変化なし）
- `webif/Cargo.toml` に `[lib]` と `[[bin]]` が追加され、既存の `[package]`/`[dependencies]` は未変更
</verification>

<success_criteria>
- 6 つの新規ファイルが存在: `lib.rs`、`config.rs`、`mcp.rs`、`worker.rs`、`turn.rs`、`http.rs`
- `Cargo.toml` に `[lib] name = "ht_webif"` と `[[bin]] name = "ht-webif"` の2セクションが追加されている
- 警告0・エラー0 で `cargo build --release` が完了する
- `main.rs` の中身は完全に未変更（後続プランで移送する）
- Plan 02 はこの状態の上に積み増しできる（空モジュールに `McpClient` と `config` を流し込むだけ）
</success_criteria>

<output>
完了後、以下を作成すること: `.planning/phases/02-module-refactor/02-01-SUMMARY.md`
</output>
