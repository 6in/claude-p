---
phase: quick-260526-b7m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - .github/workflows/release.yml
autonomous: false
requirements:
  - QUICK-260526-b7m

must_haves:
  truths:
    - "`make help` lists all recipes and runs without invoking cargo/cross"
    - "`make check-tools` succeeds when docker + cross are installed, and fails fast with an install hint when either is missing"
    - "`make linux-amd64` produces `dist/ht-webif-linux-amd64` (ELF, x86_64) via `cross build --release --target x86_64-unknown-linux-gnu`"
    - "`make linux-arm64` produces `dist/ht-webif-linux-arm64` (ELF, aarch64) via `cross build --release --target aarch64-unknown-linux-gnu`"
    - "`make windows-amd64` produces `dist/ht-webif-windows-amd64.exe` (PE32+) via `cross build --release --target x86_64-pc-windows-gnu`"
    - "`make all` runs the 3 cross-stable targets serially without invoking macOS recipes"
    - "`make clean-dist` removes the `dist/` directory"
    - "Makefile contains NO macos-* recipes (macOS handled by CI matrix only, per D-01)"
    - "Makefile contains NO recipes that duplicate justfile (no `build`/`run`/`test`/`fmt`/`clippy`/`clean`), per D-02"
    - "`.github/workflows/release.yml` triggers on `push: tags: ['v*']` AND `workflow_dispatch`, and uploads all 6 target binaries as artifacts (windows-arm64 built natively on windows-latest via `rustup target add aarch64-pc-windows-msvc`)"
    - "`.github/workflows/ci.yml` is untouched"
    - "`justfile` is untouched"
    - "`webif/src/*` and `webif/Cargo.toml` are untouched"
  artifacts:
    - path: "Makefile"
      provides: "クロスビルド専用 Makefile (linux-amd64/arm64 + windows-amd64 + ツールチェック + help + clean-dist)"
      contains: "cross build --release --target"
      min_lines: 60
    - path: ".github/workflows/release.yml"
      provides: "6 ターゲット release matrix workflow (tag push + manual dispatch、artifact upload)"
      contains: "actions/upload-artifact@v4"
      min_lines: 60
  key_links:
    - from: "Makefile"
      to: "webif/Cargo.toml"
      via: "cross --manifest-path webif/Cargo.toml build ..."
      pattern: "manifest-path[[:space:]]+webif/Cargo.toml"
    - from: ".github/workflows/release.yml"
      to: "matrix.target"
      via: "strategy.matrix.include の 6 ターゲット定義 (linux/windows は ubuntu-latest+cross、macos は macos-latest+cargo)"
      pattern: "x86_64-unknown-linux-gnu|aarch64-unknown-linux-gnu|x86_64-pc-windows-gnu|aarch64-pc-windows-msvc|x86_64-apple-darwin|aarch64-apple-darwin"
---

<objective>
リポジトリ直下に **クロスビルド専用 Makefile** と **GitHub Actions release matrix workflow** を新規追加し、Linux/Windows/macOS × AMD64/ARM64 の 6 ターゲットで `ht-webif` バイナリを生成できるようにする。dev フロー (justfile) と CI フロー (ci.yml) は一切触らない。

Purpose: タグ push (`v*`) または手動 dispatch で 6 OS×Arch 組合せの binary を artifact として残せる体制を整える。dev box (Linux) からは Docker+cross で 3 ターゲットをローカル生成可能にする。macOS の 2 ターゲットは GitHub Actions の macos-latest runner に委譲し、ローカル Makefile からは完全に排除する (D-01)。

Output: `Makefile` (repo root)、`.github/workflows/release.yml` (新規)。`dist/<binary>` がローカルクロスビルドの成果物配置場所。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260526-b7m-os-makefile-cross-linux-windows-amd64-ar/260526-b7m-CONTEXT.md
@justfile
@.github/workflows/ci.yml
@webif/Cargo.toml

<interfaces>
<!-- 既存の dev / CI 表面。Makefile・release.yml が衝突してはいけない領域を明示。 -->

justfile (touch 厳禁、共存):
- set working-directory := 'webif'
- recipes: build / run / test / fmt / clippy / clean (cd は不要、`just <recipe>` のみで動く)
- 役割: dev shortcut

.github/workflows/ci.yml (touch 厳禁、別ファイル):
- name: CI
- on: push/pull_request to main
- runs-on: ubuntu-latest 単一
- working-directory: webif
- ステップ: fmt-check → clippy -D warnings → test (release profile)

webif/Cargo.toml:
- [package] name = "ht-webif"
- [[bin]] name = "ht-webif", path = "src/main.rs"
- [lib] name = "ht_webif", path = "src/lib.rs"

cross の現状 (planner 調査結果):
- 公式 Docker image が用意されている triple: x86_64-unknown-linux-gnu, aarch64-unknown-linux-gnu, x86_64-pc-windows-gnu
- `aarch64-pc-windows-msvc` は cross 公式 image なし (cross-toolchains 側 = ユーザがビルドする必要あり)
- macOS targets も同様に公式 image なし (Apple SDK ライセンス都合)

ターゲット triple マップ:
| target name    | triple                       | toolchain         |
|----------------|------------------------------|-------------------|
| linux-amd64    | x86_64-unknown-linux-gnu     | cross (Docker)    |
| linux-arm64    | aarch64-unknown-linux-gnu    | cross (Docker)    |
| windows-amd64  | x86_64-pc-windows-gnu        | cross (Docker)    |
| windows-arm64  | aarch64-pc-windows-msvc      | CI only (windows-latest + rustup)  |
| macos-amd64    | x86_64-apple-darwin          | CI only (macos-latest + cargo)     |
| macos-arm64    | aarch64-apple-darwin         | CI only (macos-latest + cargo)     |

**windows-arm64 配置の planner 判断**: cross 公式 image が無いため、Makefile には windows-arm64 レシピを置かない。代わりに release.yml の matrix で `windows-latest` runner に `rustup target add aarch64-pc-windows-msvc` を実行させ native cargo build でクロスする (Windows ホスト上で MSVC linker が使えるため成立)。これにより 6 ターゲット全てが CI で生成され、ローカル Makefile は cross が「素直に」サポートする 3 ターゲットに集中できる。CONTEXT D-01 の "windows-arm64 を planner で決める" 委譲枠を、「CI のみで生成、Makefile 不在」として消化する。

Cargo --manifest-path パターン (D-03 適用):
- cross は --manifest-path をサポート。`cross build --release --target <triple> --manifest-path webif/Cargo.toml`
- これにより Makefile から repo root にいながら webif/ をビルドできる ($(CARGO_DIR)/Cargo.toml を変数化)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Makefile を repo 直下に新規作成 (linux-amd64/arm64 + windows-amd64 + ツールチェック + help + clean-dist)</name>
  <files>Makefile</files>
  <action>
リポジトリ直下に `Makefile` を新規作成する (D-03)。クロスビルド専用、justfile の dev レシピと衝突する名前 (build/run/test/fmt/clippy/clean) は一切作らない (D-02)。macos-* レシピは置かない (D-01: macOS は CI 委譲)。

Makefile の構成要素 (上から順):

(1) ヘッダコメント — 日本語、`#` で始まる。このファイルの責務 (cross-build 専用)、justfile との役割分担、macOS は CI 委譲、windows-arm64 も CI 委譲 (cross 公式 image 無し) を 5-10 行で説明する。

(2) 変数定義: `CARGO_MANIFEST := webif/Cargo.toml` (D-03 の --manifest-path 経由で webif/ を指す)、`BIN_NAME := ht-webif`、`DIST_DIR := dist`、`CROSS := cross`、3 つの triple 変数 `TRIPLE_LINUX_AMD64 := x86_64-unknown-linux-gnu`、`TRIPLE_LINUX_ARM64 := aarch64-unknown-linux-gnu`、`TRIPLE_WINDOWS_AMD64 := x86_64-pc-windows-gnu`。

(3) `.PHONY` 宣言で全ターゲット名を列挙: help、check-tools、linux-amd64、linux-arm64、windows-amd64、all、clean-dist。

(4) **default ターゲットは `help`** にする (D-02 / D-15: 曖昧な default ビルドを避ける)。Makefile 先頭の最初のレシピを help にすれば自動で default 化する。

(5) `help` レシピ — 各ターゲットの 1 行説明を `echo` で出力する (Claude's Discretion)。日本語でよい。少なくとも以下を必ず案内: make all / make linux-amd64 / make linux-arm64 / make windows-amd64 / make clean-dist / make check-tools、および「macOS と windows-arm64 は CI (release.yml) で生成」「dev は `just <recipe>` を使う」の 2 行注記。

(6) `check-tools` レシピ (D-04: fail-fast + install hint) — `command -v docker >/dev/null 2>&1` と `command -v cross >/dev/null 2>&1` の 2 段チェック。失敗時はインストール手順 (`cargo install cross --git https://github.com/cross-rs/cross` および Docker 公式インストール案内 URL https://docs.docker.com/get-docker/) を echo して `exit 1`。1 つのレシピ内で docker と cross の両方をチェックする (両者必須なため)。

(7) `$(DIST_DIR)` レシピ (order-only prerequisite で使う) — `mkdir -p $(DIST_DIR)`。

(8) 3 つのクロスビルドレシピ (linux-amd64 / linux-arm64 / windows-amd64) — 全て以下のパターン:
  - prerequisites: `check-tools | $(DIST_DIR)` (| は order-only prerequisite)
  - body 1: `$(CROSS) build --release --target $(TRIPLE_<NAME>) --manifest-path $(CARGO_MANIFEST)`
  - body 2: 成果物コピー — `cp webif/target/$(TRIPLE_<NAME>)/release/$(BIN_NAME)<ext> $(DIST_DIR)/$(BIN_NAME)-<short>.<ext>`
    - linux 2 つは拡張子なし、出力名は ht-webif-linux-amd64 / ht-webif-linux-arm64
    - windows は `.exe` 付き、出力名は ht-webif-windows-amd64.exe
  - body 3: `@echo "built: $(DIST_DIR)/..."` で完了通知 (Claude's Discretion)

(9) `all` レシピ — prerequisites として 3 つのクロスビルドターゲットを並べる (`linux-amd64 linux-arm64 windows-amd64`)。body は `@echo` で「3 つの cross-stable target が完了。macOS と windows-arm64 は .github/workflows/release.yml で生成される」と表示する程度。

(10) `clean-dist` レシピ — `rm -rf $(DIST_DIR)`。**注意:** `cargo clean` は呼ばない (`just clean` の責務、D-02 共存)。

禁止事項 (verify でチェックされる):
- `cargo build` / `cargo run` / `cargo test` / `cargo fmt` / `cargo clippy` / `cargo clean` — どれも書かない (justfile の役割)
- `macos-` / `darwin-` で始まるターゲット名 — 一切書かない (D-01)
- justfile が持つ recipe 名 (build/run/test/fmt/clippy/clean) をエイリアスとして置かない (D-02)
- recipe body で `cd webif &&` を使わない — `--manifest-path` で解決する (D-03)
- Japanese comments のみ使用 (CLAUDE.md conventions)、識別子は英語のまま

Implementation hints: tab indentation for recipe bodies (Makefile 必須)。`@` プレフィックスで command echoing を抑制してよい (help と echo 系では推奨)。
  </action>
  <verify>
    <automated>test -f Makefile &amp;&amp; make -n help &gt;/dev/null &amp;&amp; make -n linux-amd64 2&gt;&amp;1 | grep -q 'cross build --release --target x86_64-unknown-linux-gnu' &amp;&amp; make -n linux-arm64 2&gt;&amp;1 | grep -q 'cross build --release --target aarch64-unknown-linux-gnu' &amp;&amp; make -n windows-amd64 2&gt;&amp;1 | grep -q 'cross build --release --target x86_64-pc-windows-gnu' &amp;&amp; make -n all 2&gt;&amp;1 | grep -q 'cross build' &amp;&amp; ! grep -E '^(macos|darwin)' Makefile &amp;&amp; ! grep -E '^(build|run|test|fmt|clippy|clean):' Makefile &amp;&amp; grep -q 'check-tools' Makefile &amp;&amp; grep -q 'clean-dist' Makefile &amp;&amp; grep -q 'manifest-path' Makefile</automated>
  </verify>
  <done>
- Makefile が repo root に存在する
- `make -n help`, `make -n linux-amd64`, `make -n linux-arm64`, `make -n windows-amd64`, `make -n all`, `make -n clean-dist`, `make -n check-tools` の全てが dry-run で展開できる (構文エラーなし)
- 3 つの cross-build dry-run 出力に正しい triple が含まれる
- macos-* / darwin レシピが存在しない (D-01)
- justfile 既存 recipe 名 (build/run/test/fmt/clippy/clean) と衝突する recipe が存在しない (D-02)
- `--manifest-path` パターンが使われている (D-03)
- `check-tools` と `clean-dist` recipe が存在する (D-04, scope)
- justfile / ci.yml / webif/src/* / webif/Cargo.toml は touch されていない (`git status` で確認)
  </done>
</task>

<task type="auto">
  <name>Task 2: .github/workflows/release.yml を新規作成 (6 ターゲット matrix、tag/manual トリガ、artifact upload)</name>
  <files>.github/workflows/release.yml</files>
  <action>
`.github/workflows/release.yml` を新規作成する (D-05)。既存 `ci.yml` とは別ファイル、別トリガで併存する (ci.yml は touch 厳禁)。

Workflow の構成要素:

(1) `name: Release`

(2) トリガ (D-05): `on.push.tags: ['v*']` と `on.workflow_dispatch:` の両方を持たせる。

(3) ジョブ `build` (matrix):
  - `name: build (${{ matrix.target }})`
  - `runs-on: ${{ matrix.os }}` (matrix で OS 切替)
  - `strategy.fail-fast: false` (1 ターゲット失敗時に他をキャンセルしない)
  - `strategy.matrix.include` で 6 エントリを明示する (下記の 6 行を 1 エントリずつ書く):
    | os             | target                       | use_cross | bin_name                       |
    |----------------|------------------------------|-----------|--------------------------------|
    | ubuntu-latest  | x86_64-unknown-linux-gnu     | true      | ht-webif-linux-amd64           |
    | ubuntu-latest  | aarch64-unknown-linux-gnu    | true      | ht-webif-linux-arm64           |
    | ubuntu-latest  | x86_64-pc-windows-gnu        | true      | ht-webif-windows-amd64.exe     |
    | windows-latest | aarch64-pc-windows-msvc      | false     | ht-webif-windows-arm64.exe     |
    | macos-latest   | x86_64-apple-darwin          | false     | ht-webif-macos-amd64           |
    | macos-latest   | aarch64-apple-darwin         | false     | ht-webif-macos-arm64           |

(4) ステップ (順):
  - `uses: actions/checkout@v4`
  - **Rust toolchain インストール**: `uses: actions-rust-lang/setup-rust-toolchain@v1` (ci.yml と同じ action) with `toolchain: stable` と `target: ${{ matrix.target }}` (`rustup target add` を兼ねる)
  - **Cache**: `uses: Swatinem/rust-cache@v2` with `workspaces: webif -> webif/target` と `key: ${{ matrix.target }}` (target ごとに cache 分離)
  - **cross インストール** (条件付き `if: matrix.use_cross`): `run: cargo install cross --git https://github.com/cross-rs/cross --locked` (cross-rs README の推奨インストール手順)
  - **Build (cross)** `if: matrix.use_cross` の `run:` に `cross build --release --target ${{ matrix.target }} --manifest-path webif/Cargo.toml` を 1 行で書く
  - **Build (native cargo)** `if: '!matrix.use_cross'` の `run:` に `cargo build --release --target ${{ matrix.target }} --manifest-path webif/Cargo.toml` を 1 行で書く
  - **Stage binary** — Windows/Unix の path 拡張子差を吸収する shell ステップ。`shell: bash` を明示し (windows-latest でも Git Bash で動作)、`mkdir -p dist` → 拡張子判定 (`EXT=""` 既定、target 名に `windows` を含めば `EXT=".exe"`) → `cp "webif/target/${{ matrix.target }}/release/ht-webif$EXT" "dist/${{ matrix.bin_name }}"` の 3 ステップを 1 つの `run: |` ブロックにまとめる。
  - **Upload artifact**: `uses: actions/upload-artifact@v4` with `name: ${{ matrix.bin_name }}`, `path: dist/${{ matrix.bin_name }}`, `if-no-files-found: error`

(5) ci.yml との関係 (D-05):
  - トリガは完全に独立 (ci.yml = push/PR to main、release.yml = tags + manual)
  - `defaults.run.working-directory: webif` を置かない (release.yml は `--manifest-path` で webif を指すため、cross install など repo root で動くべきステップが混在する)
  - 同じ check (fmt/clippy/test) は走らせない (CI の責務、release.yml は build と upload のみ)

(6) コメント: 冒頭に日本語コメントで「release matrix workflow — tag (v*) または手動 dispatch で 6 ターゲット build。ci.yml と別ファイル別トリガ (D-05)。macOS は macos-latest 上 native cargo、windows-arm64 は windows-latest 上 native cargo (cross 公式 image なし)、その他は ubuntu-latest 上 cross。成果物は artifact として残すだけで Release 発行は行わない (CONTEXT scope)。」と説明する。

禁止事項:
- ci.yml の編集 (別ファイル、D-05)
- `actions/create-release` や `softprops/action-gh-release` 等のリリース発行 action (CONTEXT scope: artifact upload までで止める)
- `defaults.run.working-directory: webif` (ci.yml の流儀をコピーしない — このワークフローは repo root と webif/ を行き来する)
- 単一 OS ジョブを matrix の外に置く (全 6 ターゲットを include matrix で揃える)

Implementation note (Claude's Discretion): matrix.include で `use_cross: true|false` を YAML bool として書き、`if:` 式は `if: matrix.use_cross` / `if: '!matrix.use_cross'` の組合せを採用する (`== true` の string/bool 揺れを避ける)。実装時に `python3 -c "import yaml; yaml.safe_load(open(...))"` で parse 検証してから commit する。
  </action>
  <verify>
    <automated>test -f .github/workflows/release.yml &amp;&amp; python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/release.yml')); assert 'Release' in d['name'], 'name'; t=d[True] if True in d else d.get('on'); assert 'tags' in str(t), 'tag trigger'; assert 'workflow_dispatch' in str(t), 'manual trigger'; m=d['jobs']['build']['strategy']['matrix']['include']; assert len(m)==6, f'expected 6 targets, got {len(m)}'; targets={e['target'] for e in m}; expected={'x86_64-unknown-linux-gnu','aarch64-unknown-linux-gnu','x86_64-pc-windows-gnu','aarch64-pc-windows-msvc','x86_64-apple-darwin','aarch64-apple-darwin'}; assert targets==expected, f'targets mismatch: {targets ^ expected}'; print('OK 6 targets')" &amp;&amp; grep -q 'actions/upload-artifact@v4' .github/workflows/release.yml &amp;&amp; grep -q 'cross build --release' .github/workflows/release.yml &amp;&amp; grep -q 'cargo build --release' .github/workflows/release.yml &amp;&amp; grep -q 'manifest-path webif/Cargo.toml' .github/workflows/release.yml &amp;&amp; ! grep -qE 'create-release|action-gh-release' .github/workflows/release.yml &amp;&amp; diff -q .github/workflows/ci.yml &lt;(git show HEAD:.github/workflows/ci.yml)</automated>
  </verify>
  <done>
- `.github/workflows/release.yml` が新規作成され、YAML として parse 可能
- `on:` に `tags: ['v*']` と `workflow_dispatch` の両方
- matrix.include に 6 ターゲットすべて (linux-amd64/arm64、windows-amd64/arm64、macos-amd64/arm64) が triple ベースで含まれる
- `cross build` と `cargo build` の両方の build ステップが存在 (cross 用 / native 用の使い分け)
- 全 6 エントリで `--manifest-path webif/Cargo.toml` を渡している
- `actions/upload-artifact@v4` で 6 ターゲット分の binary を upload
- Release 発行 (create-release/action-gh-release) を行わない (scope)
- `ci.yml` は git HEAD と完全一致 (touch 厳禁)
- justfile / webif/src/* / webif/Cargo.toml / Makefile (Task 1 で確定) は touch されていない
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Local Makefile smoke + workflow YAML lint の人手確認</name>
  <files>Makefile, .github/workflows/release.yml</files>
  <action>
Task 1 と Task 2 で作成した 2 ファイル (`Makefile` と `.github/workflows/release.yml`) に対し、人手で smoke check と保護対象ファイルの不変性を確認する checkpoint。Claude は実装作業を一切行わない — 既に作成済みのファイルに対する人手検証だけを案内する。

<what-built>
- リポジトリ直下の `Makefile` (cross 経由 linux-amd64/arm64 + windows-amd64 の 3 cross-stable target、check-tools、help、clean-dist、all)
- `.github/workflows/release.yml` (6 ターゲット matrix、tag/manual トリガ、artifact upload、ci.yml は不変)
</what-built>

<how-to-verify>
**A. Makefile help と dry-run (約 30 秒)**
1. リポジトリ直下で `make help` を実行し、3 つのクロスビルドターゲット名と `make all` / `make clean-dist` / `make check-tools` および「macOS と windows-arm64 は CI で生成」「dev は just を使う」の注記が表示されることを目視確認する。
2. `make -n all` を実行し、3 つの cross-build コマンドが期待した triple (x86_64-unknown-linux-gnu、aarch64-unknown-linux-gnu、x86_64-pc-windows-gnu) で展開されることを目視確認する。
3. `! grep -E '^(macos|darwin|build|run|test|fmt|clippy):' Makefile && echo OK` で macOS と justfile 衝突 recipe の不在を再確認する (Task 1 の automated verify と重なるが human safety net)。

**B. (任意・docker 環境がある場合のみ) 実際のクロスビルド (約 3-5 分)**
- `make check-tools` を実行し、docker と cross の有無を判定する。両方インストール済みなら次に進む。未インストールなら hint message が出ることだけ確認して B-2 をスキップする。
- `make linux-amd64` を実行し、`dist/ht-webif-linux-amd64` が生成され、`file dist/ht-webif-linux-amd64` が `ELF 64-bit LSB executable, x86-64` を含むことを確認する。

**C. release.yml の lint と matrix 完全性 (約 30 秒)**
1. `python3 -c "import yaml; print(len(yaml.safe_load(open('.github/workflows/release.yml'))['jobs']['build']['strategy']['matrix']['include']))"` が `6` を返すことを確認する。
2. (任意・push 権限がある場合) リモートに push 後、`gh workflow list` に `Release` が現れる、または `gh workflow view release.yml` でエラーが出ないことを確認する。リモート push 権限が無い場合は本ステップをスキップ可。
3. `diff <(git show HEAD:.github/workflows/ci.yml) .github/workflows/ci.yml` が空であることを再確認する (ci.yml 不変)。

**D. 変更ファイルの最終確認 (約 10 秒)**
- `git status --porcelain | grep -E '\.(rs|toml)$|^.M justfile|^.M .github/workflows/ci.yml'` が空であることを確認する (Rust source、Cargo.toml、justfile、ci.yml は touch 厳禁)。期待される変更は `Makefile` (新規) と `.github/workflows/release.yml` (新規) の 2 ファイルのみ。

**判定:** A1/A2/A3 が PASS、C1/C3 が PASS、D が PASS で全体 PASS。B はインフラ前提のあるオプション (docker 無くても可)、C2 は権限前提のあるオプション。
</how-to-verify>

<resume-signal>
- 全て PASS なら `approved` と入力
- B (docker 無し) や C2 (push 権限無し) をスキップしたなら `approved (skipped: B / C2)` のように skip 内容を明示
- 何か異常があれば内容を記述
</resume-signal>
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify — automated verify N/A (Task 1/2 automated checks already cover file content; this task gates only on user signal)"</automated>
  </verify>
  <done>
- 上記 how-to-verify の A1/A2/A3、C1/C3、D を実行し PASS
- B (docker) と C2 (push 権限) はインフラ依存のためオプション扱い、skip は明記して OK
- 異常があれば修正してから再度 checkpoint
  </done>
</task>

</tasks>

<verification>
**プラン全体の最終チェック (実行時に確認するスクリプト):**

- 新規 2 ファイルが存在: `test -f Makefile && test -f .github/workflows/release.yml`
- CONTEXT で touch 禁止のファイルが unchanged: `git diff --name-only HEAD | grep -vE '^(Makefile|\.github/workflows/release\.yml)$'` が空であること
- justfile recipe 名と Makefile target 名が衝突しない (D-02): `comm -12 <(grep -E '^[a-z][a-z-]*:' justfile | cut -d: -f1 | sort -u) <(grep -E '^[a-z][a-z-]*:' Makefile | cut -d: -f1 | sort -u)` が空であること
- Makefile に macos-* / darwin-* がない (D-01): `! grep -E '^(macos|darwin)' Makefile`
- release.yml の matrix が 6 件: `python3 -c "import yaml; print(len(yaml.safe_load(open('.github/workflows/release.yml'))['jobs']['build']['strategy']['matrix']['include']))"` が `6`

**Must-haves 再確認:** must_haves.truths の 13 項目が全て満たされること。特に:
- "macOS handled by CI matrix only" — Makefile に macos-* 不在
- "justfile is untouched" + "ci.yml is untouched" + "webif/* untouched"
- "windows-arm64 built natively on windows-latest" — release.yml matrix で確認 (Task 2 automated verify)
</verification>

<success_criteria>
- Makefile が repo 直下に存在し、`make help` で 3 ターゲット (linux-amd64/arm64、windows-amd64) + ユーティリティ (check-tools、all、clean-dist) を案内する
- `make -n <target>` で 3 ターゲット全てが `cross build --release --target <correct-triple> --manifest-path webif/Cargo.toml` に展開される
- Makefile に macOS recipe が無い (D-01)、justfile と recipe 名が衝突しない (D-02)
- `.github/workflows/release.yml` が新規作成され、6 ターゲット matrix を持ち、tag (`v*`) push と manual dispatch の両方でトリガされ、各ターゲットの binary を artifact として upload する
- ci.yml / justfile / webif/src/* / webif/Cargo.toml が全て unchanged
- Task 3 human-verify で `approved` (または skip 明記付き approved)
</success_criteria>

<output>
After completion, create `.planning/quick/260526-b7m-os-makefile-cross-linux-windows-amd64-ar/260526-b7m-SUMMARY.md` recording:
- 新規作成した 2 ファイルの最終的な path
- Makefile の最終 recipe 一覧 (実際に書き出した名前)
- release.yml の matrix 6 ターゲット (target triple + os + use_cross の 3 列表)
- windows-arm64 を Makefile から外し CI 専属とした planner 判断の根拠 (cross 公式 image 無し、windows-latest + native cargo + rustup target で代替) — 将来 cross が windows-arm64 を公式サポートしたら Makefile に昇格可能、と meeting note 残す
- 触らなかった保護対象ファイル一覧 (justfile / ci.yml / webif/src/* / webif/Cargo.toml)
- 既知の運用上の注意: docker daemon が動いていないと `make check-tools` 段階で fail-fast すること、`cargo install cross --locked` を初回に実行する必要があること
</output>
