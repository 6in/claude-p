---
phase: 260527-00i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webif/src/config.rs
  - webif/src/main.rs
  - webif/.env.example
  - webif/README.md
  - webif/CLAUDE.md
autonomous: true
requirements:
  - QUICK-CONFIG-PORT
  - QUICK-CONFIG-TURNS-DIR
---

<objective>
`PORT` と `TURNS_DIR` を環境変数で設定可能にする（多重インスタンス運用 Phase 1）。

Purpose: 同一ホスト上で複数 WebIF を起動できるようにする土台を作る。`PORT` と `TURNS_DIR` が衝突しなければ並列起動可能になる。Phase 2（`TURNS_DIR` を `turns-${PORT}/` に自動既定）は別タスクとして切り出し済み、本プランの範囲外。

Output:
- `webif/src/config.rs` に `load_port()` / `load_turns_dir()` を追加（`anyhow::Result<T>` で統一）
- `webif/src/main.rs:23` と `:40` のハードコード値を読み込み関数経由に差し替え
- `.env.example`・`README.md`・`CLAUDE.md` の追従更新

優先順位は dotenvy 標準どおり **実環境変数 > .env > 既定値**。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@webif/CLAUDE.md
@webif/src/config.rs
@webif/src/main.rs
@webif/.env.example
@webif/README.md

<interfaces>
<!-- 既存の load_ht_mcp_path のシグネチャを揃える参考 -->

From webif/src/config.rs (現状):
```rust
pub const TURN_TIMEOUT: Duration = Duration::from_secs(300);
pub const MCP_TIMEOUT: Duration = Duration::from_secs(30);

/// HT_MCP_PATH 環境変数を読む。存在しない場合は "ht-mcp" を既定値とする。
pub fn load_ht_mcp_path() -> String {
    std::env::var("HT_MCP_PATH").unwrap_or_else(|_| "ht-mcp".to_string())
}
```

From webif/src/main.rs (現状の該当行):
- L6:  `use ht_webif::config::load_ht_mcp_path;`
- L23: `let turns_dir = std::env::current_dir()?.join("turns");`
- L40: `let addr = "127.0.0.1:8080";`
- L41: `let listener = tokio::net::TcpListener::bind(addr).await?;`
- L42: `println!("WebIF 起動: http://{addr}");`

新規追加するシグネチャ（決定: option (a) — 両方 anyhow::Result<T> で統一）:
```rust
pub fn load_port() -> anyhow::Result<u16>;
pub fn load_turns_dir() -> anyhow::Result<std::path::PathBuf>;
```

決定理由: `?` で main から伝播でき、`current_dir()` 失敗時の panic も排除できる。
既存の `load_ht_mcp_path() -> String`（fail-safe な既定値があるので Result 不要）と semantics が違うのは妥当。
</interfaces>

<conventions>
@webif/CLAUDE.md の Conventions セクション準拠:
- `//` `//!` `///` コメントと `anyhow!` / `with_context` メッセージは日本語
- 識別子・シグネチャは英語
- `?` で propagation、`.with_context(|| format!("..."))` で context 付与
- エラー文脈には値を入れる（例: `format!("PORT のパースに失敗 (値: {s:?})")`）
- `cargo fmt` の rustfmt 既定を尊重（4 space, trailing commas）
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.rs に load_port / load_turns_dir を追加し main.rs を差し替え</name>
  <files>webif/src/config.rs, webif/src/main.rs</files>
  <action>
1. `webif/src/config.rs` の末尾に 2 つの関数を追加する。既存 `load_ht_mcp_path` と同じ doc-comment スタイル（`///` 日本語）に揃える。

   - `pub fn load_port() -> anyhow::Result<u16>`:
     - `std::env::var("PORT")` を `match` で受ける
     - `Ok(s)` の場合: `s.parse::<u16>()` を `.with_context(|| format!("PORT のパースに失敗 (値: {s:?})"))` で包んで返す
     - `Err(_)` の場合: `Ok(8080)`
     - `use anyhow::Context;` をファイル先頭に追加（`with_context` 用）

   - `pub fn load_turns_dir() -> anyhow::Result<std::path::PathBuf>`:
     - `std::env::var("TURNS_DIR")` が `Ok(s)` なら `Ok(std::path::PathBuf::from(s))`
     - `Err(_)` なら `Ok(std::env::current_dir().with_context(|| "current_dir 取得失敗")?.join("turns"))`
     - これで main.rs:23 の `std::env::current_dir()?.join("turns")` と完全に等価な既定動作になる（後方互換）

   - doc-comment は日本語で「既定値」「優先順位は実環境変数 > .env > 既定値」「相対パスは CWD 起点」等を明記する

2. `webif/src/main.rs` の import を更新:
   - L6 を `use ht_webif::config::{load_ht_mcp_path, load_port, load_turns_dir};` に変更

3. `webif/src/main.rs:23` を差し替え:
   - 旧: `let turns_dir = std::env::current_dir()?.join("turns");`
   - 新: `let turns_dir = load_turns_dir()?;`

4. `webif/src/main.rs:40-42` を差し替え:
   - 旧:
     ```
     let addr = "127.0.0.1:8080";
     let listener = tokio::net::TcpListener::bind(addr).await?;
     println!("WebIF 起動: http://{addr}");
     ```
   - 新:
     ```
     let port = load_port()?;
     let addr = format!("127.0.0.1:{port}");
     let listener = tokio::net::TcpListener::bind(&addr).await?;
     println!("WebIF 起動: http://{addr}");
     ```
   - `bind` は `ToSocketAddrs` を取り `&String` も `&str` に deref で渡せるので `bind(&addr)` で問題ない
   - `println!("WebIF 起動: http://{addr}")` は `addr` が `String` でも `Display` 経由で同じ出力になる

5. `cargo fmt` を走らせて整形する。

6. ビルド・テスト・lint をすべて green にする:
   - `cargo fmt --check`
   - `cargo clippy --all-targets --release -- -D warnings`
   - `cargo test --release` （既存テストは `http.rs:194` の `build_test_state(turns_dir: PathBuf)` 経由で turns_dir を引数化済みなので影響なし）
   - `cargo build --release`

設計判断:
- ユニットテスト追加は見送る。`std::env::set_var` を使うテストは並列実行で他テストと干渉し、`serial_test` 等の dev-dep を増やす価値が薄い。代わりに既存の `webif/scripts/smoke.sh`（既定値で 8080/`turns/` を使う end-to-end 検証）で後方互換性を保証する。
- 単体テストは将来 `serial_test` を導入する余地を残しつつ、本プランでは追加しない（quick scope を尊重）。

Important constraints (per webif/CLAUDE.md):
- コメントとエラーメッセージは日本語
- 識別子は英語
- `anyhow` + `?` + `with_context`、`unwrap()` や `expect()` は使わない
- 既存 `load_ht_mcp_path` の doc-comment 形式（`/// HT_MCP_PATH 環境変数を読む。存在しない場合は "..." を既定値とする。`）に揃える
  </action>
  <verify>
    <automated>cd webif && cargo fmt --check && cargo clippy --all-targets --release -- -D warnings && cargo test --release && cargo build --release</automated>
    Additional manual checks:
    - `grep -n "pub fn load_port" webif/src/config.rs` returns 1 hit
    - `grep -n "pub fn load_turns_dir" webif/src/config.rs` returns 1 hit
    - `grep -n "load_turns_dir()?" webif/src/main.rs` returns 1 hit
    - `grep -n "load_port()?" webif/src/main.rs` returns 1 hit
    - `grep -n "127.0.0.1:8080" webif/src/main.rs` returns 0 hits（ハードコードが消えていること）
    - Default-value smoke: `cd webif && ./scripts/smoke.sh` succeeds (binary listens on 8080, writes to `./turns/`)
    - Multi-instance smoke: `cd webif && PORT=8081 TURNS_DIR=$(mktemp -d) cargo run --release` をバックグラウンドで起動 → `curl -sf -o /dev/null -w "%{http_code}" http://127.0.0.1:8081/turns/00000000-000000-000` が `4xx`（`000` の接続失敗ではない）→ プロセスを kill
  </verify>
  <done>
    - `cargo fmt --check` / `cargo clippy --release -D warnings` / `cargo test --release` がすべて pass
    - 既定値 (`PORT` 未設定 / `TURNS_DIR` 未設定) で main.rs の挙動が現状と完全一致（`127.0.0.1:8080`、CWD/turns に書き込む）
    - `PORT=8081 TURNS_DIR=/tmp/xxx` 起動で 8081 にバインドし、/tmp/xxx に turn 成果物が出る
    - 旧ハードコード `"127.0.0.1:8080"` と `current_dir()?.join("turns")` が main.rs から消えている
  </done>
</task>

<task type="auto">
  <name>Task 2: .env.example / README.md / CLAUDE.md の追従更新</name>
  <files>webif/.env.example, webif/README.md, webif/CLAUDE.md</files>
  <action>
1. `webif/.env.example` を以下の内容に書き換える。既存の `HT_MCP_PATH=ht-mcp` は残し、その下に新しい 2 ブロックを追記する。`PORT` と `TURNS_DIR` は `# PORT=8080` のようにコメントアウト状態で記載し、オプトイン方式とする（既定値で動かしたい人が `.env` をコピーしただけで挙動が変わらないようにする）。

   最終的な `.env.example` の中身:
   ```
   # ht-webif 設定サンプル。コピーして .env にして使ってください。
   #
   # ht-mcp 実行ファイルのパス。
   # 未設定なら既定値 "ht-mcp"（PATH 上を探索）。
   # 優先順位: 実環境変数 HT_MCP_PATH > .env の値 > 既定値
   HT_MCP_PATH=ht-mcp
   #
   # HTTP listener ポート番号。未設定なら既定値 8080。
   # 優先順位: 実環境変数 PORT > .env の値 > 既定値
   # PORT=8080
   #
   # ターン成果物 (prompt/result/status) の出力ディレクトリ。未設定なら CWD/turns。
   # 複数インスタンス運用時は PORT と TURNS_DIR を変えて起動すること。
   # 優先順位: 実環境変数 TURNS_DIR > .env の値 > 既定値
   # TURNS_DIR=./turns
   ```

2. `webif/README.md` を 2 箇所更新する:

   (a) 「## 起動」セクション内に「### 複数インスタンス運用」サブセクションを追加する。挿入位置は L69 の「外部から接続したい場合は SSH トンネル...」の直後（「## 動作確認」見出しの直前）が自然。

   追加内容:
   ```markdown
   ### 複数インスタンス運用

   同一ホストで複数の WebIF を走らせる場合は `PORT` と `TURNS_DIR` を分けて起動する:

   ```bash
   PORT=8081 TURNS_DIR=./turns-8081 cargo run --release
   PORT=8082 TURNS_DIR=./turns-8082 cargo run --release
   ```

   既定値はそれぞれ `8080` と `./turns`。`.env` でも設定可能（`.env.example` を参照）。
   ```

   (b) 「## 環境変数」セクション（L193 付近）のテーブルに 2 行追加:

   旧:
   ```
   | 変数 | 既定値 | 説明 |
   |------|--------|------|
   | `HT_MCP_PATH` | `ht-mcp` | `ht-mcp` バイナリのパス。未設定なら PATH 上を探索 |
   ```

   新:
   ```
   | 変数 | 既定値 | 説明 |
   |------|--------|------|
   | `HT_MCP_PATH` | `ht-mcp` | `ht-mcp` バイナリのパス。未設定なら PATH 上を探索 |
   | `PORT` | `8080` | HTTP listener のポート番号。多重起動時はインスタンス毎に変える |
   | `TURNS_DIR` | `./turns` | ターン成果物の出力先ディレクトリ。多重起動時は PORT と一緒に分離する |
   ```

3. `webif/CLAUDE.md` の「## Constraints」セクション（L17 付近）の `**Single host**` 箇条書きを更新する:

   旧:
   ```
   - **Single host**: 現状は WebIF と ht-mcp と claude が同一ホスト＝同一ファイルシステム。HT-PROTOCOL §7 はこの前提のもとで成立している。
   ```

   新:
   ```
   - **Single host**: WebIF と ht-mcp と claude は同一ホスト＝同一ファイルシステムを共有する前提。同一ホスト上の多重インスタンス運用は `PORT` と `TURNS_DIR` を変えれば可能（v0.1 段階1で env 化済み）。HT-PROTOCOL §7 はこの前提のもとで成立している。
   ```

   既存の HT-PROTOCOL §7 への参照は新文末に残すこと（情報の loss を防ぐ）。

Important:
- README 内のコードフェンスはネストするので、外側 4 backtick / 内側 3 backtick の構造を維持する必要がある場合は注意（実際は外側マークダウン block ではなく直接挿入なので 3 backtick で OK）。
- 日本語の表記揺れに注意（「環境変数」「インスタンス」「既定値」を統一）。
- ファイル全体の文字エンコーディングは UTF-8 のまま。改行は LF。
  </action>
  <verify>
    <automated>grep -n "^# PORT=8080" webif/.env.example && grep -n "^# TURNS_DIR=" webif/.env.example && grep -n "複数インスタンス運用" webif/README.md && grep -n "| \`PORT\` |" webif/README.md && grep -n "| \`TURNS_DIR\` |" webif/README.md && grep -n "PORT と TURNS_DIR を変えれば可能" webif/CLAUDE.md</automated>
    Additional checks:
    - `webif/.env.example` の既存行 `HT_MCP_PATH=ht-mcp` がコメントアウトされずに残っていること: `grep -n "^HT_MCP_PATH=ht-mcp$" webif/.env.example`
    - README の「## 起動」セクションが破損していないこと（目視で見出し構造を確認: `grep -n "^##" webif/README.md` で順序が変わっていない）
    - CLAUDE.md の「HT-PROTOCOL §7 はこの前提のもとで成立している」が残っていること: `grep -n "HT-PROTOCOL §7" webif/CLAUDE.md` で 1 hit
  </verify>
  <done>
    - `.env.example` に `# PORT=8080` と `# TURNS_DIR=./turns`（およびそれぞれのコメント）が追加されている
    - README.md に「### 複数インスタンス運用」サブセクションが追加され、環境変数テーブルに 2 行追加されている
    - CLAUDE.md の Single host 制約に多重インスタンス可能の旨が追記され、HT-PROTOCOL §7 参照も保持されている
    - 全ファイル UTF-8 / LF
  </done>
</task>

</tasks>

<verification>
全タスク完了後の総合検証:

1. ビルド・lint・テストの green:
   - `cd webif && cargo fmt --check`
   - `cd webif && cargo clippy --all-targets --release -- -D warnings`
   - `cd webif && cargo test --release`
   - `cd webif && cargo build --release`

2. 後方互換性（既定値時の挙動が現状と一致）:
   - `cd webif && ./scripts/smoke.sh` が成功（8080 / ./turns で動く）

3. 新機能（多重インスタンス）の最小確認:
   ```bash
   cd webif
   PORT=8081 TURNS_DIR=$(mktemp -d) cargo run --release &
   SERVER_PID=$!
   sleep 5
   CODE=$(curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8081/turns/00000000-000000-000)
   kill $SERVER_PID 2>/dev/null
   wait $SERVER_PID 2>/dev/null
   # CODE は 404 等の 4xx であること（000 は接続失敗）
   echo "HTTP code: $CODE"
   ```

4. ドキュメント整合:
   - `grep -c "PORT" webif/.env.example` ≥ 2
   - `grep -c "TURNS_DIR" webif/.env.example` ≥ 2
   - `grep -c "PORT" webif/README.md` ≥ 1
   - `grep -c "TURNS_DIR" webif/README.md` ≥ 1
   - `grep -c "TURNS_DIR" webif/CLAUDE.md` ≥ 1
</verification>

<success_criteria>
- `PORT` 未設定で 8080 にバインド（後方互換）
- `TURNS_DIR` 未設定で CWD/turns に書き込む（後方互換）
- `PORT=8081 TURNS_DIR=/tmp/xxx cargo run` で 8081 にバインドし /tmp/xxx に成果物が出る
- パース失敗 (例: `PORT=abc`) は panic ではなく `anyhow` エラーで起動失敗（main から `?` 伝播）
- 優先順位 **実環境変数 > .env > 既定値** が dotenvy 標準どおり機能する
- ドキュメント 3 ファイル（.env.example / README.md / CLAUDE.md）が同じ事実を矛盾なく記述している
- `cargo fmt` / `cargo clippy -D warnings` / `cargo test` / `cargo build` がすべて green
</success_criteria>

<output>
After completion:
1. Stage and commit Task 1 と Task 2 を別コミットに分ける:
   - コミット 1 (Task 1 完了時): `feat(config): make PORT and TURNS_DIR configurable via env`
     - staged: `webif/src/config.rs`, `webif/src/main.rs`
   - コミット 2 (Task 2 完了時): `docs: document PORT/TURNS_DIR env vars and multi-instance usage`
     - staged: `webif/.env.example`, `webif/README.md`, `webif/CLAUDE.md`

2. 各コミットメッセージは HEREDOC で渡し、フッターに以下を含める:
   ```
   Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
   ```

3. 完了後にサマリーをこの quick ディレクトリに残す:
   `.planning/quick/260527-00i-make-port-and-turns-dir-configurable/260527-00i-SUMMARY.md`

   内容には以下を含める:
   - 変更ファイル一覧
   - 後方互換性の確認結果（smoke.sh 成功 / HTTP 8081 マルチインスタンス確認）
   - Phase 2（`TURNS_DIR` を `turns-${PORT}/` 自動既定）への申し送り
</output>
