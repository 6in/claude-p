---
phase: 260528-iav
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - webif/docs/STACK.md
  - webif/docs/CONVENTIONS.md
  - webif/docs/ARCHITECTURE.md
  - webif/CLAUDE.md
autonomous: true
requirements:
  - QUICK-260528-iav
must_haves:
  truths:
    - "webif/docs/STACK.md exists with the full technology-stack reference extracted from the current CLAUDE.md ## Technology Stack block"
    - "webif/docs/CONVENTIONS.md exists with the full coding-conventions reference extracted from the current CLAUDE.md ## Conventions block"
    - "webif/docs/ARCHITECTURE.md exists with the full architecture reference extracted from the current CLAUDE.md ## Architecture block"
    - "webif/CLAUDE.md still contains every GSD marker pair (project/stack/conventions/architecture/skills/workflow/profile) with their start/end comments intact and in the original order"
    - "Inside each GSD marker block (except project, workflow, profile, skills — which are short and preserved verbatim), the body is collapsed to a TL;DR of 5–12 bullets / 1–3 short paragraphs and ends with a 詳細: link to webif/docs/<file>.md"
    - "All dead artefacts (empty headers like ## System Overview with empty code fence, ## Language and Edition, ## Import Organization, ## Configuration, ## Module Design, the orphan ### Data Flow subheadings, ## Cross-Cutting Concerns trailing blank header, duplicated bullet groups in stack/conventions/architecture) are removed from CLAUDE.md"
    - "Project section (lines 2–17), Constraints subsection, GSD Workflow Enforcement block, Developer Profile block, and Project Skills block are byte-identical to the originals"
    - "No content duplicated from HT-PROTOCOL.md; CLAUDE.md and the new docs/ files reference it by link where the wire protocol is involved"
  artifacts:
    - path: "webif/docs/STACK.md"
      provides: "Full technology stack reference (languages, runtime, frameworks, key deps, configuration, platform requirements)"
      min_lines: 40
    - path: "webif/docs/CONVENTIONS.md"
      provides: "Full coding conventions (naming, language for comments, doc comments, code style, error handling, async/concurrency, HTTP handlers, serde, logging, function/module design)"
      min_lines: 60
    - path: "webif/docs/ARCHITECTURE.md"
      provides: "Full architecture reference (component responsibilities, pattern overview, layers, data flow, key abstractions, entry points, architectural constraints, anti-patterns, error handling)"
      min_lines: 80
    - path: "webif/CLAUDE.md"
      provides: "Slim TL;DR CLAUDE.md with GSD scaffolding preserved and links into webif/docs/"
      contains: "<!-- GSD:project-start"
  key_links:
    - from: "webif/CLAUDE.md (stack block)"
      to: "webif/docs/STACK.md"
      via: "詳細: [docs/STACK.md](docs/STACK.md)"
      pattern: "docs/STACK\\.md"
    - from: "webif/CLAUDE.md (conventions block)"
      to: "webif/docs/CONVENTIONS.md"
      via: "詳細: [docs/CONVENTIONS.md](docs/CONVENTIONS.md)"
      pattern: "docs/CONVENTIONS\\.md"
    - from: "webif/CLAUDE.md (architecture block)"
      to: "webif/docs/ARCHITECTURE.md"
      via: "詳細: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)"
      pattern: "docs/ARCHITECTURE\\.md"
---

<objective>
Refactor `webif/CLAUDE.md` (currently 289 lines, auto-generated by GSD with stale/bloated content) into a slim TL;DR that an LLM coding agent can absorb in seconds. Preserve the GSD marker scaffolding (`<!-- GSD:xxx-start ... -->` / `<!-- GSD:xxx-end -->`) so future GSD regenerations still have a place to write. Move the verbose reference material out to `webif/docs/STACK.md`, `webif/docs/CONVENTIONS.md`, and `webif/docs/ARCHITECTURE.md`, and link to them from CLAUDE.md.

Purpose: keep CLAUDE.md as a fast, accurate context primer (the wedge that opens a session) while making the deep reference still discoverable on demand.

Output: 3 new docs files + a rewritten CLAUDE.md, all linked together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Source content to extract from (read once each, then close out — do not re-read the same range):
@webif/CLAUDE.md
@webif/HT-PROTOCOL.md
@webif/README.md

Existing doc style reference (note tone/structure; do NOT duplicate content):
@webif/docs/blogs.md

State / project signal:
@.planning/STATE.md

## Source-block ranges in current webif/CLAUDE.md (1-indexed, from the read above)

- GSD:project-start..end  → lines 1–18    (KEEP VERBATIM in new CLAUDE.md)
- GSD:stack-start..end    → lines 20–65   (compress to TL;DR; move detail to docs/STACK.md)
- GSD:conventions-start..end → lines 67–149 (compress to TL;DR; move detail to docs/CONVENTIONS.md)
- GSD:architecture-start..end → lines 151–261 (compress to TL;DR; move detail to docs/ARCHITECTURE.md)
- GSD:skills-start..end   → lines 263–267 (KEEP VERBATIM — already short)
- GSD:workflow-start..end → lines 269–280 (KEEP VERBATIM)
- GSD:profile-start..end  → lines 284–289 (KEEP VERBATIM)

## Dead artefacts to drop from CLAUDE.md (and NOT carry into docs/)

- `## System Overview` followed by empty ```text```/``` fence (lines 154–156)
- Bare header `## Language and Edition` with no body (line 70)
- Bare header `## Import Organization` with no body (line 101)
- Bare header `## Configuration` with no body (line 133)  — note: there is a real `## Configuration` body in the stack block at lines 49–57; keep that in docs/STACK.md
- Bare header `## Module Design` with no body (line 148)
- `## Data Flow` subheadings with no bodies: `### Primary Request Path`, `### Synchronous Path`, `### Polling Path`, `### Raw Command Path`, `### Restart Path` (lines 197–202) — these are orphan headings. In docs/ARCHITECTURE.md, replace with a single short prose description per path, OR drop them and rely on HT-PROTOCOL.md §6 + the `src/main.rs` line refs already in the Component Responsibilities table.
- Trailing bare `## Cross-Cutting Concerns` with no body (line 260)
- Anti-Patterns section currently has only `###` headers with no bodies (lines 246–251) — in docs/ARCHITECTURE.md, give each a one-sentence "why it's wrong" gloss (HT-PROTOCOL.md already explains several; link).
- Duplicate / mis-grouped bullets inside Frameworks (lines 36–37 say "None. No tests/…" and "cargo + just" — these belong under Build Tools / Testing, not Frameworks). Re-shelf in docs/STACK.md.
- `## Serde Patterns` with only a `#[derive(Deserialize)]` fragment (lines 131–132) — flesh out to one short bullet in docs/CONVENTIONS.md or drop.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create webif/docs/STACK.md (full stack reference)</name>
  <files>webif/docs/STACK.md</files>
  <action>
Write `webif/docs/STACK.md` as the canonical Technology Stack reference. Source the content from `webif/CLAUDE.md` lines 20–65 (the current GSD:stack block). Structure with these top-level sections (use `#` for the doc title and `##` for sections):

- Title: `# Technology Stack (ht-webif)`
- `## Languages` — keep the 3 bullets (Rust edition 2021, Markdown for docs only, JSON wire/turn-files).
- `## Runtime` — Tokio (`#[tokio::main]`, `full` features), listen address `127.0.0.1:8080` (default — note this is overridable via `PORT` per `260527-00i` quick task), Cargo, lockfile committed.
- `## Frameworks` — `axum` 0.8 (routes `/prompt`, `/turns/{turn_id}`, `/command`, `/restart`), `tokio` 1 full (mpsc + Mutex), `hyper`/`tower` transitive.
- `## Build & Test Tooling` — `cargo` + `just` (`webif/justfile`); CI at repo root `.github/workflows/`. Move the "None. No `tests/`…" bullet here and update it (Phase 03 added unit tests via `cargo test --release`, see STATE.md decisions 03-01..03-06).
- `## Key Dependencies` — verbatim the 9 bullets from current lines 39–48 (tokio, axum, serde, serde_json, anyhow, chrono, dotenvy, hyper/tower/http, mio/socket2, tracing). Add `tower-http` for CORS (from quick task 260527-lb8 / commit c59b41b).
- `## Configuration` — `.env` loaded by `dotenvy::dotenv()` at startup; precedence `env > .env > default`; cwd must be `webif/`. List env vars: `PORT`, `TURNS_DIR`, `HT_MCP_PATH`, `CORS_ORIGINS` (last two from quick tasks 260527-00i and 260527-lb8). Note `.env`/`.env.example` location.
- `## Platform Requirements` — Rust edition 2021 stable; `ht-mcp` binary on `PATH` or via `HT_MCP_PATH`; `claude` CLI on `PATH`; POSIX-like OS (atomic rename for result/status files per HT-PROTOCOL §6); loopback bind so remote access needs reverse proxy or SSH tunnel; no Dockerfile / systemd / Procfile.

When citing the wire protocol, link to `../HT-PROTOCOL.md` rather than re-explaining. When citing source lines, keep the `src/main.rs:NNN` references — they are load-bearing for navigation.

Do NOT include conventions, architecture, anti-patterns, or data-flow content here — that belongs in CONVENTIONS.md / ARCHITECTURE.md. This file is stack-only.

End the file with a one-line back-link: `戻る: [../CLAUDE.md](../CLAUDE.md)`.
  </action>
  <verify>
    <automated>test -f webif/docs/STACK.md && grep -q '^# Technology Stack' webif/docs/STACK.md && grep -q '## Key Dependencies' webif/docs/STACK.md && grep -q 'HT_MCP_PATH' webif/docs/STACK.md && grep -q 'CORS_ORIGINS' webif/docs/STACK.md && [ "$(wc -l < webif/docs/STACK.md)" -ge 40 ]</automated>
  </verify>
  <done>webif/docs/STACK.md exists, ≥40 lines, has the 7 required sections, mentions PORT/TURNS_DIR/HT_MCP_PATH/CORS_ORIGINS, links back to ../CLAUDE.md and over to ../HT-PROTOCOL.md.</done>
</task>

<task type="auto">
  <name>Task 2: Create webif/docs/CONVENTIONS.md (full conventions reference)</name>
  <files>webif/docs/CONVENTIONS.md</files>
  <action>
Write `webif/docs/CONVENTIONS.md` as the canonical Coding Conventions reference. Source content from `webif/CLAUDE.md` lines 67–149 (the current GSD:conventions block). Structure:

- Title: `# Coding Conventions (ht-webif)`
- `## Naming` — combine current "Naming Patterns" content. File names (snake_case Rust source, UPPER-KEBAB docs), turn-file prefixes (`prompt-`/`result-`/`status-` per HT-PROTOCOL §4), function/variable names (`snake_case` async by default, short verb names), struct names (`PascalCase`, DTO suffix `Req`/`Resp`), constants (`SCREAMING_SNAKE_CASE` with explicit type), routes (single-segment lowercase nouns, snake_case path params), runtime dir `./turns/` (now overridable via `TURNS_DIR`).
- `## Language for Comments and Logs` — English identifiers, Japanese for `//` / `//!` / `///` comments, `println!` / `eprintln!` text, `anyhow!` messages.
- `## Documentation Comments` — file-level `//!` doc at top of `lib.rs` (post Phase 02 module refactor — was `main.rs` before, see STATE.md decision 02-04), `///` on non-trivial fns, `//` explains why not what.
- `## Code Style` — `cargo fmt` defaults (no `rustfmt.toml`); run `cargo fmt` before commits; `cargo clippy` ad-hoc; ASCII section banners as visual separators; module split now lives in `src/mcp.rs` / `src/worker.rs` / `src/turn.rs` / `src/http.rs` per Phase 02 refactor (STATE.md decisions 02-02/02-03/02-04).
- `## Error Handling` — `?` for propagation; `.with_context(...)` for OS errors; `.ok_or_else(|| anyhow!(...))` for `Option`; best-effort cleanup (`let _ = ...`); snapshot-failure fallback strings; handlers return `Result<Json<T>, (StatusCode, String)>`; `fn ise<E: Display>` helper → 500; explicit `StatusCode::BAD_REQUEST` / `GATEWAY_TIMEOUT` / `NOT_FOUND` for non-500.
- `## Async / Concurrency` — `Arc<Mutex<Worker>>` shared by `AppState` and `worker_loop`; `tokio::sync::Mutex` (held across `.await`); lock acquired for full handler body (serialized worker is intentional, HT-PROTOCOL §12); `mpsc::channel::<Job>(64)` fan-in; closed channel → 500; `tokio::time::timeout(MCP_TIMEOUT, ...)`; polling loops use `Instant::now() + Duration` + `tokio::time::sleep`. Quote the four magic timing constants (700ms boot wait, 1s turn-complete poll, 500ms post-keystroke settle, 1500ms post-command settle) with `src/main.rs` line refs.
- `## HTTP Handler Conventions (axum 0.8)` — Defensive `turn_id` whitelist `^[0-9-]+$` (now also covered by Phase 03 unit test `path_traversal_*`, per STATE.md decision 03-04). New handlers accepting FS-path identifiers MUST apply equivalent whitelist.
- `## Serde Patterns` — `#[derive(Deserialize)]` on request DTOs, `#[derive(Serialize)]` on response DTOs, derive-only (no custom impls today).
- `## Logging` — bracketed source tags `[restart]` / `[worker]` / `[shared-fate]`; startup banner; `ht-mcp` stderr inherited; always log recovery events; per-turn success NOT logged (rely on `status-<turnId>.json`); per-turn failure logged with `turn_id` + attempt counter.
- `## Function Design` — borrow by `&str` / `&Path` / `&[String]` where ownership isn't needed; `String` when storing; `&mut self` for mutating methods; constructors return `Result<Self>`; fallible internals return `Result<T>` (anyhow); handlers return `Result<Json<T>, (StatusCode, String)>`; `()` fine for fire-and-forget.
- `## Module Design` — Post-Phase-02 the single file is split into `mcp.rs` (transport) → `worker.rs` (session lifecycle) → `turn.rs` (job + turn-file plumbing) → `http.rs` (axum router) → `lib.rs` (re-exports + crate-level doc). `main.rs` is now a thin bin shim. Dependency direction is downstream-only (http → turn → worker → mcp). Test-only ctors guarded by `#[cfg(test)] pub(crate)` (STATE.md decisions 03-01 / 03-04).

End with: `戻る: [../CLAUDE.md](../CLAUDE.md)`.
  </action>
  <verify>
    <automated>test -f webif/docs/CONVENTIONS.md && grep -q '^# Coding Conventions' webif/docs/CONVENTIONS.md && grep -q '## Async / Concurrency' webif/docs/CONVENTIONS.md && grep -q 'turn_id' webif/docs/CONVENTIONS.md && grep -q 'mcp.rs' webif/docs/CONVENTIONS.md && [ "$(wc -l < webif/docs/CONVENTIONS.md)" -ge 60 ]</automated>
  </verify>
  <done>webif/docs/CONVENTIONS.md exists, ≥60 lines, has Naming / Comments / Doc Comments / Code Style / Error Handling / Async / HTTP / Serde / Logging / Function Design / Module Design sections, references the post-Phase-02 module layout, and links back to ../CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 3: Create webif/docs/ARCHITECTURE.md (full architecture reference)</name>
  <files>webif/docs/ARCHITECTURE.md</files>
  <action>
Write `webif/docs/ARCHITECTURE.md` as the canonical Architecture reference. Source content from `webif/CLAUDE.md` lines 151–261 (the current GSD:architecture block). Structure:

- Title: `# Architecture (ht-webif)`
- `## System Overview` — one short prose paragraph (3–5 sentences) describing the curl → axum → mpsc → worker_loop → Worker (Arc<Mutex>) → McpClient → ht-mcp → claude TUI → filesystem chain. Reference `../HT-PROTOCOL.md` for the wire details rather than re-drawing diagrams (drop the empty ```text``` fence from the original).
- `## Component Responsibilities` — verbatim the table from current lines 157–169, with `src/main.rs:NNN` references UPDATED to point to the new modules where post-Phase-02 split moved them (use STATE.md decisions to infer: HTTP handlers → `src/http.rs`, worker_loop / process_job → `src/turn.rs` (or wherever Plan 02-03/02-04 placed them), Worker → `src/worker.rs`, McpClient → `src/mcp.rs`, AppState → likely `src/http.rs` or `src/lib.rs`). If exact line numbers are not known, write the **module name only** (`src/http.rs`) without a line number rather than carry stale `src/main.rs:NNN` refs. Verify by `grep -n` against the actual source files before committing.
- `## Pattern Overview` — verbatim the 5 bullets (async job queue, filesystem as state store, status-file = done sentinel, shared-fate session recovery, single-file Rust → update last to "module-split Rust" per Phase 02).
- `## Layers` — HTTP / Worker-loop / Worker / McpClient. Per layer state Purpose / Location (module) / Contains / Depends on / Used by. Drop `src/main.rs` line-range refs that are now stale; cite module file paths instead.
- `## Data Flow` — one short prose paragraph per path (Primary async POST /prompt, Synchronous wait=true POST /prompt, GET /turns/{id}, POST /command, POST /restart). 2–4 sentences each. Drop the orphan `###` headers.
- `## Key Abstractions` — turnId (format `YYYYMMDD-HHMMSS-mmm` UTC, orchestrator-only allocation per HT-PROTOCOL §3.2), Job (`{ turn_id, fresh }`, value type, move-only via mpsc), McpClient (stdin/stdout, monotonic `next_id` matches request/response), Worker (owns current claude session, `Arc<Mutex<Worker>>` shared), Prompt body convention (`build_prompt_body` appends 出力規約 to force result→status order).
- `## Entry Points` — `main()` in `src/main.rs` (loads .env, builds Worker, ensures turns_dir, spawns worker_loop, runs axum on `127.0.0.1:${PORT:-8080}`); HTTP routes table (the 4 endpoints); background task `worker_loop` (1 instance, consumes mpsc, calls process_job).
- `## Architectural Constraints` — verbatim the 9 bullets from current lines 235–245, with `src/main.rs:NNN` refs updated to module names.
- `## Anti-Patterns` — for each of the 5 anti-patterns originally listed as bare `###` headers, write one sentence of "why this is wrong" + a sentence on the right pattern. Link to HT-PROTOCOL §12 / §6 / §3.2 where applicable. The 5: (a) TUI への長文一括送信, (b) 完了通知をインメモリで持つ, (c) turnId をクライアントや TUI 内 claude に発番させる, (d) TUI が wedge した時に MCP 呼び出しを無制限に待つ, (e) 旧セッションを残したまま新規セッションを作る.
- `## Error Handling` — verbatim the 7 error-handling bullets from current lines 253–259, with line refs updated to module names.

Drop the trailing `## Cross-Cutting Concerns` empty header.

End with: `戻る: [../CLAUDE.md](../CLAUDE.md)`.
  </action>
  <verify>
    <automated>test -f webif/docs/ARCHITECTURE.md && grep -q '^# Architecture' webif/docs/ARCHITECTURE.md && grep -q '## Component Responsibilities' webif/docs/ARCHITECTURE.md && grep -q '## Anti-Patterns' webif/docs/ARCHITECTURE.md && grep -q '## Data Flow' webif/docs/ARCHITECTURE.md && ! grep -q '^### Primary Request Path' webif/docs/ARCHITECTURE.md && [ "$(wc -l < webif/docs/ARCHITECTURE.md)" -ge 80 ]</automated>
  </verify>
  <done>webif/docs/ARCHITECTURE.md exists, ≥80 lines, has System Overview / Component Responsibilities / Pattern Overview / Layers / Data Flow / Key Abstractions / Entry Points / Architectural Constraints / Anti-Patterns / Error Handling sections, has bodies under every Data Flow path (no orphan ### headers), and links back to ../CLAUDE.md.</done>
</task>

<task type="auto">
  <name>Task 4: Rewrite webif/CLAUDE.md (TL;DR + GSD scaffolding preserved)</name>
  <files>webif/CLAUDE.md</files>
  <action>
Rewrite `webif/CLAUDE.md` end-to-end. Use the Write tool, not Edit (this is a full rewrite). Output structure, top to bottom:

1. **GSD:project block (KEEP VERBATIM)** — copy lines 1–18 of the current file byte-for-byte. Do not reword the Core Value sentence. Do not touch Constraints bullets.

2. **GSD:stack block (TL;DR)** — between the existing `<!-- GSD:stack-start source:codebase/STACK.md -->` and `<!-- GSD:stack-end -->` markers, replace the body with:
   ```
   ## Technology Stack

   - **Language:** Rust edition 2021, single binary (`ht-webif`).
   - **Runtime:** Tokio (`full`), axum 0.8 HTTP server bound to `127.0.0.1:${PORT:-8080}`.
   - **Process model:** spawns `ht-mcp` as a child via stdio JSON-RPC; `ht-mcp` in turn spawns `claude` TUI.
   - **Key crates:** axum, tokio, serde/serde_json, anyhow, chrono (turnId), dotenvy, tower-http (CORS).
   - **Config:** `.env` in `webif/` (cwd-relative). Env vars: `PORT`, `TURNS_DIR`, `HT_MCP_PATH`, `CORS_ORIGINS`. Precedence: env > .env > default.
   - **Build/test:** `cargo` + `webif/justfile`. CI: `.github/workflows/ci.yml` (fmt-check → clippy → test, single ubuntu-latest stable job).
   - **Platform:** POSIX-like OS (atomic rename for turn files); requires `ht-mcp` and `claude` on `PATH`; loopback bind (use SSH tunnel / reverse proxy for remote).

   詳細: [docs/STACK.md](docs/STACK.md)
   ```

3. **GSD:conventions block (TL;DR)** — between the existing markers, replace the body with:
   ```
   ## Conventions

   - **Comments / log strings / `anyhow!` messages:** Japanese. **Identifiers:** English.
   - **Modules:** `src/main.rs` is a thin shim; logic split into `mcp.rs` (transport) → `worker.rs` (session lifecycle) → `turn.rs` (job/turn-files) → `http.rs` (axum router) → `lib.rs` (re-exports + crate doc).
   - **Naming:** snake_case fns/vars, PascalCase structs, `Req`/`Resp` DTO suffixes, SCREAMING_SNAKE_CASE consts with explicit type.
   - **Error handling:** `?` + `anyhow::Result` internally; handlers return `Result<Json<T>, (StatusCode, String)>` via `ise()` helper for 500s; explicit non-500 status codes at the boundary.
   - **Async/locks:** `tokio::sync::Mutex` (held across `.await`), one `Arc<Mutex<Worker>>` shared by `AppState` and `worker_loop`. Wrap MCP calls in `tokio::time::timeout(MCP_TIMEOUT, ...)`.
   - **HTTP safety:** any `turn_id`-style identifier that hits the filesystem MUST be whitelisted (current rule: `^[0-9-]+$`).
   - **Logging:** `eprintln!` with bracketed source tags (`[restart]`, `[worker]`, `[shared-fate]`). Per-turn success: file-only (read `status-<turnId>.json`). Per-turn failure: log + file.
   - **Tooling:** `cargo fmt` + `cargo clippy` before commits. CI enforces `cargo fmt --check`.

   詳細: [docs/CONVENTIONS.md](docs/CONVENTIONS.md)
   ```

4. **GSD:architecture block (TL;DR)** — between the existing markers, replace the body with:
   ```
   ## Architecture

   Data flow: `curl → axum handler → mpsc<Job> → worker_loop → Worker (Arc<Mutex>) → McpClient (stdio JSON-RPC) → ht-mcp → claude TUI → turns/{prompt,result,status}-<turnId>.{txt,json}`.

   - **Single worker, serial turns** (HT-PROTOCOL §12): one in-flight turn at a time; concurrency story is "add more workers", not "parallelise inside one worker".
   - **Filesystem is the state store:** `turns/<prompt|result|status>-<turnId>.{txt|json}` is the source of truth. mpsc carries job *requests* only — never completion signals.
   - **Status file appearance = done** (HT-PROTOCOL §6 sentinel).
   - **Shared-fate recovery:** dead claude TUI → recreate session; wedged ht-mcp → full `/restart` rebuilds the child process.
   - **TurnId allocation:** orchestrator (WebIF) only — never the TUI or the client (HT-PROTOCOL §3.2). Format: `YYYYMMDD-HHMMSS-mmm` UTC.
   - **Timeouts:** `MCP_TIMEOUT = 30s` per MCP call; `TURN_TIMEOUT = 300s` per turn attempt (max 2 attempts); sync `wait:true` handler has its own ~700s deadline.
   - **Endpoints:** `POST /prompt` (async default, `wait:true` for sync), `GET /turns/{turn_id}`, `POST /command` (raw TUI keystrokes), `POST /restart` (kill+respawn ht-mcp+claude).
   - **Anti-patterns:** sending huge prompts inline to the TUI; tracking turn state in memory; letting clients pick turnIds; unbounded MCP waits; leaking the old session when creating a new one. See linked doc for the "why".

   詳細: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) ／ ワイヤープロトコル: [HT-PROTOCOL.md](HT-PROTOCOL.md)
   ```

5. **GSD:skills block (KEEP VERBATIM)** — copy current lines 263–267 unchanged.

6. **GSD:workflow block (KEEP VERBATIM)** — copy current lines 269–280 unchanged.

7. **GSD:profile block (KEEP VERBATIM)** — copy current lines 284–289 unchanged.

Formatting rules for the rewrite:
- Exactly one blank line between adjacent GSD blocks (drop the double-blank gap that currently exists around lines 281–283).
- The `<!-- GSD:xxx-start source:... -->` / `<!-- GSD:xxx-end -->` comment lines stay byte-identical to the originals (including the `source:` paths) — do not change them.
- Do NOT introduce any new top-level `## ` headers outside the GSD blocks.
- Do NOT include any markdown table or code fence inside the TL;DR sections (keeps it skimmable).
- Target final file length: 80–120 lines (down from 289).
  </action>
  <verify>
    <automated>test -f webif/CLAUDE.md && for m in project stack conventions architecture skills workflow profile; do grep -q "<!-- GSD:${m}-start" webif/CLAUDE.md && grep -q "<!-- GSD:${m}-end -->" webif/CLAUDE.md || { echo "missing GSD marker: ${m}"; exit 1; }; done && grep -q '詳細: \[docs/STACK.md\]' webif/CLAUDE.md && grep -q '詳細: \[docs/CONVENTIONS.md\]' webif/CLAUDE.md && grep -q '詳細: \[docs/ARCHITECTURE.md\]' webif/CLAUDE.md && ! grep -q '^### Primary Request Path' webif/CLAUDE.md && ! grep -q '^## System Overview$' webif/CLAUDE.md && ! grep -q '^## Module Design$' webif/CLAUDE.md && [ "$(wc -l < webif/CLAUDE.md)" -le 130 ] && [ "$(wc -l < webif/CLAUDE.md)" -ge 70 ]</automated>
  </verify>
  <done>webif/CLAUDE.md is 70–130 lines, all 7 GSD marker pairs present in original order, each compressed block ends with a `詳細: [docs/...]` link, no dead headers remain, Project/Constraints/Skills/Workflow/Profile blocks byte-identical to originals.</done>
</task>

</tasks>

<verification>
After all 4 tasks complete, run these checks from `/home/parallels/workspaces/ht-mcp-sample/`:

```bash
# 1. All 4 files exist
ls webif/CLAUDE.md webif/docs/STACK.md webif/docs/CONVENTIONS.md webif/docs/ARCHITECTURE.md

# 2. CLAUDE.md is substantially shorter
wc -l webif/CLAUDE.md   # expect 70–130

# 3. GSD markers preserved in original order
grep -n 'GSD:' webif/CLAUDE.md
# Expected order: project-start, project-end, stack-start, stack-end, conventions-start, conventions-end,
#                 architecture-start, architecture-end, skills-start, skills-end, workflow-start, workflow-end,
#                 profile-start, profile-end

# 4. Cross-links resolve
grep -c 'docs/STACK.md\|docs/CONVENTIONS.md\|docs/ARCHITECTURE.md' webif/CLAUDE.md   # expect ≥3
grep -c '../CLAUDE.md' webif/docs/STACK.md webif/docs/CONVENTIONS.md webif/docs/ARCHITECTURE.md  # each ≥1

# 5. Dead artefacts gone from CLAUDE.md
grep -E '^## (System Overview|Language and Edition|Import Organization|Module Design|Cross-Cutting Concerns)$' webif/CLAUDE.md && { echo 'dead header still present'; exit 1; } || true
grep -E '^### (Primary|Synchronous|Polling|Raw Command|Restart) ' webif/CLAUDE.md && { echo 'orphan data-flow header still present'; exit 1; } || true

# 6. Project section verbatim (spot-check the Core Value line)
grep -q '^\*\*Core Value:\*\* \*\*`-p` を避けつつ curl で Claude を実行できる' webif/CLAUDE.md
```
</verification>

<success_criteria>
- `webif/docs/STACK.md`, `webif/docs/CONVENTIONS.md`, `webif/docs/ARCHITECTURE.md` all exist and meet their per-task line/section thresholds.
- `webif/CLAUDE.md` is between 70 and 130 lines (was 289).
- All 7 GSD marker pairs are present in `webif/CLAUDE.md` in the original order with byte-identical comment lines.
- The Project / Constraints / Skills / Workflow / Profile blocks are byte-identical to the originals.
- Each compressed GSD block (stack / conventions / architecture) ends with a `詳細: [docs/...]` link.
- No dead headers (`## System Overview`, `## Language and Edition`, `## Import Organization`, `## Module Design`, `## Cross-Cutting Concerns`, orphan `### *Request Path`/`### Synchronous Path`/etc.) remain in `webif/CLAUDE.md`.
- No content from `HT-PROTOCOL.md` is duplicated; all references to wire-protocol details link to it instead.
- Each `docs/*.md` file links back to `../CLAUDE.md`.
</success_criteria>

<output>
After completion, create `.planning/quick/260528-iav-claude-md-gsd-tl-dr-docs/260528-iav-SUMMARY.md` recording:
- Final line count of `webif/CLAUDE.md` (before/after).
- Sizes of the 3 new docs files.
- Any judgement calls made when updating `src/main.rs:NNN` line refs to post-Phase-02 module paths (which file each component moved to).
- Any content from the original CLAUDE.md that was dropped entirely (not just relocated) — e.g. dead headers, duplicate bullets — so the user can audit.
</output>
