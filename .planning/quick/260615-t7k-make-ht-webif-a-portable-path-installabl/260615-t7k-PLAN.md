---
phase: quick-260615-t7k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
  - scripts/launch-agents.sh
  - justfile
  - README.md
  - README-MULTI-AGENT.md
autonomous: true
requirements: [PORTABLE-01, PORTABLE-02, PORTABLE-03, PORTABLE-04]
must_haves:
  truths:
    - "ユーザはどのディレクトリからでも `ht-webif`（PATH 経由）を起動でき、agents/ プロファイルを env > ./agents > ~/.config/claude-p/agents の順で解決できる"
    - "ユーザは任意の cwd から `launch-agents.sh up` を実行でき、$(pwd)/instances.conf を読み、PATH 上の ht-webif を起動できる"
    - "ユーザは `just install` で ht-webif と launch-agents.sh を ~/.local/bin に、agents/*.toml を ~/.config/claude-p/agents に配置できる"
    - "agents/ がどの探索パスにも無い場合、探索パス一覧を含む明確なエラーで失敗する"
  artifacts:
    - path: "src/config.rs"
      provides: "load_agents_dir の 3 段 precedence（AGENTS_DIR env > ./agents > ~/.config/claude-p/agents）"
      contains: "XDG_CONFIG_HOME"
    - path: "scripts/launch-agents.sh"
      provides: "cwd 基準 instances.conf + PATH 発見 ht-webif のポータブルランチャー"
      contains: "command -v ht-webif"
    - path: "justfile"
      provides: "install レシピ"
      contains: "install"
  key_links:
    - from: "scripts/launch-agents.sh"
      to: "ht-webif (PATH)"
      via: "command -v ht-webif で発見し nohup spawn"
      pattern: "command -v ht-webif"
    - from: "src/config.rs load_agents_dir"
      to: "~/.config/claude-p/agents"
      via: "XDG_CONFIG_HOME / HOME フォールバック"
      pattern: "claude-p"
---

<objective>
ht-webif を PATH 配布可能なポータブルツールにする。3 つの可動部を改修する:
(1) agents/ プロファイル探索を locked precedence（env > cwd > XDG グローバル）に拡張、
(2) launch-agents.sh をスクリプト位置非依存・PATH バイナリ発見・cwd 維持のポータブルランチャーに、
(3) `just install` で PATH 配布を実現。最後にドキュメントを更新する。

Purpose: どのディレクトリからでも `ht-webif` / `launch-agents.sh` を呼べるようにし、リポジトリ作業ディレクトリへの依存を断つ。設計判断はユーザと確定済み（locked、再検討不可）。
Output: 更新された src/config.rs（3 段 precedence + 単体テスト）、ポータブル launch-agents.sh、justfile install レシピ、README.md / README-MULTI-AGENT.md。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

# 既存実装（改修対象の現行コード）
@src/config.rs
@scripts/launch-agents.sh
@justfile
@src/main.rs
@src/profile.rs
</context>

<locked_decisions>
以下はユーザと確定済み。実装はこれに厳密に従うこと（再検討禁止）:
- D-01: agents/ 探索順 = AGENTS_DIR env（最優先・既存維持） > ./agents/（cwd） > ~/.config/claude-p/agents/（$XDG_CONFIG_HOME 尊重、未設定時 $HOME/.config）。全滅なら探索パス一覧入りの明確なエラー。
- D-02: グローバル agents パス = ~/.config/claude-p/agents（XDG 準拠）。
- D-03: instances.conf は $(pwd)/instances.conf からのみ読む。無ければ明確なエラー。
- D-04: インストールは `just install` レシピで PATH 配布。
</locked_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/config.rs — load_agents_dir を 3 段 precedence に変更（D-01/D-02）</name>
  <files>src/config.rs</files>
  <behavior>
    - AGENTS_DIR env が設定されていれば、その値を PathBuf で返す（最優先・既存挙動維持）。
    - AGENTS_DIR 未設定で cwd/agents が存在するディレクトリなら、cwd/agents を返す。
    - AGENTS_DIR 未設定 かつ cwd/agents が無く、XDG グローバル（XDG_CONFIG_HOME/claude-p/agents、未設定時 HOME/.config/claude-p/agents）が存在するなら、それを返す。
    - XDG_CONFIG_HOME が設定されていれば HOME より優先してグローバルパスを構成する。空文字列の XDG_CONFIG_HOME は未設定扱い。
    - 全探索パスが存在しない場合は anyhow エラーを返し、メッセージに試行した 2 パス（cwd/agents, XDG グローバル）と AGENTS_DIR の案内を含む（日本語）。
    - 単体テストは HOME / XDG_CONFIG_HOME / cwd を tempdir で差し替え、env 優先 / cwd フォールバック / グローバルフォールバック / 全滅エラーの順序を検証する。
  </behavior>
  <action>
    `load_agents_dir` のシグネチャを `anyhow::Result<std::path::PathBuf>` のまま、本体を D-01 の 3 段 precedence に書き換える。新クレートは追加せず std::env のみで解決すること。
    手順:
    (1) `std::env::var("AGENTS_DIR")` が Ok ならその PathBuf を即返す（既存挙動・最優先）。
    (2) `std::env::current_dir()?.join("agents")` が `.is_dir()` なら返す。
    (3) グローバルパスを構成する。`std::env::var("XDG_CONFIG_HOME")` が Ok かつ非空ならそれを base にして `<base>/claude-p/agents`、そうでなければ `std::env::var("HOME")` を base にして `<HOME>/.config/claude-p/agents`。`.is_dir()` なら返す。
    (4) どれも無ければ `anyhow::bail!` で探索した cwd/agents パスとグローバルパスを列挙し、「AGENTS_DIR を設定するか agents/ を配置してください」と案内する日本語メッセージを返す。
    空文字列の XDG_CONFIG_HOME は未設定扱いにする（locked_design #1/#2 の XDG 尊重を満たすため）。コメント・エラーメッセージは日本語、識別子は英語（CLAUDE.md 規約）。
    既存の `agents_dir_defaults_to_cwd_agents_when_unset_and_uses_env_when_set` テストは新挙動に合わせて更新する。env 操作テストは既存の save/restore パターン（同ファイルの AGENT テスト参照）を踏襲し、AGENTS_DIR / XDG_CONFIG_HOME / HOME を save → 操作 → restore する。cwd 差し替えは tempfile::tempdir + std::env::set_current_dir を使い、並列テスト汚染を避けるため順序検証を 1 テスト関数にまとめる（既存 config.rs テストの方針と一致）。tempfile は profile.rs が既に dev-dependency として使用しているため追加不要。
  </action>
  <verify>
    <automated>cargo test --lib config 2>&1 | tail -20 && cargo fmt --check && cargo clippy --all-targets --release -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <done>load_agents_dir が D-01 の 3 段 precedence（env > cwd/agents > XDG グローバル > エラー）で解決する。新規単体テストが env 優先 / cwd フォールバック / グローバルフォールバック / 全滅エラーの 4 ケースを検証し全グリーン。cargo fmt --check と clippy -D warnings が通る。</done>
</task>

<task type="auto">
  <name>Task 2: scripts/launch-agents.sh — ポータブル化（cwd 基準 + PATH 発見、D-03）</name>
  <files>scripts/launch-agents.sh</files>
  <action>
    launch-agents.sh をスクリプト位置非依存にする。
    (1) `SCRIPT_DIR` / `WEBIF_DIR` 派生（現行 行 20-21 相当）を削除し、`INSTANCES_CONF="$(pwd)/instances.conf"` に変更する（D-03）。既存の存在確認エラー（行 30-33 相当）はそのまま活かし、パス表示が $(pwd)/instances.conf を指すようにする。
    (2) cmd_up 内のリリースビルド処理（`server_bin="${WEBIF_DIR}/target/release/ht-webif"`、`cargo build --release` 呼び出し、`target/release` 直叩き — 現行 行 94-105 相当）を全削除する。代わりにループ前に `server_bin="$(command -v ht-webif)"` で PATH から発見し、見つからなければ `ht-webif が PATH にありません。just install を実行してください` というエラーで `exit 1` する。`set -euo pipefail` 下で確実に止めるため `server_bin="$(command -v ht-webif)" || { log "ERROR: ..."; exit 1; }` の形にする。
    (3) 全箇所の `turns_dir` 構成 `${WEBIF_DIR}/turns-${port}/${agent}`（cmd_up 行 138 相当・cmd_status 行 361 相当）を `$(pwd)/turns-${port}/${agent}` に変更する（表示・報告用、cwd 基準）。
    (4) spawn サブシェル内の `cd "$WEBIF_DIR"`（行 166 相当）を削除し、ユーザの cwd を維持する。nohup の env 行（行 180-181 相当）の `TURNS_DIR` を `"$(pwd)/turns-${port}"` に変更し（cd しなくなるため絶対パスで明示）、`"$server_bin"` は command -v で得た絶対パスを使う。
    (5) PID/LOG は /tmp 規約のまま（変更しない）。
    (6) 既存の readiness ポーリング（check_info_agent / D-09 agent 名一致 / WR-04 失敗集計 / WR-06 戻り値 2 のポート衝突ハード失敗）、down-all、status のロジックは一切変更しないこと。turns 表示パスのみ cwd 基準に直す。
    コメント・ログは日本語（CLAUDE.md 規約）。
  </action>
  <verify>
    <automated>bash -n scripts/launch-agents.sh && grep -q 'command -v ht-webif' scripts/launch-agents.sh && grep -q 'pwd)/instances.conf' scripts/launch-agents.sh && grep -cv '^#' scripts/launch-agents.sh | head -1 && ! grep -v '^#' scripts/launch-agents.sh | grep -Eq 'WEBIF_DIR|cargo build --release|target/release' && echo PORTABLE_OK</automated>
  </verify>
  <done>bash -n が構文 OK。INSTANCES_CONF が $(pwd)/instances.conf を指す。ht-webif を `command -v` で発見し、未発見時は just install を案内して exit 1。コメント以外の本体に WEBIF_DIR / cargo build --release / target/release が残っていない。readiness/down-all/status ロジックは不変。`up` を実行すると cwd 基準の turns-<port>/<agent> と .env が解決される。</done>
</task>

<task type="auto">
  <name>Task 3: justfile install レシピ追加 + ドキュメント更新（D-04）</name>
  <files>justfile, README.md, README-MULTI-AGENT.md</files>
  <action>
    A. justfile に `install` レシピを追加する（D-04）。
    - 先頭に `cargo build --release` を実行する。
    - `~/.local/bin` が無ければ `mkdir -p ~/.local/bin`。`target/release/ht-webif` と `scripts/launch-agents.sh` を `~/.local/bin/` にコピーし、launch-agents.sh に実行ビットを付与する（`chmod +x`）。
    - agents グローバルディレクトリを `${XDG_CONFIG_HOME:-$HOME/.config}/claude-p/agents` として解決し、`mkdir -p` する（XDG 尊重、Task 1 の Rust 側 precedence と一致させる）。`agents/*.toml` をそこへコピーする。既存ファイルを不用意に壊さない配慮として、レシピ末尾でコピー先パスを表示し、既存ファイルは上書きする旨を注記コメントで残す（あるいは `cp -i` は非対話レシピで止まるため使わず、コピー前後のファイル名をエコーする）。
    - レシピ末尾で、`~/.local/bin` が PATH に含まれているか `case ":$PATH:" in *":$HOME/.local/bin:"*) ... ;; *) 注意表示 ;; esac` で判定し、無い場合は「~/.local/bin を PATH に追加してください」という注意を表示する。
    - 任意で `uninstall` レシピを追加してよい（~/.local/bin の ht-webif / launch-agents.sh を削除。agents グローバルは破壊的なので削除しないか、明示メッセージのみ）。必須ではない。
    just レシピ内のシェル行は `#!/usr/bin/env bash` のレシピ shebang か、各行を1コマンドにする just の規約に従う（複数行ロジックは `#!/usr/bin/env bash` shebang レシピにまとめると安全）。

    B. README.md を更新する。
    - インストールセクション（現行 行 33 の `cargo build --release` 周辺）に `just install` による PATH 配布手順を追記する: `just install` → 任意のディレクトリから `ht-webif` / `launch-agents.sh up` が呼べる。
    - 多重インスタンスセクション（行 72 以降）に「instances.conf は cwd から読む」点を明記する。`launch-agents.sh` の TURNS_DIR が cwd 基準（行 122 相当の `<webif>/turns-<port>` を `$(pwd)/turns-<port>` に）になる旨へ更新する。
    - 環境変数表（行 418 相当 `AGENTS_DIR` の行）付近に、AGENTS_DIR 未設定時の探索順（./agents > ~/.config/claude-p/agents）を追記する。

    C. README-MULTI-AGENT.md を更新する。
    - 起動セクション（行 38-40 の `AGENT=claude ./ht-webif` など）を PATH から呼べる形（`AGENT=claude ht-webif`）に更新し、`just install` 後はどこからでも単体起動できる点を明記する。
    - 多重インスタンス節（行 153 以降）の「ビルド 1 回 → バイナリ直 spawn（CR-01）」記述（行 180 相当）を、PATH 発見（`command -v ht-webif`）に置き換える。just install を前提とする旨と、instances.conf は cwd から読む点を追記する。
    - agents 探索順（env > ./agents > ~/.config/claude-p/agents）をプロファイルセクション（行 29 以降）に追記する。

    全ドキュメントは日本語（プロジェクト規約）。実在のレシピ名・パスと一致させること。
  </action>
  <verify>
    <automated>just --list 2>&1 | grep -q install && grep -q 'just install' README.md && grep -q 'claude-p/agents' README.md && grep -Eq 'command -v ht-webif|just install' README-MULTI-AGENT.md && echo DOCS_OK</automated>
  </verify>
  <done>`just --list` に install レシピが現れる。install は cargo build → ~/.local/bin へ ht-webif と launch-agents.sh 配置 → ${XDG_CONFIG_HOME:-$HOME/.config}/claude-p/agents へ agents/*.toml 配置 → PATH 未登録時の注意表示、を行う。README.md / README-MULTI-AGENT.md が PATH 導入手順・cwd 基準 instances.conf・agents 探索順・PATH からの単体起動を反映している。  <done>`just --list` に install レシピが現れる。install は cargo build → ~/.local/bin へ ht-webif と launch-agents.sh 配置 → ${XDG_CONFIG_HOME:-$HOME/.config}/claude-p/agents へ agents/*.toml 配置 → PATH 未登録時の注意表示、を行う。README.md / README-MULTI-AGENT.md が PATH 導入手順・cwd 基準 instances.conf・agents 探索順・PATH からの単体起動を反映している。</done>
</task>

</tasks>

<verification>
全体検証（プロジェクト規約 + locked decisions の充足）:

1. `cargo test --release` 全グリーン（既存 + Task 1 新規テスト）。
2. `cargo fmt --check` と `cargo clippy --all-targets --release -- -D warnings` が通る。
3. `bash -n scripts/launch-agents.sh` 構文 OK、本体に WEBIF_DIR / cargo build / target/release が残っていない。
4. `just --list` に install（任意で uninstall）レシピが現れる。
5. 手動スモーク（任意・実機）: `just install` 後、リポジトリ外の一時ディレクトリに instances.conf を置いて `launch-agents.sh up` が PATH 上の ht-webif を起動し、cwd 基準で turns-<port>/<agent> を作ることを確認できる。
6. locked decisions D-01〜D-04 が実装・ドキュメントに反映されている。
</verification>

<success_criteria>
- load_agents_dir が AGENTS_DIR env > ./agents > ~/.config/claude-p/agents（XDG 尊重）の順で解決し、全滅時は探索パス一覧入りの明確なエラーを返す（D-01/D-02）。
- launch-agents.sh が $(pwd)/instances.conf のみを読み（無ければ明確なエラー、D-03）、`command -v ht-webif` で PATH からバイナリを発見し、cwd を維持して spawn する。readiness/down-all/status の既存ロジックは不変。
- `just install` で ht-webif と launch-agents.sh が ~/.local/bin に、agents/*.toml が XDG グローバルに配置され、PATH 未登録時に注意が表示される（D-04）。
- README.md / README-MULTI-AGENT.md が PATH 導入手順・cwd 基準 instances.conf・agents 探索順・PATH 単体起動を反映。
- cargo test 全グリーン、fmt/clippy クリーン、日本語コメント・ログ規約を遵守。
</success_criteria>

<output>
Create `.planning/quick/260615-t7k-make-ht-webif-a-portable-path-installabl/260615-t7k-SUMMARY.md` when done
</output>
