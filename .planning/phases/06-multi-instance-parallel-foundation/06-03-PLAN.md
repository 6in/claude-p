---
phase: 06-multi-instance-parallel-foundation
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - README.md
autonomous: true
requirements: [PARA-04]
must_haves:
  truths:
    - "README に多重インスタンス起動手順（instances.conf 書式 + just up-all / down-all / agents-status）が記載されている"
    - "README に Codex の CODEX_HOME 分離手順が具体例付きで記載されている"
    - "README に claude / opencode のクレデンシャル共有可否が理由付きで明記されている"
  artifacts:
    - path: "README.md"
      provides: "多重インスタンス + クレデンシャル分離ガイド（既存 §72-86 を更新）"
      contains: "instances.conf"
  key_links:
    - from: "README.md"
      to: "scripts/launch-agents.sh + instances.conf"
      via: "launcher コマンドと conf 書式の文書化"
      pattern: "up-all"
---

<objective>
README.md の既存「複数インスタンス運用」節（行 72-86）を更新し、(1) instances.conf 書式 + `just up-all`/`down-all`/`agents-status` の使い方、(2) エージェント別クレデンシャル分離ガイド（Codex は CODEX_HOME 分離必須・具体例、claude/opencode は共有可否を理由付きで明記）を記載する。Phase 5 で繰延された CODEX_HOME / OpenCode インスタンス分離検証をここで決着させる（D-11）。

Purpose: 運用者が多重インスタンス起動とクレデンシャル分離で迷わないようにする（PARA-04、成功基準4）。
Output: README.md の多重インスタンス + クレデンシャル分離節。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-multi-instance-parallel-foundation/06-CONTEXT.md
@.planning/phases/06-multi-instance-parallel-foundation/06-PATTERNS.md
@README.md
@.planning/phases/06-multi-instance-parallel-foundation/06-02-PLAN.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan updates:
- `README.md` 複数インスタンス運用節: instances.conf 書式説明 + `just up-all`/`just down-all`/`just agents-status` 使用例 + エージェント別クレデンシャル分離表（claude=共有可/理由、codex=CODEX_HOME 分離必須/具体例、opencode=Phase 5 検証結果反映）
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: README 多重インスタンス + クレデンシャル分離ガイド更新</name>
  <files>README.md</files>
  <read_first>
    - README.md（行 70-86 既存「複数インスタンス運用」節 = 更新対象、行 169-201 claude-p 節 = 文体参照）
    - scripts/launch-agents.sh と instances.conf（Plan 02 成果物 — 実際のサブコマンド名 up/down-all/status、conf 書式、CODEX_HOME 列を文書と一致させる）
    - 06-PATTERNS.md §"README.md"（更新3項目: conf 書式 / just レシピ / エージェント別 credential 分離の理由文面）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md（Codex/OpenCode の auth セットアップ・credential 書き込み先 ~/.codex / ~/.config/opencode の出典）
  </read_first>
  <action>
    README.md 既存「複数インスタンス運用」節（行 72-86）を、launch-agents.sh ベースの多重インスタンス手順に更新する。(1) instances.conf の書式（`<agent> <port> [KEY=VALUE ...]`、コメント/空行無視、デフォルト3行 claude:8080/codex:8081/opencode:8082）を説明。(2) `just up-all`（一括起動 + /info readiness）/ `just down-all`（全停止）/ `just agents-status`（状態表示）の使用例を Plan 02 の実装と一致させる。(3) クレデンシャル分離ガイドを表または箇条書きで: claude は `~/.claude/` OAuth が read-only 共有のため分離不要（理由明記）、codex は `~/.codex/` にセッション/auth 状態を書くため per-instance `CODEX_HOME` 分離必須（instances.conf の CODEX_HOME 列 + 具体パス例を掲載）、opencode は `~/.config/opencode/` の Phase 5 実機検証結果に基づき共有可否と理由を明記。TURNS_DIR 分離（D-16 の `<TURNS_DIR>/<agent>/` 自動分離 + 同一エージェント多重起動時の per-port turns-${PORT}）でコリジョン回避することにも触れる（成功基準3）。既存の単純な PORT/AGENT/TURNS_DIR 手動起動例（行 74-84）は launcher 不使用の素の手順として残すか launcher 節に統合するかは可読性で判断。コメント/本文日本語。
  </action>
  <verify>
    <automated>grep -q 'instances.conf' README.md && grep -q 'up-all' README.md && grep -q 'CODEX_HOME' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains `instances.conf` with format explanation
    - README.md contains `just up-all` / `down-all` usage
    - README.md contains `CODEX_HOME` separation example for codex
    - README.md states claude and opencode credential sharing decision with rationale
    - launcher subcommand names in README match scripts/launch-agents.sh (up / down-all / status)
  </acceptance_criteria>
  <done>README に多重インスタンス起動手順・CODEX_HOME 分離手順・claude/opencode 共有可否ガイドが記載されている（成功基準4）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — docs only) | README 更新のみ。新規コードパス・エンドポイント・ファイルアクセスなし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | README のクレデンシャル例 | mitigate | CODEX_HOME 等の例はプレースホルダパス（例: /home/user/.codex-instance1）のみ。実トークン・実秘密値を README に記載しない |
</threat_model>

<verification>
- README.md に instances.conf / up-all / CODEX_HOME が存在
- launcher サブコマンド名が scripts/launch-agents.sh と一致
- クレデンシャル例がプレースホルダのみ（実秘密値なし）
</verification>

<success_criteria>
- README に多重インスタンス起動手順・CODEX_HOME 分離手順・クレデンシャル分離ガイドが記載（成功基準4、PARA-04）
- Phase 5 繰延の CODEX_HOME / OpenCode 分離検証がここで決着（D-11）
</success_criteria>

<output>
Create `.planning/phases/06-multi-instance-parallel-foundation/06-03-SUMMARY.md` when done
</output>
