---
phase: 05-codex-cli-and-opencode-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/opencode.toml
  - scripts/setup-opencode.sh
  - scripts/e2e-opencode.sh
  - README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [AGNT-03, AGNT-04]
tags: [agent-profile, opencode, e2e, config, toml, bash, setup-script]

must_haves:
  truths:
    - "AGENT=opencode ./ht-webif で POST /prompt を送ると result-<turnId>.txt に非空回答 + status-<turnId>.json に done が書かれる (AGNT-03)"
    - "AGENT=opencode で POST /prompt {\"fresh\":true} がセッションリセット方式（/new または respawn）で動作し履歴隔離が成立する (AGNT-04, D-10)"
    - "agents/opencode.toml が trigger_template=/turn {prompt_path}（ASCII のみ）/ ready_pattern=Ask anything / prerequisite 手順コメントを正確に記述している (SC5, Pitfall 1/7)"
    - "scripts/setup-opencode.sh が ~/.config/opencode/commands/turn.md を冪等生成する (D-04, 既存ならスキップ)"
    - "fresh_mode 確定値が実機挙動に基づき決定され、フォールバック時は ROADMAP SC4 と AGNT-04 文面が実測で更新される (D-01/D-02/D-03)"
  artifacts:
    - path: "agents/opencode.toml"
      provides: "OpenCode エージェントプロファイル"
      contains: "trigger_template = \"/turn {prompt_path}\""
    - path: "scripts/setup-opencode.sh"
      provides: "turn.md 冪等生成スクリプト"
      contains: "commands/turn.md"
    - path: "scripts/e2e-opencode.sh"
      provides: "OpenCode E2E 検証スクリプト（result/status/履歴隔離チェック）"
      contains: "AGENT=opencode"
  key_links:
    - from: "scripts/e2e-opencode.sh"
      to: "agents/opencode.toml"
      via: "AGENT=opencode 起動 → load_agent_profile が opencode.toml を読む"
      pattern: "AGENT=opencode"
    - from: "agents/opencode.toml"
      to: "~/.config/opencode/commands/turn.md"
      via: "trigger_template /turn が turn.md カスタムコマンドを展開（setup スクリプトが前提生成）"
      pattern: "/turn \\{prompt_path\\}"
---

<objective>
OpenCode v1.4.3 のエージェントプロファイル `agents/opencode.toml` を確立し、前提となるカスタムコマンド `~/.config/opencode/commands/turn.md` を冪等生成する `scripts/setup-opencode.sh` を同梱し、`ht-webif` 経由のフル E2E と `fresh:true`（`/new` 試行 → 必要なら respawn フォールバック）を `scripts/e2e-opencode.sh` で実証する。

Purpose: AGNT-03（OpenCode E2E result 取得）と AGNT-04（OpenCode fresh:true）を満たす。05-RESEARCH.md で `/turn {prompt_path}` カスタムコマンド方式が HIGH confidence で検証済み（inline-completion/file-autocomplete ブロックを回避、Claude Haiku 4.5 / GitHub Copilot で covenant 遵守確認、~13s E2E）。fresh:true は D-01（`/new` 試行）→ D-02（不成立なら respawn）→ D-03（フォールバック確定時はドキュメント更新）の手順で決着させる。
Output: `agents/opencode.toml`、`scripts/setup-opencode.sh`、`scripts/e2e-opencode.sh`（新規）、README.md 追記、（D-03 発動時のみ）ROADMAP.md / REQUIREMENTS.md 文面更新。Rust コード変更なし（config-only）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-PATTERNS.md
@agents/claude.toml
@src/profile.rs
@scripts/smoke.sh
</context>

<artifacts_this_phase_produces>
このプランが新規に作成するシンボル / ファイル（drift 検証で新規として扱うべきもの）:

- `agents/opencode.toml` — 新規ファイル（OpenCode プロファイル）
- `scripts/setup-opencode.sh` — 新規ファイル（turn.md 冪等生成スクリプト）
- `scripts/e2e-opencode.sh` — 新規ファイル（OpenCode E2E 検証スクリプト）
- `~/.config/opencode/commands/turn.md` — setup スクリプトがホスト上に生成するカスタムコマンド定義（リポジトリ外、コミット対象外）

このプランは Rust コードを一切変更しない。`AgentProfile` スキーマ（Phase 4 確立）の既存フィールドのみを使用する。`clear_command` の複数キー化などのスキーマ拡張は行わない（D-02、`deny_unknown_fields` のため不可）。README.md / ROADMAP.md / REQUIREMENTS.md は既存ファイルへの追記・文面更新であり新規シンボルではない。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: scripts/setup-opencode.sh と agents/opencode.toml を作成する</name>
  <files>scripts/setup-opencode.sh, agents/opencode.toml</files>
  <read_first>
    - agents/claude.toml — フィールド順序・コメント様式の手本（`command =` が正）
    - src/profile.rs — AgentProfile スキーマ。フィールド名は `command`。`deny_unknown_fields`。output_covenant は `{result_path}`/`{status_path}`、trigger_template は `{prompt_path}` を含む必須
    - .planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md — Pattern 2（lines 195-258）が profile の出典。Pitfall 1（マルチバイト破棄）, Pitfall 7（inline-completion ブロックと /turn 回避策）, Addendum /turn workaround（lines 658-695、turn.md 内容と trigger）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-PATTERNS.md — scripts/setup-opencode.sh セクション（lines 252-283）の冪等生成パターン、agents/opencode.toml セクション（lines 77-96）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md — D-04（setup スクリプト同梱で turn.md 冪等生成）, D-05（スコープは turn.md 生成のみ）, D-06（prerequisite コメント）, D-13（opencode.toml 確定値）
  </read_first>
  <action>
    **(a) scripts/setup-opencode.sh**: `smoke.sh` の shebang + `set -euo pipefail` + `log()`（タグ `[setup-opencode]`）構造を踏襲し、単一責務（turn.md 生成のみ、D-05）の冪等スクリプトを作る。実行ビット +x。
    - `TURN_CMD_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/opencode/commands"`、`TURN_CMD_FILE="$TURN_CMD_DIR/turn.md"`
    - 既に存在すれば `log "turn.md は既に存在します ... (スキップ)"` + `exit 0`（ホスト上に既存ファイルあり — 必ず冪等であること）
    - 無ければ `mkdir -p "$TURN_CMD_DIR"` して heredoc で turn.md を生成。turn.md 内容は frontmatter `description: Read a prompt file and follow the instructions in it` + 本文 `Read the file $ARGUMENTS and follow the instructions in it.`（05-RESEARCH.md Addendum の working recipe どおり）
    - auth 確認や doctor 的検査は**含めない**（D-05、単一責務）

    **(b) agents/opencode.toml**: `agents/claude.toml` のコメント様式を踏襲。確定値（D-13, 05-RESEARCH.md Pattern 2）— トップレベルキー名は `src/profile.rs` の `command`（`cmd` ではない）に一致させること:
    - `command = ["opencode"]`
    - `ready_pattern = "Ask anything"`（起動後 4-8s で現れる入力フィールドプレースホルダ）
    - `fresh_mode` と `clear_command` は **Task 2 の実機試行結果で確定**するため、Task 1 では研究推奨の安全側 `fresh_mode = "respawn"` を仮置きする（D-02: respawn は Phase 4 でテスト済み、Rust 変更ゼロ）。`clear_command = "/new"` はフィールドとして残しつつ、コメントで two-Enter ダイアログ問題（Pitfall 3）を注記する
    - `trigger_template = "/turn {prompt_path}"`（**ASCII のみ厳守** — Pitfall 1: 日本語は ht_send_keys が無音破棄。Pitfall 7: 自然言語は inline-completion ブロック → /turn カスタムコマンドで回避。`{prompt_path}` 必須）
    - `output_covenant` — **英語のみ**（マルチバイト制約 Pitfall 1）。05-RESEARCH.md Pattern 2 の英語 covenant（`[ht-webif output rule] ...`）を使用。`{result_path}`/`{status_path}` 両プレースホルダ必須
    - `startup_timeout_secs = 15`（4-8s 観測）、`turn_timeout_secs = 300`
    - `model_flag`/`model_value` はコメントアウト例（GitHub Copilot で claude モデル推奨）

    ファイル先頭に D-06 の prerequisite コメントブロック: opencode が PATH 上にあること、`opencode auth login`（または `opencode providers list` で GitHub Copilot 認証確認）、**`bash scripts/setup-opencode.sh` で turn.md を生成する**こと（D-04/D-06）。既知問題（マルチバイト破棄・inline-completion ブロック・GLM-4.6 covenant 不安定 → GitHub Copilot Claude Haiku 4.5 で遵守確認済み）もコメントに残す。実測タイミング（起動 4-8s / E2E ~13s）を注記する。
  </action>
  <verify>
    <automated>bash -n scripts/setup-opencode.sh && test -x scripts/setup-opencode.sh && grep -q 'commands/turn.md\|commands"' scripts/setup-opencode.sh && test -f agents/opencode.toml && grep -q 'trigger_template = "/turn {prompt_path}"' agents/opencode.toml && grep -q 'ready_pattern = "Ask anything"' agents/opencode.toml && cargo test --lib profile 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/setup-opencode.sh` が存在し実行可能（`test -x`）、`bash -n` 通過、冪等（turn.md 既存時は exit 0 でスキップ）
    - turn.md 生成内容が frontmatter `description:` + 本文 `Read the file $ARGUMENTS and follow the instructions in it.`
    - `agents/opencode.toml` が存在し `load_agent_profile("opencode", ...)` でパース成功（deny_unknown_fields / validate_profile 通過）
    - トップレベルキー `command = ["opencode"]`、`ready_pattern = "Ask anything"`、`trigger_template = "/turn {prompt_path}"`
    - `trigger_template` と `output_covenant` が ASCII のみ（マルチバイト文字を含まない — Pitfall 1）
    - `output_covenant` に `{result_path}` と `{status_path}` を含む
    - ファイル先頭に `# 前提条件:` コメントがあり `opencode auth login` と `bash scripts/setup-opencode.sh` 参照を含む（SC5, D-06）
    - `cargo test` 27 件グリーン維持
  </acceptance_criteria>
  <done>setup-opencode.sh が turn.md を冪等生成でき、opencode.toml が ASCII-only trigger/covenant とプロファイル確定値・prerequisite コメントを含み load_agent_profile を通過する</done>
</task>

<task type="auto">
  <name>Task 2: scripts/e2e-opencode.sh を作成し fresh_mode を確定する</name>
  <files>scripts/e2e-opencode.sh, agents/opencode.toml</files>
  <read_first>
    - scripts/smoke.sh — 構造の手本（Task 1 と同じパターン群）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-PATTERNS.md — scripts/e2e-opencode.sh セクション（lines 238-248）: e2e-codex.sh と同構造、PORT=8082 / AGENT=opencode / require_tool "opencode"
    - .planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md — D-01（まず /new を試行し実挙動を記録）, D-02（不成立なら respawn フォールバック）, D-10/D-11（履歴隔離 2 ターンテスト）, D-07（コミットするスクリプト）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md — Pattern 3（/new two-Enter 問題、lines 260-269）, OQ-2/OQ-3
    - agents/opencode.toml — Task 1 で作成したプロファイル（fresh_mode 仮置き値）
  </read_first>
  <action>
    `scripts/smoke.sh`（および 05-01 の e2e-codex.sh と同構造）を踏襲して `scripts/e2e-opencode.sh` を新規作成する。実行ビット +x。

    OpenCode 固有の差分:
    - `PORT="${PORT:-8082}"`（D-07、codex の 8081 と分離）
    - 起動コマンド `AGENT=opencode PORT=8082 TURNS_DIR=./turns/opencode cargo run --release` を background 起動 → `CARGO_PID=$!`
    - `require_tool "opencode" ...`、`require_tool "jq"`、`require_tool "curl"`
    - 起動前に `bash "$SCRIPT_DIR/setup-opencode.sh"` を呼んで turn.md を保証する（前提条件の自動充足）
    - `log()` タグ `[e2e-opencode]`

    検証ロジック（e2e-codex.sh と同じ 3 段階）:
    1. **基本 E2E（AGNT-03）**: `{"prompt":"What is 2+3? Answer with the number only.","wait":true}` → `.status == "done"` かつ `.result` 非空
    2. **履歴シード（ターン1）**: `{"prompt":"Remember this secret number: 7331","wait":true}`
    3. **fresh 履歴隔離（AGNT-04 + D-10）**: `{"prompt":"What secret number did I tell you?","fresh":true,"wait":true}` → result に `7331` 非含有を確認

    **fresh_mode 確定手順（D-01/D-02/D-03 — このタスクの核心）**:
    - まず opencode.toml の `fresh_mode = "command"` + `clear_command = "/new"` で fresh:true を試行する（D-01: ht-webif 経由の実挙動を記録）。`/new` 試行はタイムアウトを短め（例: 60s）に設定し、スタックした場合のスナップショット/挙動を SUMMARY に記録してターン浪費を抑える
    - `/new` でダイアログがスタックする/履歴が漏れる場合は `fresh_mode = "respawn"` に切り替えて opencode.toml を更新し再実行する（D-02、Rust 変更ゼロ）
    - **どちらで確定したかをスクリプト内コメントと SUMMARY に明記する**

    全段階 pass で `log "OK: OpenCode E2E 完了"` + `exit 0`。エビデンス（turnId・所要時間・result・/new 試行結果）を stderr に出力。
  </action>
  <verify>
    <automated>bash -n scripts/e2e-opencode.sh && test -x scripts/e2e-opencode.sh && grep -q 'AGENT=opencode' scripts/e2e-opencode.sh && grep -q '8082' scripts/e2e-opencode.sh && grep -q '7331' scripts/e2e-opencode.sh && grep -q 'setup-opencode.sh' scripts/e2e-opencode.sh && echo SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/e2e-opencode.sh` が存在し実行可能、`bash -n` 通過
    - `set -euo pipefail`、`trap cleanup EXIT INT TERM`、`wait_for_server` を含む
    - `AGENT=opencode` と `PORT=8082`（または `${PORT:-8082}`）でサーバ起動
    - 起動前に `scripts/setup-opencode.sh` を呼んで turn.md を保証する
    - 履歴隔離テスト（`7331` seed → fresh:true → result に `7331` 非含有検証）を含む（D-10）
    - 基本 E2E が `.status == "done"` かつ `.result` 非空をチェック
    - opencode.toml の `fresh_mode` が実機試行に基づき確定値（`command`/`respawn`）に設定されている。どちらで確定したかがコメントに明記されている（D-01/D-02）
  </acceptance_criteria>
  <done>e2e-opencode.sh が構文チェック通過・実行可能で、setup-opencode.sh を前提充足し基本 E2E + 履歴隔離を AGENT=opencode / PORT=8082 で検証し、opencode.toml の fresh_mode が実機試行で確定している</done>
</task>

<task type="auto">
  <name>Task 3: README 追記と（フォールバック時）ROADMAP/REQUIREMENTS 文面更新</name>
  <files>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - README.md — 既存のエージェント追加手順・env var 表（Phase 4 で AGENT/AGENTS_DIR 追記済み）。追記の章立てスタイルを踏襲
    - .planning/ROADMAP.md — Phase 5 §Success Criteria（特に SC4: `/new` 方式）。D-03 発動時に文面更新
    - .planning/REQUIREMENTS.md — AGNT-04 文面（lines 23-24、既にフォールバック条項あり）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md — D-03（フォールバック確定時の文面更新規約）, Claude's Discretion（README 章立て）
    - agents/codex.toml, agents/opencode.toml — README で参照する確定プロファイル
  </read_first>
  <action>
    **(a) README.md 追記**（Claude's Discretion の章立て）: 新エージェント（codex / opencode）の追加手順と前提条件を記載する。含める内容:
    - Codex: `codex login`（ChatGPT Plus 認証）、`--dangerously-bypass-approvals-and-sandbox` の意味、`AGENT=codex PORT=8081 ./ht-webif` 起動例、`bash scripts/e2e-codex.sh` での検証
    - OpenCode: `opencode auth login`（GitHub Copilot 認証推奨）、**`bash scripts/setup-opencode.sh` で turn.md を生成する前提手順**、`AGENT=opencode PORT=8082 ./ht-webif` 起動例、`bash scripts/e2e-opencode.sh` での検証、既知制約（trigger は ASCII のみ）
    - CODEX_HOME / 多重インスタンス分離は Phase 6 で扱う旨の前方参照

    **(b) D-03 発動時のみ**（Task 2 で `/new` が不成立 → respawn 確定の場合）: ROADMAP.md Phase 5 SC4 と REQUIREMENTS.md AGNT-04 の文面を実測結果で更新する。例: 「fresh:true が respawn 方式で正常動作。`/new` はエージェント選択ダイアログ要因で不採用 — 2026-06-XX 実測」。Codex の SC2 が既に持つフォールバック条項と同じパターン。`/new` で確定した場合はこの更新は不要（現行文面のまま）。
  </action>
  <verify>
    <automated>grep -q 'codex' README.md && grep -q 'opencode' README.md && grep -q 'setup-opencode.sh' README.md && echo README_OK</automated>
  </verify>
  <acceptance_criteria>
    - README.md に Codex の起動・auth・検証手順（`codex login`、`scripts/e2e-codex.sh`）が記載されている
    - README.md に OpenCode の起動・auth・検証手順が記載され、`bash scripts/setup-opencode.sh` 前提手順への参照を含む
    - fresh_mode が respawn で確定した場合: ROADMAP.md SC4 と REQUIREMENTS.md AGNT-04 が実測文面に更新されている（`/new` で確定した場合は更新不要・現行文面維持）
    - ドキュメントと実態に乖離がない（D-03）
  </acceptance_criteria>
  <done>README に両エージェントの追加・auth・検証手順が記載され、fresh_mode フォールバック確定時は ROADMAP/REQUIREMENTS の文面が実測に揃っている</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: OpenCode 実機 E2E 検証（checkpoint）</name>
  <action>実機 OpenCode + GitHub Copilot 課金で `bash scripts/e2e-opencode.sh` を実行し、基本 E2E（result 非空 + status done, AGNT-03）と fresh:true 履歴隔離（AGNT-04）を検証する。CI 不可（D-08）。下記 how-to-verify の手順に従い、/new 試行結果と確定 fresh_mode をエビデンスとして SUMMARY に記録する。</action>
  <what-built>
    `agents/opencode.toml`（OpenCode プロファイル、`/turn` トリガー）、`scripts/setup-opencode.sh`（turn.md 冪等生成）、`scripts/e2e-opencode.sh`（再実行可能 E2E）。スクリプトは setup を前提充足し ht-webif を `AGENT=opencode` で起動して result/status 生成（AGNT-03）と fresh:true 履歴隔離（AGNT-04）を自動検証する。fresh_mode は実機試行で確定済み。
  </what-built>
  <how-to-verify>
    実エージェント + GitHub Copilot 課金が必要なため CI 不可（D-08）。手動で実機 E2E を実行する:

    1. プロジェクトルートで `bash scripts/e2e-opencode.sh` を実行する（内部で setup-opencode.sh が turn.md を保証）
    2. 期待される挙動:
       - turn.md が `~/.config/opencode/commands/turn.md` に存在（既存ならスキップ）
       - サーバが `AGENT=opencode PORT=8082` で起動（ready_pattern "Ask anything" 検知）
       - 基本 E2E: `result-<turnId>.txt` に `5`（2+3 の回答）+ `status-<turnId>.json` に `{"status":"done"}`（研究値 ~13s）
       - 履歴隔離: fresh:true ターンの回答に `7331` が含まれない
       - スクリプトが `OK: OpenCode E2E 完了` を出力して exit 0
    3. SUMMARY に記録するエビデンス: 各ターンの turnId・所要時間・result 内容・履歴隔離結果・**`/new` 試行の挙動と fresh_mode 最終確定値（command/respawn）**（D-01/D-02）
    4. fresh_mode が respawn に確定した場合、Task 3 で ROADMAP SC4 / REQUIREMENTS AGNT-04 が更新されていることを確認する（D-03）
    5. トリガーが届かない（"Ask anything" のまま）場合は Pitfall 7 のとおり turn.md 存在を再確認し、ASCII-only trigger を確認する
  </how-to-verify>
  <resume-signal>`approved`（E2E 成功）と入力するか、観測した挙動（turnId・所要時間・/new 試行結果・確定 fresh_mode・失敗内容）を記述してください。フォールバック発生時はドキュメント更新を確認後に再承認してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl クライアント → axum (loopback) | 信頼されない入力が `127.0.0.1` 経由で WebIF に入る。turn_id は http.rs で `^[0-9-]+$` 検証済み（既存） |
| WebIF → opencode TUI (ht-mcp stdio) | trigger は `/turn {prompt_path}` カスタムコマンド経由。ASCII のみ（マルチバイトは ht-mcp が破棄） |
| opencode TUI → ファイルシステム | covenant に従い result/status をプロジェクトルート内 `turns/opencode/` に書く |
| setup-opencode.sh → ~/.config/opencode/commands/ | ホストのユーザ設定ディレクトリに turn.md を書く（冪等、既存ならスキップ） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | opencode モデルが covenant に従い任意ファイルを書く | accept | 既存 claude/codex 統合と同一の脅威レベル。OS レベルで信頼確立済み。書き込み先は `{result_path}`/`{status_path}` プレースホルダに限定 |
| T-05-05 | Tampering | setup-opencode.sh が `~/.config/opencode/commands/turn.md` を書く | mitigate | スクリプトは turn.md 既存時はスキップ（冪等、上書きしない）。書き込み先は XDG 規約の固定パス（`${XDG_CONFIG_HOME:-$HOME/.config}/opencode/commands/`）に限定、外部入力で経路が変わらない |
| T-05-06 | Information Disclosure | fresh:true 後に前ターン履歴が漏れる（履歴隔離失敗） | mitigate | e2e-opencode.sh の 2 ターンテスト（7331 seed → fresh:true → 非含有検証、D-10）が漏洩を検出。漏れる場合は respawn フォールバックで隔離保証（D-02） |
| T-05-SC | Tampering | パッケージインストール（npm/pip/cargo） | n/a | Phase 5 は config-only で新規パッケージ追加なし（05-RESEARCH.md Package Legitimacy Audit: Not applicable）。インストールタスクなし |
</threat_model>

<verification>
- `cargo test` 27 件が全グリーン（Rust 変更なしのリグレッションチェック、D-08）
- `bash -n scripts/setup-opencode.sh` / `bash -n scripts/e2e-opencode.sh` 構文チェック通過
- `setup-opencode.sh` の冪等性（turn.md 既存時 exit 0 スキップ）
- `load_agent_profile("opencode", ...)` がパース成功（ASCII-only trigger/covenant 含む）
- 実機 E2E（checkpoint）: `bash scripts/e2e-opencode.sh` が exit 0 で result 非空 + status done + 履歴隔離 pass、fresh_mode 確定
</verification>

<success_criteria>
- AGNT-03: `AGENT=opencode ./ht-webif` の `POST /prompt` で result-<turnId>.txt に非空回答 + status-<turnId>.json に done（ROADMAP SC3）
- AGNT-04: `AGENT=opencode` の `fresh:true` が `/new` または respawn 方式で動作し履歴隔離が成立（ROADMAP SC4、D-01/D-02/D-10）
- agents/opencode.toml が trigger_template（/turn、ASCII）/ ready_pattern / prerequisite 手順を正確に記述（ROADMAP SC5、D-13）
- scripts/setup-opencode.sh が turn.md を冪等生成（D-04）
- D-03: フォールバック確定時は ROADMAP SC4 / REQUIREMENTS AGNT-04 文面が実測で更新済み
</success_criteria>

<output>
Create `.planning/phases/05-codex-cli-and-opencode-validation/05-02-SUMMARY.md` when done.
SUMMARY には実機 E2E のエビデンス（turnId・所要時間・result 内容・履歴隔離テスト結果）、`/new` 試行の実挙動と fresh_mode 最終確定値（command/respawn）、D-03 発動有無を含めること。
</output>
