---
phase: 05-codex-cli-and-opencode-validation
plan: 03
type: execute
wave: 1
depends_on: [05-02]
files_modified:
  - scripts/e2e-opencode.sh
autonomous: true
gap_closure: true
requirements: [AGNT-04]

must_haves:
  truths:
    - "e2e-opencode.sh の AGNT-04 検証が falsifiable になる: fresh_mode=respawn を壊した場合（無効値・recreate を no-op 化・fresh フラグ無視）に段階 3 が必ず FAIL する"
    - "段階 2.5（正の対照実験）が、非 fresh ターン間に会話継続性が存在しないこと（UNKNOWN が返ること）を実機で記録し、段階 2→3 単独では隔離を検証できない理由を明示する"
    - "段階 3 が fresh:true ターンで Worker::recreate() が実際に発火したことをサーバログ（[shared-fate] 再生成行のセッション ID 遷移）で証明する"
    - "全段階の成功判定が status == \"done\" の allowlist で行われ、status=\"unknown\"/空 result が PASS にならない（WR-01/WR-02 解消）"
  artifacts:
    - path: "scripts/e2e-opencode.sh"
      provides: "falsifiable な AGNT-04 E2E（段階 2.5 正の対照 + 段階 3 respawn 発火ログ検証 + done allowlist ゲート）"
      contains: "shared-fate"
  key_links:
    - from: "scripts/e2e-opencode.sh"
      to: "src/worker.rs Worker::recreate() の [shared-fate] ログ"
      via: "fresh:true ターン前後で LOG_FILE 内 [shared-fate] 再生成行を grep で計数し増分を検証"
      pattern: "shared-fate.*再生成"
    - from: "scripts/e2e-opencode.sh 段階 2.5"
      to: "POST /prompt（非 fresh）"
      via: "非 fresh で秘密数値を質問 → UNKNOWN を期待し継続性なしを記録"
      pattern: "fresh.*false|wait.*true"
---

<objective>
`scripts/e2e-opencode.sh` の AGNT-04（SC4: `AGENT=opencode` の `fresh:true` respawn 方式）検証を、構造的に unfalsifiable な状態から falsifiable な状態へ修正する。

検証ギャップ（05-VERIFICATION.md / CR-01）の根本原因: D-09 の `opencode run --command turn <path>` ラッパー方式では各ターンが独立した会話になるため、段階 2 でシードした秘密数値 7331 は段階 3 の前に必ず破棄される。その結果、段階 3（fresh:true で 7331 が漏れないことを確認）は `fresh_mode=respawn` が壊れていても、recreate() が no-op でも、fresh フラグが無視されていても、常に PASS する。テスト自体が AGNT-04 を検証していない。

採用方針（検証者の Option 1 — RESEARCH/SUMMARY が OpenCode の信頼できる `--continue`/`--session` 継続性パスを確認していないため、Option 2 の継続性構築は本ギャップ（検証品質）に対して過剰）:
1. 段階 2.5（正の対照実験）を追加し、非 fresh ターン間に会話継続性が存在しないこと（UNKNOWN が返ること）を実機で記録する。これにより段階 2→3 単独では隔離を検証できない理由を明示的に文書化する。
2. 段階 3 の合格条件を「7331 が含まれない」（空洞）から「fresh:true ターンで Worker::recreate() が実際に発火した」（サーバログの [shared-fate] 再生成行のセッション ID 遷移）へ置き換える。
3. 同じテストの判別力を弱めている WR-01（status != done を拒否しない）と WR-02（UNKNOWN/空 result がゲートにならない）を同時に解消する。

Purpose: AGNT-04 の実機 E2E が、respawn 機構が壊れたときに必ず落ちるようにする。fresh は「-p 相当の文脈リセット + 実行」という Core Value の代理機能であり（D-10）、その検証は見た目の PASS ではなく機構の発火を証明しなければならない。
Output: 修正された `scripts/e2e-opencode.sh`（Rust 変更ゼロ。既存 27+ テストのリグレッションなし）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-VERIFICATION.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-REVIEW.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
このプランが作成・変更するシンボル / 段階 / パス:

- `scripts/e2e-opencode.sh`（変更）— 以下を追加・変更:
  - 段階 2.5 ブロック（新規）: 非 fresh の「秘密数値は何ですか」質問 → UNKNOWN を期待し、継続性なしを記録する正の対照実験。変数 `response25` / `status25` / `result25` / `turn_id25` / `T25_*`。
  - 段階 3 の合格条件変更（既存ブロック）: `[shared-fate]` 再生成行のログ計数（fresh ターン前後の baseline / after 差分）に基づく respawn 発火検証。変数 `recreate_before` / `recreate_after`。
  - 全段階の status ゲートを `!= "done"` allowlist へ反転（段階 1 / 2 / 2.5 / 3、WR-01）。
  - 段階 3 の result 非空ハードゲートと UNKNOWN ハードゲート（WR-02）。
  - 最終サマリーに段階 2.5 行と respawn 発火証明行を追記。

このプランは新規 Rust シンボル・CLI フラグ・新規ファイルを一切作らない（src/worker.rs の既存 `[shared-fate]` ログを読むだけ）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 段階 2.5（正の対照実験）と全段階の done allowlist ゲートを追加する</name>
  <files>scripts/e2e-opencode.sh</files>
  <read_first>
    - scripts/e2e-opencode.sh（変更対象。現行の段階 1/2/3 構造・curl パターン・jq パース・log ヘルパー・cleanup を把握する）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-REVIEW.md（WR-01: status != done allowlist の根拠、WR-02: 空 result / UNKNOWN ハードゲートの根拠、CR-01: 段階 2.5 正の対照の意図）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-02-SUMMARY.md（D-09 ラッパー方式により非 fresh ターンに会話継続性が存在しないという前提）
    - src/turn.rs（read_turn が status JSON 欠損・破損時に "unknown" を返す挙動 — done allowlist が必要な理由）
  </read_first>
  <action>
    既存の段階 2（履歴シード）と段階 3 の間に「段階 2.5: 非 fresh 正の対照実験」ブロックを新設する。段階 2.5 は `fresh` を指定せず（または明示的に `fresh:false`）、段階 3 と同一の「ファイル読み取り・検索・シェル実行を禁止し会話記憶のみで答える」制約付きで「私が以前伝えた秘密の数値は何ですか？知らない場合は UNKNOWN とだけ書いてください」を送る。日本語プロンプトは段階 3 と同様に jq の --arg で JSON を構築してエスケープする。レスポンスを `response25` / `status25` / `result25` / `turn_id25` にパースし、所要時間を `T25_*` で計測・ログ出力する。

    段階 2.5 の合否は次の意味論で記録する: 期待値は UNKNOWN（D-09 ラッパー方式では非 fresh ターンにも会話継続性がないため）。result に 7331 が含まれていれば、それは会話継続性ではなくファイル検索由来であることを示す警告として記録する（テストを fail にはしないが、なぜ段階 2→3 単独で隔離を検証できないかを明示するログを必ず出す）。具体的には「OpenCode は D-09 アーキテクチャ上、非 fresh でも前ターンを記憶しない。よって段階 2→3 の 7331 非出現は隔離の証拠にならず、段階 3 では respawn 機構の発火そのものを検証する」という趣旨のログ行を出力する。

    全段階（段階 1 / 2 / 2.5、および後続 Task 2 で扱う段階 3）の status 判定を WR-01 に従って allowlist へ反転する: 現行の `if [[ "$status" == "timeout" ]] || [[ "$status" == "failed" ]]` を `if [[ "$statusN" != "done" ]]` に置き換え、エラーログに期待値 done を明記して exit 1 する。これにより read_turn が "unknown" を返すガーブル時や jq フォールバックの "unknown" が PASS をすり抜けなくなる。

    日本語コメント・ログ文字列・識別子英語の CLAUDE.md 規約を厳守する。set -euo pipefail / trap cleanup を壊さない。フェンスコードや full 実装を新規追加するのではなく既存の段階ブロック様式（段階 1/2/3 の curl→jq→ゲートの形）に合わせる。
  </action>
  <verify>
    <automated>bash -n scripts/e2e-opencode.sh && grep -q '段階 2.5' scripts/e2e-opencode.sh && [ "$(grep -vE '^[[:space:]]*#' scripts/e2e-opencode.sh | grep -c '!= "done"')" -ge 3 ]</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/e2e-opencode.sh` が構文エラーなく通過する。
    - スクリプトに「段階 2.5」というラベルのブロックが存在し、非 fresh（fresh 未指定または fresh:false）で秘密数値を質問する curl が含まれる。
    - 段階 2.5 が「OpenCode は非 fresh でも前ターンを記憶しない／段階 2→3 単独では隔離を検証できない」趣旨のログ行を出力する（コメント行ではなく実行される log 呼び出し）。
    - コメント行を除いた本文に `!= "done"` を用いた status ゲートが段階 1・2・2.5 の少なくとも 3 箇所存在する（`grep -vE '^[[:space:]]*#' | grep -c '!= "done"'` が 3 以上）。
    - 旧来の `status == "timeout" || status == "failed"` のみで拒否する判定が段階 1・2 に残っていない。
  </acceptance_criteria>
  <done>
    段階 2.5 が非 fresh での会話継続性欠如を実機で記録し、段階 1/2/2.5 の status ゲートが done allowlist になっている。
  </done>
</task>

<task type="auto">
  <name>Task 2: 段階 3 の合格条件を respawn 発火ログ検証 + UNKNOWN/非空ハードゲートに置き換える</name>
  <files>scripts/e2e-opencode.sh</files>
  <read_first>
    - scripts/e2e-opencode.sh（Task 1 適用後の段階 3 ブロック。LOG_FILE 変数・cleanup での LOG_FILE 削除タイミング・段階 3 の curl/jq を把握する）
    - src/worker.rs（recreate() が成功時に出力する `[shared-fate] claude セッション再生成: {new} （旧 {old} を閉鎖）` ログ行（worker.rs:152-155）、および ensure_healthy() が出力する `[shared-fate] claude セッション不健全 → 再生成` 行（worker.rs:116）。fresh_mode=respawn → process_job が turn.rs:60 で recreate() を呼ぶ経路）
    - src/turn.rs（process_job: job.fresh かつ fresh_mode=="respawn" のとき worker.recreate() を呼ぶ分岐（turn.rs:59-61）。non-fresh ターンではこの分岐に入らない）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-REVIEW.md（CR-01 推奨: 段階 3 をサーバログの recreate 行検証へ置換 / WR-02: 空 result・UNKNOWN ハードゲート）
  </read_first>
  <action>
    段階 3 の合格条件を「fresh:true ターンで Worker::recreate() が実際に発火したこと」をサーバログで証明する方式へ置き換える。recreate() 成功時のログ行は `src/worker.rs` で `[shared-fate]` を含み「再生成」を含む（`grep -E 'shared-fate.*再生成'` で一致）。ただし ensure_healthy() も不健全時に `[shared-fate]` 行を出すため、fresh ターン専用の発火を判別する必要がある。判別方法: 段階 3 の fresh:true curl を送る直前に LOG_FILE 内の再生成行数を `recreate_before=$(grep -cE 'shared-fate.*再生成' "$LOG_FILE" || true)` で baseline 取得し、fresh ターン完了後に `recreate_after` を取得して、`recreate_after > recreate_before` を満たすこと（fresh:true により少なくとも 1 回 recreate が追加発火したこと）をハードゲートにする。増分が 0 なら respawn 機構が発火しておらず AGNT-04 が達成されていないので exit 1 する。grep が 0 件で grep の終了コードが非ゼロになり set -e に拾われないよう `|| true` を付す（カウントは行数で判定）。

    さらに WR-02 に従い段階 3 に 2 つのハードゲートを追加する: (1) `result3` が空なら「隔離検証は空回答では成立しない」として exit 1。(2) `result3` に UNKNOWN（大文字小文字無視）が含まれなければ「回答の意味論が不明」として exit 1。現行の「INFO ログのみ」を廃し UNKNOWN をハードゲート化する。既存の「7331 が含まれていたら fail」チェックは段階 3 の補助確認として残してよいが、それ単独を合格条件にはしない（空洞性の根本原因）。

    cleanup が成功時に LOG_FILE を削除する（rc==0 のとき rm -f）ため、recreate 行の grep は cleanup より前（段階 3 の判定中）に実行されること、および baseline 取得タイミングが段階 3 fresh curl 送信より前であることを保証する。LOG_FILE への cargo run 出力は stderr 経由でファイルに入る（既存の `>"$LOG_FILE" 2>&1`）ため `[shared-fate]` 行は LOG_FILE に到達する。fresh ターンは wait:true で result/status が返った後に grep する（status 確定 = 処理完了 = recreate ログ出力済み）。

    最終サマリーに「段階 2.5（正の対照）」行と「respawn 発火検証: recreate_before→recreate_after の増分」を示す行を追記する。「fresh:true + ファイル検索禁止で 7331 が含まれない」という旧合格メッセージは respawn 発火証明メッセージに差し替える。日本語コメント/ログ・英語識別子の規約を厳守。
  </action>
  <verify>
    <automated>bash -n scripts/e2e-opencode.sh && grep -qE 'shared-fate.*再生成' scripts/e2e-opencode.sh && grep -q 'recreate_before' scripts/e2e-opencode.sh && grep -q 'recreate_after' scripts/e2e-opencode.sh && grep -vE '^[[:space:]]*#' scripts/e2e-opencode.sh | grep -qi 'UNKNOWN' && cargo test --quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/e2e-opencode.sh` が構文エラーなく通過する。
    - 段階 3 のソースに `recreate_before` と `recreate_after` の 2 変数が存在し、`grep -cE 'shared-fate.*再生成' "$LOG_FILE"` 形式で LOG_FILE から再生成行数を計数している。
    - 段階 3 に `recreate_after > recreate_before`（fresh:true による recreate 発火増分 >= 1）をハードゲートとして exit 1 する分岐が存在する。
    - 段階 3 に result3 が空なら exit 1 するハードゲートが存在する。
    - 段階 3 に UNKNOWN（大文字小文字無視）を含まなければ exit 1 するハードゲートが存在する（旧 INFO ログのみの扱いが廃止されている）。
    - baseline 取得（recreate_before）が段階 3 の fresh:true curl 送信より前の行にあり、recreate_after の grep が cleanup（成功時 LOG_FILE 削除）より前に実行される。
    - `cargo test` がグリーンを維持する（Rust 変更ゼロのリグレッションチェック）。
  </acceptance_criteria>
  <done>
    段階 3 の合格条件が「7331 非出現」（空洞）から「fresh:true で Worker::recreate() が発火した（[shared-fate] 再生成行の増分）+ result 非空 + UNKNOWN 含有」へ置き換わり、respawn 機構を壊すと段階 3 が必ず FAIL する。
  </done>
</task>

</tasks>

<verification>
## フェーズ検証（AGNT-04 falsifiability）

1. **構文 + 静的チェック:**
   - `bash -n scripts/e2e-opencode.sh` が通過する。
   - 段階 2.5 ブロック・`recreate_before`/`recreate_after`・done allowlist・UNKNOWN ハードゲートが存在する。

2. **リグレッション:**
   - `cargo test` がグリーン（Rust 変更ゼロ）。

3. **falsifiability 検証（手動 / 実機）:**
   - 正常系: `bash scripts/e2e-opencode.sh` が全段階 PASS（段階 2.5 は UNKNOWN を記録、段階 3 は recreate 発火増分 >= 1）。
   - 反証系（実機で確認可能なら）: `agents/opencode.toml` の `fresh_mode` を一時的に無効値へ変えるか recreate 経路を阻害した場合、段階 3 の recreate 増分が 0 になり exit 1 する。これにより旧テストでは検出できなかった respawn 故障が検出されることを示す。
</verification>

<success_criteria>
- AGNT-04 / SC4 の検証が falsifiable: respawn 機構（`fresh_mode=respawn` → `Worker::recreate()`）が壊れたとき段階 3 が必ず FAIL する。
- 段階 2.5 が、D-09 ラッパー方式では非 fresh ターンに会話継続性が存在しないこと（UNKNOWN）を実機で記録し、段階 2→3 単独で隔離を検証できない理由を明示する。
- WR-01 解消: 全段階の合否が status == "done" の allowlist。status="unknown" は PASS しない。
- WR-02 解消: 段階 3 で空 result と UNKNOWN 欠如がハードゲート。
- `cargo test` グリーン維持（Rust 変更ゼロ）。
</success_criteria>

<output>
Create `.planning/phases/05-codex-cli-and-opencode-validation/05-03-SUMMARY.md` when done
</output>
