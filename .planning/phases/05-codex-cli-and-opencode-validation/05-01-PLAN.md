---
phase: 05-codex-cli-and-opencode-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/codex.toml
  - scripts/e2e-codex.sh
autonomous: false
requirements: [AGNT-01, AGNT-02]
tags: [agent-profile, codex, e2e, config, toml, bash]

must_haves:
  truths:
    - "AGENT=codex ./ht-webif で起動し POST /prompt を送ると result-<turnId>.txt に非空回答 + status-<turnId>.json に done が書かれる (AGNT-01)"
    - "AGENT=codex で POST /prompt {\"fresh\":true} が /clear 送信方式で動作し、履歴隔離が成立する (AGNT-02, D-10)"
    - "agents/codex.toml が ready_pattern=YOLO mode / fresh_mode=command / clear_command=/clear / prerequisite 手順コメントを正確に記述している (SC5)"
    - "scripts/e2e-codex.sh が再実行可能で、result 非空 + status=done + 履歴隔離 2 ターンテストを検証する (D-07, D-10)"
  artifacts:
    - path: "agents/codex.toml"
      provides: "Codex CLI エージェントプロファイル"
      contains: "ready_pattern = \"YOLO mode\""
    - path: "scripts/e2e-codex.sh"
      provides: "Codex E2E 検証スクリプト（result/status/履歴隔離チェック）"
      contains: "AGENT=codex"
  key_links:
    - from: "scripts/e2e-codex.sh"
      to: "agents/codex.toml"
      via: "AGENT=codex 起動 → load_agent_profile が codex.toml を読む"
      pattern: "AGENT=codex"
    - from: "agents/codex.toml"
      to: "src/profile.rs AgentProfile"
      via: "toml::from_str デシリアライズ（deny_unknown_fields）"
      pattern: "command = \\[\"codex\""
---

<objective>
Codex CLI v0.139.0 のエージェントプロファイル `agents/codex.toml` を確立し、`ht-webif` 経由のフル E2E（`POST /prompt` → result/status ファイル）と `fresh:true`（`/clear`）の履歴隔離を、コミットする再実行可能スクリプト `scripts/e2e-codex.sh` で実証する。

Purpose: AGNT-01（Codex E2E result 取得）と AGNT-02（Codex fresh:true）を満たす。05-RESEARCH.md で Codex は HIGH confidence で検証済み（E2E 18-27s、`/clear` 動作確認済み）だが、ht-webif 経由のフル E2E は本フェーズで初めて通す。
Output: `agents/codex.toml`（新規）、`scripts/e2e-codex.sh`（新規）。Rust コード変更なし（config-only）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md
@.planning/phases/05-codex-cli-and-opencode-validation/05-PATTERNS.md
@agents/claude.toml
@src/profile.rs
@scripts/smoke.sh
</context>

<artifacts_this_phase_produces>
このプランが新規に作成するシンボル / ファイル（drift 検証で新規として扱うべきもの）:

- `agents/codex.toml` — 新規ファイル（Codex CLI プロファイル）
- `scripts/e2e-codex.sh` — 新規ファイル（Codex E2E 検証スクリプト）

このプランは Rust コードを一切変更しない。`AgentProfile` スキーマ（`command`, `ready_pattern`, `fresh_mode`, `clear_command`, `output_covenant`, `trigger_template`, `startup_timeout_secs`, `turn_timeout_secs`, `model_flag`, `model_value`）は Phase 4 で確立済みで、このプランはその既存フィールドのみを使用する。新規 TOML フィールドは追加しない（`deny_unknown_fields` のため不可）。
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: agents/codex.toml を作成する</name>
  <files>agents/codex.toml</files>
  <read_first>
    - agents/claude.toml — フィールド順序・コメント様式の手本（`command =` が正、`cmd` ではない）
    - src/profile.rs — AgentProfile スキーマ。フィールド名は `command`（Vec<String>）。`deny_unknown_fields` のため未知キーは load 時エラー。output_covenant は `{result_path}` と `{status_path}` の両方、trigger_template は `{prompt_path}` を含む必須（validate_profile）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-RESEARCH.md — Pattern 1（lines 142-193）が出典。実測値（起動 ~3s / E2E 18-27s）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md — D-12（codex.toml の確定値）, D-06（prerequisite コメント要件）
  </read_first>
  <action>
    `agents/claude.toml` のフィールド順序とコメント様式（各フィールド直前に日本語 `# 説明` 行、optional フィールドは `# field = value` 形式）を踏襲して `agents/codex.toml` を新規作成する。

    確定値（D-12, 05-RESEARCH.md Pattern 1）— TOML のトップレベルキー名は `src/profile.rs` の struct フィールド名に正確に一致させること（`command`、`cmd` ではない）:
    - `command = ["codex", "--dangerously-bypass-approvals-and-sandbox"]`（trust ダイアログ全スキップ）
    - `ready_pattern = "YOLO mode"`（起動バナーに現れる安定部分文字列）
    - `fresh_mode = "command"`
    - `clear_command = "/clear"`（Codex の `/clear` はダイアログを開かず直接コンテキストをリセット — VERIFIED）
    - `output_covenant` — `agents/claude.toml` の日本語 covenant をそのまま流用（Codex は日本語指示に対応済み）。`{result_path}` と `{status_path}` の両プレースホルダを必ず含めること
    - `trigger_template = "{prompt_path} を読んで、その指示に従ってください。"`（日本語可 — Codex は日本語パスも処理可能。`{prompt_path}` 必須）
    - `startup_timeout_secs = 15`（起動 3s 観測、15s は余裕値）
    - `turn_timeout_secs = 300`（単純タスク 18-27s）
    - `model_flag` / `model_value` はコメントアウト例として残す（省略時は `~/.codex/config.toml` の `model = "gpt-5.5"` が使われる）

    ファイル先頭に D-06 の prerequisite コメントブロック（`# 前提条件:`）を置く。記載内容: codex が PATH 上にあること、`codex login` 実行済み（`~/.codex/auth.json` 存在）、プロジェクトが trusted もしくは `--dangerously-bypass-approvals-and-sandbox` で承認スキップ、`CODEX_HOME` による多重インスタンス分離は Phase 6 参照。実測タイミング（起動 ~3s / E2E 18-27s）もコメントに残す（運用時タイムアウト調整の参考）。`/tmp` を TURNS_DIR にすると lean-ctx がプロジェクト外読み取りを拒否するため D-16 既定の `./turns/codex/` を使う旨も注記する。
  </action>
  <verify>
    <automated>cargo run --quiet --bin ht-webif 2>&1 <<<'' | head -0; AGENT=codex cargo test --lib profile 2>&1 | tail -5; test -f agents/codex.toml && grep -q 'ready_pattern = "YOLO mode"' agents/codex.toml && grep -q 'command = \["codex"' agents/codex.toml && echo TOML_OK</automated>
  </verify>
  <acceptance_criteria>
    - `agents/codex.toml` が存在する
    - トップレベルキーが `command`（`cmd` でない）で値が `["codex", "--dangerously-bypass-approvals-and-sandbox"]`
    - `ready_pattern = "YOLO mode"`、`fresh_mode = "command"`、`clear_command = "/clear"`
    - `output_covenant` に `{result_path}` と `{status_path}` の両方を含む（grep で確認可能）
    - `trigger_template` に `{prompt_path}` を含む
    - `startup_timeout_secs = 15`、`turn_timeout_secs = 300`
    - ファイル先頭に `# 前提条件:` コメントブロックがあり auth セットアップ手順（`codex login`）を含む（SC5）
    - `cargo test` 27 件が全グリーンを維持（Rust 変更なしのリグレッションチェック）
    - プロファイルが `load_agent_profile("codex", ...)` でパース成功する（`deny_unknown_fields` / validate_profile を通過）
  </acceptance_criteria>
  <done>agents/codex.toml が deny_unknown_fields と validate_profile を通過してロードでき、確定値（command/ready_pattern/fresh_mode/clear_command/timeouts）と prerequisite コメントを正確に含む</done>
</task>

<task type="auto">
  <name>Task 2: scripts/e2e-codex.sh を作成する</name>
  <files>scripts/e2e-codex.sh</files>
  <read_first>
    - scripts/smoke.sh — 構造の手本（shebang + `set -euo pipefail`、SCRIPT_DIR/WEBIF_DIR/PORT 定数、log() ヘルパー、require_tool()、cleanup() + trap、wait_for_server() ポーリング、curl POST wait:true + jq status チェック）
    - .planning/phases/05-codex-cli-and-opencode-validation/05-PATTERNS.md — scripts/e2e-codex.sh セクション（lines 99-234）に具体的なパターン引用とポート/AGENT 割当
    - .planning/phases/05-codex-cli-and-opencode-validation/05-CONTEXT.md — D-07（コミットするスクリプトで検証）, D-10/D-11（履歴隔離 2 ターンテスト必須）, D-08（CI には繋がない）
    - agents/codex.toml — Task 1 で作成したプロファイル（このスクリプトが AGENT=codex で起動して使う）
  </read_first>
  <action>
    `scripts/smoke.sh` の構造をそのまま踏襲して `scripts/e2e-codex.sh` を新規作成する（`scripts/` は既存、mkdir 不要。実行ビット +x を付与）。流用する要素: `#!/usr/bin/env bash` + `set -euo pipefail`、`SCRIPT_DIR`/`WEBIF_DIR` 定数、`log()`（タグは `[e2e-codex]`）、`require_tool`、`cleanup()` + `trap cleanup EXIT INT TERM`、`wait_for_server()` ポーリング。

    Codex 固有の差分:
    - `PORT="${PORT:-8081}"`（D-07 専用ポート、smoke.sh の 8080 と分離）
    - サーバ起動コマンドは `AGENT=codex PORT=8081 TURNS_DIR=./turns/codex cargo run --release` を background 起動して `CARGO_PID=$!`
    - `require_tool "codex" ...`（claude ではなく）、`require_tool "jq" ...`、`require_tool "curl" ...`

    検証ロジック（3 段階、いずれか失敗で非ゼロ exit）:
    1. **基本 E2E（AGNT-01）**: `curl -s --max-time 720 -X POST $BASE_URL/prompt -H 'Content-Type: application/json' -d '{"prompt":"What is 2+2? Answer with the number only.","wait":true}'`。レスポンス JSON を jq で取り出し、`.status` が `done`（`timeout`/`failed` なら fail）かつ `.result` が非空であることを確認する
    2. **履歴シード（ターン1）**: `{"prompt":"Remember this secret number: 7331","wait":true}` を送る
    3. **fresh 履歴隔離（AGNT-02 + D-10）**: `{"prompt":"What secret number did I tell you?","fresh":true,"wait":true}` を送り、`.result` に `7331` が**含まれないこと**を `grep -q 7331` で確認する（含まれていたら履歴隔離失敗で `exit 1`）

    各段階に成功/失敗ログを出す。全段階 pass で `log "OK: Codex E2E 完了"` + `exit 0`。turns/ 配下の成果物自体はコミットしない（gitignore 済み、D-09）。スクリプトはエビデンス（turnId・所要時間・result 内容の要約）を stderr に出力する。
  </action>
  <verify>
    <automated>bash -n scripts/e2e-codex.sh && test -x scripts/e2e-codex.sh && grep -q 'AGENT=codex' scripts/e2e-codex.sh && grep -q '8081' scripts/e2e-codex.sh && grep -q '7331' scripts/e2e-codex.sh && echo SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/e2e-codex.sh` が存在し実行ビットが立っている（`test -x`）
    - `bash -n scripts/e2e-codex.sh`（構文チェック）が exit 0
    - `set -euo pipefail`、`trap cleanup EXIT INT TERM`、`wait_for_server` を含む（smoke.sh パターン踏襲）
    - `AGENT=codex` と `PORT=8081`（または `${PORT:-8081}`）でサーバを起動する
    - 履歴隔離テスト（`7331` を seed → fresh:true で質問 → result に `7331` 非含有を検証）を含む（D-10）
    - 基本 E2E が `.status == "done"` かつ `.result` 非空をチェックする
  </acceptance_criteria>
  <done>scripts/e2e-codex.sh が構文チェックを通過し実行可能で、基本 E2E（result 非空 + status done）と履歴隔離 2 ターンテストの両方を AGENT=codex / PORT=8081 で検証する</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Codex 実機 E2E 検証（checkpoint）</name>
  <action>実機 Codex + ChatGPT Plus 課金で `bash scripts/e2e-codex.sh` を実行し、基本 E2E（result 非空 + status done, AGNT-01）と fresh:true `/clear` 履歴隔離（AGNT-02）を検証する。CI 不可（D-08）。下記 how-to-verify の手順に従い、エビデンスを SUMMARY に記録する。</action>
  <what-built>
    `agents/codex.toml`（Codex プロファイル）と `scripts/e2e-codex.sh`（再実行可能 E2E スクリプト）。スクリプトは ht-webif を `AGENT=codex` で起動し、`POST /prompt` で result/status ファイル生成（AGNT-01）と `fresh:true` の `/clear` 履歴隔離（AGNT-02）を自動検証する。
  </what-built>
  <how-to-verify>
    実エージェント + 実 ChatGPT Plus 課金が必要なため CI 不可（D-08）。手動で実機 E2E を実行する:

    1. プロジェクトルートで `bash scripts/e2e-codex.sh` を実行する
    2. 期待される挙動:
       - サーバが `AGENT=codex PORT=8081` で起動（ログに `[profile]` バナー、ready_pattern "YOLO mode" 検知）
       - 基本 E2E: `result-<turnId>.txt` に `4`（または 2+2 の回答）が書かれ `status-<turnId>.json` に `{"status":"done"}`
       - 履歴隔離: fresh:true ターンの回答に `7331` が含まれない
       - スクリプトが `OK: Codex E2E 完了` を出力して exit 0
    3. SUMMARY に記録するエビデンス: 各ターンの turnId、所要時間（研究値 18-27s と比較）、result 内容、履歴隔離テストの pass/fail
    4. `/clear` が実機で不安定だった場合（履歴漏れ等）は、ROADMAP SC2 のフォールバック条項どおり `fresh_mode = "respawn"` に切り替え、codex.toml を更新して再実行する
  </how-to-verify>
  <resume-signal>`approved`（E2E 成功）と入力するか、観測した挙動（turnId・所要時間・失敗内容）を記述してください。fresh_mode フォールバックが必要なら respawn 切り替え後に再承認してください。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curl クライアント → axum (loopback) | 信頼されない入力が `127.0.0.1` 経由で WebIF に入る。turn_id は http.rs で `^[0-9-]+$` 検証済み（既存） |
| WebIF → codex TUI (ht-mcp stdio) | `--dangerously-bypass-approvals-and-sandbox` により codex がシェル実行を承認なしで行う |
| codex TUI → ファイルシステム | covenant に従い result/status ファイルを書き込む（プロジェクトルート内 `turns/codex/`） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | codex `--dangerously-bypass-approvals-and-sandbox`（無承認シェル実行） | accept | 既存 claude 統合と同一の脅威レベル。運用者が localhost でのみ起動（SEC-01 は v3+ 繰延）。プロファイルは operator-controlled な起動設定 |
| T-05-02 | Tampering | codex がプロファイル covenant に従い任意ファイルを書く | accept | OS レベルで信頼確立済み。書き込み先は `{result_path}`/`{status_path}` プレースホルダ（orchestrator 割当の turnId ベースパス）に限定 |
| T-05-03 | Spoofing | `CODEX_HOME` env var が悪意ある auth を指す可能性 | accept | operator-controlled env var、`AGENT` env var と同等のリスク。多重インスタンス分離検証は Phase 6（PARA-04） |
| T-05-SC | Tampering | パッケージインストール（npm/pip/cargo） | n/a | Phase 5 は config-only で新規パッケージ追加なし（05-RESEARCH.md Package Legitimacy Audit: Not applicable）。インストールタスクなし |
</threat_model>

<verification>
- `cargo test` 27 件が全グリーン（Rust 変更なしのリグレッションチェック、D-08）
- `bash -n scripts/e2e-codex.sh` 構文チェック通過
- `load_agent_profile("codex", ...)` がパース成功（profile テストで間接確認）
- 実機 E2E（checkpoint）: `bash scripts/e2e-codex.sh` が exit 0 で result 非空 + status done + 履歴隔離 pass
</verification>

<success_criteria>
- AGNT-01: `AGENT=codex ./ht-webif` の `POST /prompt` で result-<turnId>.txt に非空回答 + status-<turnId>.json に done（ROADMAP SC1）
- AGNT-02: `AGENT=codex` の `fresh:true` が `/clear` 送信で動作し履歴隔離が成立（ROADMAP SC2、D-10）
- agents/codex.toml が ready_pattern/fresh_mode/prerequisite 手順を正確に記述（ROADMAP SC5、D-12）
- scripts/e2e-codex.sh が再実行可能で履歴隔離 2 ターンテストを含む（D-07/D-10）
</success_criteria>

<output>
Create `.planning/phases/05-codex-cli-and-opencode-validation/05-01-SUMMARY.md` when done.
SUMMARY には実機 E2E のエビデンス（turnId・所要時間・result 内容・履歴隔離テスト結果）と、fresh_mode フォールバックが発生した場合はその記録を含めること。
</output>
