---
phase: 04-agent-profile-abstraction
plan: 04
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - src/profile.rs
  - src/turn.rs
  - src/worker.rs
autonomous: true
requirements: [PROF-03]
gap_closure: true

must_haves:
  truths:
    - "cargo fmt --check が exit 0 で通る（CI の fmt-check ジョブがグリーン）"
    - "rustfmt の自動整形のみでロジック変更がない（cargo test が依然グリーン）"
  artifacts:
    - path: "src/profile.rs"
      provides: "rustfmt 整形済みソース"
    - path: "src/turn.rs"
      provides: "rustfmt 整形済みソース"
    - path: "src/worker.rs"
      provides: "rustfmt 整形済みソース"
  key_links:
    - from: "CI fmt-check ジョブ"
      to: "src/*.rs"
      via: "cargo fmt --check"
      pattern: "cargo fmt --check"
---

<objective>
Phase 4 検証で発見された CR-02（BLOCKER）を閉じる: `cargo fmt --check` が `src/profile.rs` / `src/turn.rs` / `src/worker.rs` に差分を出力し、CI パイプライン（fmt-check → clippy → test）の最初のジョブがハード失敗する状態を解消する。

`cargo fmt` を実行して rustfmt の自動整形を適用するだけ（ロジック変更なし）。

**Wave 2 配置の理由:** このプランは 04-03（covenant ロックフリー化・spawn_command 追加・TURN_TIMEOUT 削除）が src/http.rs / src/worker.rs / src/profile.rs を変更した「後」に走る必要がある。先に fmt しても 04-03 の編集で再び未整形になるため、最終コードに対して一度だけ整形をかける。

Purpose: CI fmt-check ゲートをグリーンにし、Phase 4 を出荷可能にする（PROF-03 / SC-6）。

Output: rustfmt クリーンな全ソース。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-profile-abstraction/04-VERIFICATION.md
@.planning/phases/04-agent-profile-abstraction/04-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: cargo fmt を実行し全ソースを整形（CR-02）</name>
  <files>src/profile.rs, src/turn.rs, src/worker.rs</files>
  <read_first>
    - src/profile.rs（既知差分: line 60 toml::from_str チェーン、line 68-72 validate_profile の if 条件）
    - src/turn.rs（既知差分: line 20-28 build_prompt_body シグネチャ、line 37-41 trigger メソッドチェーン、line 164 assert_eq! マクロ）
    - src/worker.rs（既知差分: line 31 付近の長行、Worker::new / boot / spawn_session / from_parts シグネチャ折り返し）
  </read_first>
  <action>
`cargo fmt` を実行する（`--check` なし＝実際に整形を適用する）。これは rustfmt の自動整形のみで、ロジック・識別子・文字列リテラルは一切変更しない。

整形後、`cargo fmt --check` が exit 0（差分なし）になることを確認する。さらに `cargo test` を実行し、整形によって既存テストが壊れていないこと（24 + 04-03 で追加された spawn_command テスト）を確認する。

注意: 04-03 が先に完了している前提（depends_on: 04-03）。04-03 で追加・変更されたコード（AppState.output_covenant、spawn_command メソッド、worker.rs の create_session 呼び出し変更）も含めて整形対象になる。手作業で個別行を編集せず、必ず `cargo fmt` コマンドで一括整形すること（rustfmt の判断が唯一の真実源）。
  </action>
  <verify>
    <automated>cargo fmt --check 2>&1 | grep -q "Diff" && echo FAIL || echo OK; cargo test 2>&1 | grep -E "test result|error\[" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --check` が exit 0（出力に "Diff in" が 0 件）
    - `cargo test` が exit 0（全テスト passed、04-03 完了後の総数）
    - `cargo clippy --all-targets` が警告なし（CI 2 番目のジョブも通ることを確認）
    - ロジック変更なし: src/profile.rs / src/turn.rs / src/worker.rs の `git diff` が rustfmt の空白・改行・折り返しのみ（識別子・文字列リテラル・制御フローの変更なし）
  </acceptance_criteria>
  <done>cargo fmt --check が exit 0。CI fmt-check ジョブがグリーンになる。cargo test / clippy も依然グリーン。</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

- ソースコードの整形のみ（新規シンボル・シグネチャ変更なし）
- `cargo fmt --check` exit 0 状態（CI fmt-check ジョブのグリーン化）
- src/profile.rs / src/turn.rs / src/worker.rs の rustfmt 整形差分（ロジック不変）
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (なし) | このプランはソース整形のみで、信頼境界・データフロー・実行可能ロジックを一切変更しない |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | cargo fmt が意図せずロジックを変更する | mitigate | rustfmt は空白・改行・折り返しのみ変更し、トークン列は不変。acceptance_criteria の `cargo test` exit 0 と git diff レビュー（整形のみ）で保証。 |

このプランは package-manager install を含まず、新規ネットワークエンドポイント・auth パス・ファイルアクセスパターンを導入しない。
</threat_model>

<verification>
- `cargo fmt --check` exit 0（差分なし）
- `cargo test` exit 0
- `cargo clippy --all-targets` 警告なし
- git diff が rustfmt 整形のみ（ロジック不変）
</verification>

<success_criteria>
- CI fmt-check ジョブがグリーン（CR-02 解消、SC-6 充足）
- ロジック変更ゼロ、テスト・clippy 依然グリーン
</success_criteria>

<output>
Create `.planning/phases/04-agent-profile-abstraction/04-04-SUMMARY.md` when done
</output>
