---
phase: 04-agent-profile-abstraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/profile.rs
  - src/lib.rs
  - src/config.rs
  - agents/claude.toml
autonomous: true
requirements: [PROF-01, PROF-02, PROF-05, PROF-06]
must_haves:
  truths:
    - "D-13: AgentProfile デシリアライズが agents/claude.toml をパースし、5 つの挙動フィールド + 2 つの任意タイムアウト + 任意 model_flag/model_value（指定時のみ spawn コマンドへ追加）を読める"
    - "load_agent_profile が存在しないプロファイル名で探索パスと利用可能プロファイル一覧を含むエラーを返す"
    - "load_agent_profile が output_covenant のプレースホルダ欠落・trigger_template のプレースホルダ欠落を起動時に拒否する"
    - "load_agent_name が AGENT 未設定時に \"claude\" を返す（後方互換）"
    - "load_agents_dir が AGENTS_DIR 未設定時に CWD/agents を返す"
    - "agents/claude.toml が現行ハードコード値（command/ready_pattern/fresh_mode/clear_command/output_covenant/trigger_template）を完全に保持する"
    - "D-08: スキーマは Phase 4 で配線するフィールドのみ定義し、wedge_pattern / prerequisite_check / extra_args 専用フィールドを持たない（extra_args は command の Vec<String> に直書き）"
    - "D-15: TOML 値への環境変数上書きを設けない — env=インフラ層 / TOML=エージェント挙動 の 2 層を維持する"
  artifacts:
    - path: "src/profile.rs"
      provides: "AgentProfile struct + load_agent_profile + validate_profile + list_available_profiles"
      contains: "pub struct AgentProfile"
      min_lines: 80
    - path: "agents/claude.toml"
      provides: "Claude プロファイル（5 ハードコード抽出 + タイムアウト/モデルのコメント例）"
      contains: "ready_pattern = \"auto mode\""
    - path: "Cargo.toml"
      provides: "toml = \"1\" 依存追加"
      contains: "toml ="
  key_links:
    - from: "src/lib.rs"
      to: "src/profile.rs"
      via: "pub mod profile"
      pattern: "pub mod profile"
    - from: "src/profile.rs"
      to: "agents/claude.toml"
      via: "toml::from_str into AgentProfile"
      pattern: "toml::from_str"
---

<objective>
エージェントプロファイル機構の土台を作る。`AgentProfile` 値型と `agents/claude.toml` を定義し、env からプロファイル名・探索ディレクトリを解決する `config.rs` ローダーを追加する。これは Phase 4 の「契約レイヤー」— 後続プラン (04-02) が消費する型・ローダー・TOML をすべてここで確定させる。

このプランは挙動を一切変えない（まだ既存コードに配線しない）。`src/mcp.rs` / `src/worker.rs` / `src/turn.rs` / `src/main.rs` への配線は 04-02 で行う。

Purpose: PROF-01（TOML でエージェント定義）/ PROF-02（AGENT 選択 + claude デフォルト）/ PROF-05（タイムアウト・モデル上書き）/ PROF-06（TOML 追加だけで新エージェント）の基盤を整え、04-02 が編集前に消費できる契約を先に固定する。
Output: `src/profile.rs`（新規）、`agents/claude.toml`（新規・コミット）、`src/config.rs` 拡張、`src/lib.rs` モジュール宣言、`Cargo.toml` の `toml = "1"`。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-profile-abstraction/04-CONTEXT.md
@.planning/phases/04-agent-profile-abstraction/04-RESEARCH.md
@.planning/phases/04-agent-profile-abstraction/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: toml 依存追加 + profile モジュール宣言 + agents/claude.toml 抽出</name>
  <files>Cargo.toml, src/lib.rs, agents/claude.toml</files>
  <read_first>
    - Cargo.toml（既存依存の並び・[dependencies] セクション）
    - src/lib.rs（pub mod 宣言の並び）
    - src/turn.rs（行 22-35 の build_prompt_body format! 出力 = output_covenant のバイト出典、行 47-50 のトリガー文 = trigger_template の出典）
    - src/mcp.rs（行 192-194 の `["claude"]` = command 値の出典）
    - src/worker.rs（行 45 の `"auto mode"` = ready_pattern 値の出典）
    - .planning/phases/04-agent-profile-abstraction/04-RESEARCH.md（§Code Examples の agents/claude.toml 完全版 + CRITICAL NOTE のバイト一致要件）
    - .planning/phases/04-agent-profile-abstraction/04-PATTERNS.md（§agents/claude.toml — TOML multiline note）
  </read_first>
  <action>
    Cargo.toml の [dependencies] に `toml = "1"` を 1 行追加（既存 serde の近く、アルファ順を崩さない位置で可）。これが本フェーズ唯一の新規依存（D-05/STACK.md）。
    src/lib.rs の pub mod ブロックに `pub mod profile;` を config と http の間に追加。
    agents/claude.toml を新規作成しリポにコミットする。フィールドと値（現行ハードコードからの抽出、D-09/D-10）:
      - command = ["claude"]（出典 src/mcp.rs:194）
      - ready_pattern = "auto mode"（出典 src/worker.rs:45）
      - fresh_mode = "command"（現行 /clear 送信経路に対応、D-04）
      - clear_command = "/clear"（出典 src/turn.rs:55）
      - output_covenant: TOML basic multiline string `"""..."""`。中身は src/turn.rs:23-35 の format! 出力規約フッタ部分を {result_path} / {status_path} プレースホルダ化したもの。CRITICAL: v1.0 の build_prompt_body 出力とバイト一致しなければならない（04-02 のゴールデンテストで検証）。現行は task 本文の後に改行 2 つ + 区切り線 `────────────────────────────────` + 規約ヘッダが続く。output_covenant にはこの「task 本文より後ろの部分（区切り線以降を含む）」を入れ、build_prompt_body 側で `{task}\n\n{covenant}` を組み立てる前提で値を構成する。TOML multiline の先頭改行抑制には `\` 行末エスケープを使い、末尾余分改行を入れないこと（PITFALLS Pitfall 5）。
      - trigger_template = "{prompt_path} を読んで、その指示に従ってください。"（出典 src/turn.rs:47-50）
    任意フィールドはコメントアウト例として記載: `# startup_timeout_secs = 25`、`# turn_timeout_secs = 300`、`# model_flag = "--model"`、`# model_value = "opus"`。各フィールドに日本語説明コメントを付け、Phase 5 で codex.toml/opencode.toml を作る際のテンプレートとして機能させる。
    ファイル冒頭に「Claude Code プロファイル。新プロファイル作成テンプレート」のヘッダコメントを置く。
  </action>
  <verify>
    <automated>test -f agents/claude.toml && grep -q 'ready_pattern = "auto mode"' agents/claude.toml && grep -q 'command = \["claude"\]' agents/claude.toml && grep -q '{result_path}' agents/claude.toml && grep -q '{status_path}' agents/claude.toml && grep -q '{prompt_path}' agents/claude.toml && grep -q '^pub mod profile;' src/lib.rs && grep -q '^toml = ' Cargo.toml</automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains a line matching `^toml = "1"`
    - src/lib.rs contains `pub mod profile;`
    - agents/claude.toml exists and contains `command = ["claude"]`, `ready_pattern = "auto mode"`, `fresh_mode = "command"`, `clear_command = "/clear"`
    - agents/claude.toml output_covenant value contains both `{result_path}` and `{status_path}`
    - agents/claude.toml trigger_template value contains `{prompt_path}`
    - agents/claude.toml contains commented examples `# model_flag` and `# model_value`
  </acceptance_criteria>
  <done>toml 依存・profile モジュール宣言・agents/claude.toml が揃い、TOML が 5 挙動フィールド + 必須プレースホルダ + 任意フィールドのコメント例を含む。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/profile.rs — AgentProfile struct + load_agent_profile + validate + scan + tests</name>
  <files>src/profile.rs</files>
  <behavior>
    - load_agent_profile("claude", agents_dir) が正常な claude.toml をパースして AgentProfile を返す
    - load_agent_profile が存在しない名前でエラーを返し、エラー文字列にエージェント名・探索パス（"agents/"）・利用可能プロファイル一覧（"claude"）を含む
    - validate_profile が output_covenant に {result_path} か {status_path} が欠けると bail する
    - validate_profile が trigger_template に {prompt_path} が欠けると bail する
    - 未指定の startup_timeout_secs は 25、turn_timeout_secs は 300 にデフォルトされる
    - deny_unknown_fields により未知キーを含む TOML はパースエラーになる
    - D-13: model_flag/model_value は任意（Option）で、未指定でもパースが成功する
  </behavior>
  <read_first>
    - src/config.rs（行 33-40 load_turns_dir の env > CWD join + with_context パターン、行 5 の anyhow::Context import）
    - src/mcp.rs（行 1-12 のモジュール構成・import スタイル）
    - src/turn.rs（行 117-208 の #[cfg(test)] mod tests co-location + tempfile::tempdir 利用）
    - .planning/phases/04-agent-profile-abstraction/04-RESEARCH.md（§Pattern 1 struct 定義、§Pattern 2 loader、§Code Examples Profile-Not-Found Test）
    - .planning/phases/04-agent-profile-abstraction/04-PATTERNS.md（§src/profile.rs の struct/loader/validate/scan/test 全パターン）
  </read_first>
  <action>
    src/profile.rs を新規作成。RESEARCH.md §Pattern 1 のとおり `#[derive(Debug, Clone, serde::Deserialize)]` + `#[serde(deny_unknown_fields)]` の `pub struct AgentProfile` を定義（D-06/D-07）。フィールド: command: Vec<String>, ready_pattern: String, fresh_mode: String, clear_command: String, output_covenant: String, trigger_template: String, startup_timeout_secs: u64（`#[serde(default = "default_startup_timeout")]`）, turn_timeout_secs: u64（`#[serde(default = "default_turn_timeout")]`）, model_flag: Option<String>, model_value: Option<String>（D-13: 任意、指定時のみ 04-02 で spawn コマンドへ追加）。`fn default_startup_timeout() -> u64 { 25 }` と `fn default_turn_timeout() -> u64 { 300 }` を定義。スキーマは Phase 4 配線フィールドのみ — wedge_pattern / prerequisite_check / extra_args フィールドは設けない（D-08、後付けは serde 任意フィールドで後方互換）。
    `pub fn load_agent_profile(name: &str, agents_dir: &Path) -> anyhow::Result<AgentProfile>`: agents_dir.join(format!("{name}.toml")) を std::fs::read_to_string で同期読み込み（async 不可、起動前のため A1）。読み込み失敗時は list_available_profiles を呼び、anyhow! で「agents/{name}.toml が見つかりません（探索: {path}）。利用可能: {list}」形式のエラーを返す（D-02/D-03、空なら「なし」）。読み込み成功後 toml::from_str で AgentProfile にデシリアライズ（失敗は .with_context で「agents/{name}.toml のパースに失敗」）。最後に validate_profile を呼ぶ。TOML 値の env 上書きは設けない（D-15、env=インフラ / TOML=挙動 の 2 層維持）。
    `fn validate_profile(p: &AgentProfile, name: &str) -> anyhow::Result<()>`（D-11）: output_covenant が "{result_path}" と "{status_path}" の両方を contains しなければ anyhow::bail!「output_covenant に {result_path} と {status_path} が必要」。trigger_template が "{prompt_path}" を contains しなければ bail!「trigger_template に {prompt_path} が必要」。
    `fn list_available_profiles(agents_dir: &Path) -> Vec<String>`: std::fs::read_dir で *.toml の file_stem を集め、失敗時は unwrap_or_default で空 Vec（D-03 フォールバック）。
    識別子は英語、コメント/エラー文言は日本語（conventions）。ログを出すなら `[profile]` タグ。
    末尾に `#[cfg(test)] mod tests`（co-location、D-04）。tempfile::tempdir で claude.toml を動的生成し、(a) 正常パース成功、(b) 不在エージェントでエラーがエージェント名・"agents/"・"claude" を含む、(c) output_covenant プレースホルダ欠落で bail、(d) trigger_template プレースホルダ欠落で bail、(e) 任意タイムアウトのデフォルト値（25/300）、(f) deny_unknown_fields による未知キー拒否、をテスト。test.toml はリポにコミットせず tempdir 内で生成（D-04）。
  </action>
  <verify>
    <automated>cargo test --lib profile 2>&1 | tail -20 && grep -c '"auto mode"' src/profile.rs; cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/profile.rs contains `pub struct AgentProfile`
    - src/profile.rs contains `#[serde(deny_unknown_fields)]`
    - src/profile.rs contains `pub fn load_agent_profile`
    - src/profile.rs declares `model_flag: Option<String>` and `model_value: Option<String>` (D-13)
    - `cargo build` exits 0
    - `cargo test --lib profile` exits 0 with all profile tests passing
    - profile tests include a missing-toml case asserting the error string contains the agent name, "agents/", and "claude"
    - profile tests include a deny_unknown_fields rejection case
  </acceptance_criteria>
  <done>src/profile.rs が AgentProfile・load_agent_profile・validate_profile・list_available_profiles を提供し、co-located テストが全グリーン。cargo build 成功。</done>
</task>

<task type="auto">
  <name>Task 3: src/config.rs — load_agent_name + load_agents_dir</name>
  <files>src/config.rs</files>
  <read_first>
    - src/config.rs（行 14-16 load_ht_mcp_path の単純文字列デフォルト、行 33-40 load_turns_dir の PathBuf + CWD join + with_context）
    - .planning/phases/04-agent-profile-abstraction/04-RESEARCH.md（§Code Examples config.rs Additions）
    - .planning/phases/04-agent-profile-abstraction/04-PATTERNS.md（§src/config.rs）
  </read_first>
  <action>
    src/config.rs の load_turns_dir の後に 2 関数を追加（既存 load_* と同型、env > .env > デフォルト、D-01/PROF-02）:
      - `pub fn load_agent_name() -> String`: std::env::var("AGENT").unwrap_or_else(|_| "claude".to_string())。load_ht_mcp_path:14-16 と同形。Claude デフォルトで後方互換（PROF-02）。
      - `pub fn load_agents_dir() -> anyhow::Result<std::path::PathBuf>`: AGENTS_DIR があれば PathBuf 化、無ければ std::env::current_dir()?.join("agents")。load_turns_dir:33-40 と同形、with_context「current_dir 取得失敗」。
    これらの env（AGENT/AGENTS_DIR）はインフラ層に属し、TOML 値を上書きしない（D-15、env / TOML の 2 層分離を維持）。
    各関数に日本語 doc コメント（既存スタイル踏襲、優先順位の明記）。新規 import は不要（PathBuf/Context は既存利用）。
  </action>
  <verify>
    <automated>grep -q 'pub fn load_agent_name' src/config.rs && grep -q 'pub fn load_agents_dir' src/config.rs && grep -q 'unwrap_or_else(|_| "claude"' src/config.rs && cargo build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/config.rs contains `pub fn load_agent_name() -> String`
    - src/config.rs contains `pub fn load_agents_dir() -> anyhow::Result<std::path::PathBuf>`
    - load_agent_name returns "claude" when AGENT is unset (literal `unwrap_or_else(|_| "claude"` present)
    - load_agents_dir joins "agents" onto current_dir when AGENTS_DIR is unset
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>load_agent_name と load_agents_dir が既存 load_* パターンに揃って追加され、cargo build 成功。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ディスク → プロセス | 起動時に agents/<name>.toml を読み込む。パスは AGENT + AGENTS_DIR（どちらも運用者制御の env）から構成 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | agents/*.toml のパース（toml::from_str） | mitigate | serde deny_unknown_fields で未知キーを起動時拒否（typo/混入を即検出、D-07）。プレースホルダ検証（D-11）で不正テンプレートを起動時に弾く |
| T-04-02 | Elevation/Path | agents_dir.join("{name}.toml") のパス構成 | accept | AGENT/AGENTS_DIR はクライアント入力ではなく運用者制御の env。読み取り専用 std::fs::read_to_string のみ。新規 ASVS カテゴリ該当なし（RESEARCH §Security Domain） |
| T-04-SC | Tampering | crates 依存追加（toml） | mitigate | toml は RESEARCH §Package Legitimacy Audit で OK 判定（crates.io 11 年・11.7M DL/週）。新規 SLOP/SUS なし。追加は `toml = "1"` のみ |
</threat_model>

<verification>
- `cargo build` がグリーン（新規 profile モジュールがコンパイル）
- `cargo test --lib profile` が profile.rs の全テストグリーン
- agents/claude.toml が 5 挙動フィールド + 必須プレースホルダを含む
- 既存 13 テストは未変更（このプランは既存コードに配線しないため回帰なし）— `cargo test` 全グリーン
</verification>

<success_criteria>
- AgentProfile が agents/claude.toml をパースできる（PROF-01）
- load_agent_name が claude デフォルトを返す（PROF-02）
- 任意タイムアウト・model フィールドがスキーマに存在（PROF-05、D-13）
- TOML 追加のみで新プロファイル認識可能な構造が成立（PROF-06、04-02 のテストで最終検証）
- cargo build + cargo test 全グリーン（リファクタ前回帰なし、Success Criteria 5 の土台）
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-01)

新規シンボル・ファイル（drift 検証の対象外 = この phase で新規作成）:
- `src/profile.rs`（新規ファイル）
- `pub struct AgentProfile`（フィールド: command, ready_pattern, fresh_mode, clear_command, output_covenant, trigger_template, startup_timeout_secs, turn_timeout_secs, model_flag, model_value）
- `pub fn load_agent_profile(name: &str, agents_dir: &Path) -> anyhow::Result<AgentProfile>`
- `fn validate_profile(p: &AgentProfile, name: &str) -> anyhow::Result<()>`（非 pub）
- `fn list_available_profiles(agents_dir: &Path) -> Vec<String>`（非 pub）
- `fn default_startup_timeout() -> u64` / `fn default_turn_timeout() -> u64`（非 pub）
- `pub fn load_agent_name() -> String`（src/config.rs）
- `pub fn load_agents_dir() -> anyhow::Result<std::path::PathBuf>`（src/config.rs）
- `pub mod profile;`（src/lib.rs）
- `agents/claude.toml`（新規ファイル、TOML キー: command, ready_pattern, fresh_mode, clear_command, output_covenant, trigger_template）
- `toml = "1"`（Cargo.toml 依存）
</artifacts_produced>

<output>
Create `.planning/phases/04-agent-profile-abstraction/04-01-SUMMARY.md` when done
</output>
</output>
