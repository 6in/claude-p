---
phase: 04-agent-profile-abstraction
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/mcp.rs
  - src/worker.rs
  - src/turn.rs
  - src/main.rs
  - src/http.rs
  - README.md
autonomous: true
requirements: [PROF-01, PROF-03, PROF-04, PROF-05, PROF-06]
must_haves:
  truths:
    - "AGENT=claude ./ht-webif が v1.0 と挙動等価（4 エンドポイント・ready 検知・fresh:true・タイムアウト）"
    - "build_prompt_body が agents/claude.toml の output_covenant 経由で v1.0 format! 出力とバイト一致する（ゴールデンテスト）"
    - "src/worker.rs に \"auto mode\" 文字列リテラルが 0 箇所（全 4 サイトが profile.ready_pattern 化）"
    - "src/mcp.rs に create_claude_session が 0 箇所（trait/McpClient/FakeMcp すべて create_session(cmd) にリネーム）"
    - "fresh:true が profile.fresh_mode に従い command(clear_command 送信) / respawn(recreate) を分岐する"
    - "起動時に AGENT 名と実際の turns ディレクトリ（<base>/<agent-name>）がログ出力される"
    - "AGENT=<unknown> 起動でプロファイル不在の明確なエラーで即終了する"
  artifacts:
    - path: "src/turn.rs"
      provides: "build_prompt_body(covenant_template param) + fresh_mode dispatch + trigger_template + profile turn_timeout + golden test"
      contains: "covenant_template"
    - path: "src/worker.rs"
      provides: "Worker に profile フィールド + 4 ready-pattern サイトの profile 化"
      contains: "profile"
    - path: "src/main.rs"
      provides: "プロファイルロード配線 + D-16 turns サブディレクトリ + 起動バナー"
      contains: "load_agent_profile"
    - path: "README.md"
      provides: "AGENT/AGENTS_DIR 環境変数 + D-16 TURNS_DIR 挙動変更の記載"
      contains: "AGENT"
  key_links:
    - from: "src/main.rs"
      to: "src/profile.rs"
      via: "load_agent_profile(&agent_name, &agents_dir)"
      pattern: "load_agent_profile"
    - from: "src/main.rs"
      to: "Worker::new"
      via: "profile を Worker に渡す"
      pattern: "Worker::new\\([^)]*profile"
    - from: "src/turn.rs"
      to: "worker.profile"
      via: "covenant/trigger/fresh_mode/turn_timeout を profile から取得"
      pattern: "worker\\.profile"
---

<objective>
04-01 で定義した `AgentProfile` 契約を既存コードへ配線する。trait Mcp のメソッドリネーム、worker.rs の 4 ready-pattern サイト、turn.rs のテンプレート化と fresh_mode 分岐、main.rs の起動シーケンス（D-16 turns サブディレクトリ含む）、http.rs テストフィクスチャ更新、README のドキュメント更新を行う。

これは純粋リファクタ — 観測可能な挙動は v1.0 と完全一致しなければならない。ゴールデンテスト（D-12）がそれをバイト単位で固定する。

Purpose: PROF-03（現行挙動再現・4 エンドポイント不変）/ PROF-04（fresh_mode 分岐）/ PROF-05（profile タイムアウト適用）/ PROF-01・PROF-06（配線完了で TOML 駆動が成立）。
Output: 5 つの src ファイル修正 + README 更新。全 src/worker.rs の "auto mode" と全 src/mcp.rs の create_claude_session が消え、ゴールデンテスト + プロファイル不在テストが追加された状態。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-agent-profile-abstraction/04-CONTEXT.md
@.planning/phases/04-agent-profile-abstraction/04-RESEARCH.md
@.planning/phases/04-agent-profile-abstraction/04-PATTERNS.md
@.planning/phases/04-agent-profile-abstraction/04-01-PLAN.md
@agents/claude.toml
@src/profile.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/mcp.rs trait リネーム + src/worker.rs profile 配線（4 ready サイト）</name>
  <files>src/mcp.rs, src/worker.rs</files>
  <behavior>
    - 既存の mcp.rs / worker.rs 関連テスト（restart/recreate/handshake 系）がリネーム後も全グリーン
    - FakeMcp.create_session(cmd) が引数を無視して scripted reply キューから pop する
    - Worker<M> が profile フィールドを保持し、from_parts（test 用）が profile を受け取る
  </behavior>
  <read_first>
    - src/mcp.rs（行 19-27 trait Mcp 定義、行 191-205 McpClient::create_claude_session、行 251-313 FakeMcp struct + create_claude_session impl、行 264-291 フィールド構成）
    - src/worker.rs（全体: 行 12-16 struct、21-28 new、31-36 boot、40-53 spawn_session、84-94 ensure_healthy、100-122 recreate、134-159 restart、62-69 from_parts）
    - src/http.rs（行 218-233 build_test_state の from_parts 呼び出し — 引数が増えることを把握）
    - .planning/phases/04-agent-profile-abstraction/04-PATTERNS.md（§src/mcp.rs、§src/worker.rs の BEFORE/AFTER パターン全部、Pitfall 2/3）
    - .planning/phases/04-agent-profile-abstraction/04-RESEARCH.md（§Pattern 3、§Open Questions 2 spawn_session の profile 受け渡し）
  </read_first>
  <action>
    src/mcp.rs:
      - trait Mcp（行 22）の `async fn create_claude_session(&mut self) -> Result<String>;` を `async fn create_session(&mut self, cmd: &[String]) -> Result<String>;` にリネーム。
      - McpClient impl（行 192-205）を `create_session(&mut self, cmd: &[String])` にリネームし、call_tool の `json!({ "command": ["claude"] })` を `json!({ "command": cmd })` に変更。Session ID パース以降は不変。
      - FakeMcp impl（行 303-307）を `create_session(&mut self, _cmd: &[String])` にリネーム（引数無視）。フィールド名 create_session_replies は不変。エラー文言の "create_claude_session" は "create_session" に更新。
      - リネーム後 `grep -n create_claude_session src/mcp.rs` が 0 件であること（Pitfall 3: FakeMcp は #[cfg(test)] なので cargo build では検出されない → cargo test 必須）。
    src/worker.rs:
      - Worker<M> struct（行 12-16）に `pub profile: crate::profile::AgentProfile` フィールドを追加。
      - `Worker::new`（行 21-28）の引数に `profile: crate::profile::AgentProfile` を追加。boot に &profile を渡し、Self { ..., profile } で構築。
      - `boot`（行 31-36）の引数に `profile: &AgentProfile` を追加し、spawn_session に渡す。
      - `spawn_session`（行 40-53）の引数を `(client: &mut McpClient, profile: &AgentProfile)` に変更（Open Questions 2 の推奨）。`create_claude_session()` → `create_session(&profile.command)`、`Duration::from_secs(25)` → `Duration::from_secs(profile.startup_timeout_secs)`、`snap.contains("auto mode")` → `snap.contains(&profile.ready_pattern)`。
      - `ensure_healthy`（行 84-94）: `self.profile.ready_pattern.clone()` をローカルに取り、matches! 内の `snap.contains("auto mode")` → `snap.contains(&ready_pattern)`。
      - `recreate`（行 100-122）: `self.client.create_claude_session()` → `self.client.create_session(&self.profile.command.clone())`、25 → `self.profile.startup_timeout_secs`、`"auto mode"` → `&self.profile.ready_pattern`（borrow checker のため必要なら ready_pattern を事前 clone）。
      - `restart`（行 137-159）: 同様に create_session(&self.profile.command)、profile.startup_timeout_secs、profile.ready_pattern 化。
      - `from_parts`（行 62-69、#[cfg(test)]）の引数に `profile: crate::profile::AgentProfile` を追加し Self に含める。
      - 完了後 `grep -n '"auto mode"' src/worker.rs` が 0 件、`grep -n create_claude_session src/worker.rs` が 0 件であること（Pitfall 2）。
    この段階では http.rs の from_parts 呼び出しが引数不足でコンパイルエラーになる — それは Task 3 で修正する。よってこのタスク単独では cargo test は通らなくてよいが、cargo build（bin/lib の非テスト）はグリーンであること。
  </action>
  <verify>
    <automated>test $(grep -c '"auto mode"' src/worker.rs) -eq 0 && test $(grep -c 'create_claude_session' src/mcp.rs) -eq 0 && test $(grep -c 'create_claude_session' src/worker.rs) -eq 0 && cargo build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"auto mode"' src/worker.rs` returns 0
    - `grep -c 'create_claude_session' src/mcp.rs` returns 0
    - `grep -c 'create_claude_session' src/worker.rs` returns 0
    - src/mcp.rs trait Mcp contains `async fn create_session(&mut self, cmd: &[String])`
    - src/worker.rs Worker struct contains `profile`
    - src/worker.rs Worker::new signature includes a `profile` parameter
    - `cargo build` exits 0
  </acceptance_criteria>
  <done>trait/McpClient/FakeMcp が create_session(cmd) にリネームされ、Worker が profile フィールドを持ち 4 ready-pattern サイトが profile 駆動。"auto mode" と create_claude_session の残骸ゼロ。cargo build グリーン。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/turn.rs — build_prompt_body テンプレート化 + fresh_mode 分岐 + golden test</name>
  <files>src/turn.rs</files>
  <behavior>
    - build_prompt_body(task, result, status, &profile.output_covenant) の出力が v1.0 format! 出力とバイト一致する（ゴールデンテスト、D-12）
    - 既存 build_prompt_body_includes_task_and_paths テストが新シグネチャ（covenant_template 引数）で通る
    - fresh:true + fresh_mode="command" で clear_command が submit_line される
    - fresh:true + fresh_mode="respawn" で worker.recreate が呼ばれる
    - 未知の fresh_mode で bail する
  </behavior>
  <read_first>
    - src/turn.rs（全体: 行 11 TURN_TIMEOUT import、22-36 build_prompt_body、40-79 process_job、47-50 trigger、54-57 fresh /clear、60 deadline、117-133 既存テスト build_prompt_body_includes_task_and_paths）
    - agents/claude.toml（output_covenant / trigger_template / clear_command の実値 — golden 期待値の出典）
    - src/worker.rs（profile フィールド・recreate・submit_line の最新シグネチャ）
    - .planning/phases/04-agent-profile-abstraction/04-RESEARCH.md（§Pattern 4 fresh_mode dispatch、§Golden Test Pattern、Pitfall 1/5）
    - .planning/phases/04-agent-profile-abstraction/04-PATTERNS.md（§src/turn.rs の CRITICAL first step + 全 BEFORE/AFTER + golden test 完全版）
  </read_first>
  <action>
    CRITICAL 最初の手順（Pitfall 1）: turn.rs を編集する前に、現行 build_prompt_body（行 23-35）の format! が生成する文字列を「やってほしいこと」「/tmp/result-X.txt」「/tmp/status-X.json」で具体化したバイト列を、後述ゴールデンテストの `const EXPECTED` リテラルにコピーしておく。これを失うと D-12 のバイト一致検証ができなくなる。
    build_prompt_body（行 22）のシグネチャを `pub fn build_prompt_body(task: &str, result_path: &Path, status_path: &Path, covenant_template: &str) -> String` に変更。本体は covenant_template に対し `.replace("{result_path}", &result_path.display().to_string())` と `.replace("{status_path}", &status_path.display().to_string())` を適用し（str::replace のみ、D-09）、`format!("{task}\n\n{body}")` で組み立てる。最終バイト列が v1.0 と一致するよう、agents/claude.toml の output_covenant の改行/区切り線構成を golden test で固定する。
    process_job（行 40-79）— profile は worker.profile から取得（RESEARCH Open Questions 1 の推奨、シグネチャ変更不要）:
      - trigger（行 47-50）を `worker.profile.trigger_template.replace("{prompt_path}", &prompt_path.display().to_string())` に変更（D-10）。borrow 競合を避けるため必要なら trigger_template を事前 clone。
      - build_prompt_body 呼び出し箇所に第 4 引数として profile.output_covenant を渡す（prompt ファイル書き込み経路。現行コードで build_prompt_body を呼んでいる箇所を確認し covenant を渡す）。
      - fresh 分岐（行 54-57）を fresh_mode で分岐（D-04/PROF-04、RESEARCH §Pattern 4）: "command" → `worker.submit_line(&clear_command).await?`（clear_command は worker.profile.clear_command.clone()）、"respawn" → `worker.recreate().await?`、その他 → `anyhow::bail!("未知の fresh_mode: {other}")`。後続の sleep 1000ms は不変。
      - deadline（行 60）を `Instant::now() + Duration::from_secs(worker.profile.turn_timeout_secs)` に変更（PROF-05）。TURN_TIMEOUT import（行 11）が未使用になるなら削除（config.rs の定数自体は残してよい — D-14 の MCP_TIMEOUT は不変）。
    既存テスト build_prompt_body_includes_task_and_paths（行 125-133）を新シグネチャに更新: 第 4 引数に両プレースホルダを含む最小テンプレート（例 "x {result_path} {status_path}"）を渡し、既存 assertion を維持。
    ゴールデンテスト build_prompt_body_covenant_matches_v1_output を追加（D-12、RESEARCH §Golden Test Pattern）: `const EXPECTED` に v1.0 バイト列、`load_agent_profile("claude", Path::new("agents"))` で実 TOML をロードし build_prompt_body の出力と `assert_eq!`。期待値の出典コメント（agents/claude.toml output_covenant）を付す。
  </action>
  <verify>
    <automated>test $(grep -c '"/clear"' src/turn.rs) -eq 0 && grep -q 'covenant_template' src/turn.rs && grep -q 'turn_timeout_secs' src/turn.rs && grep -q 'fresh_mode' src/turn.rs && cargo test --lib turn 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/turn.rs build_prompt_body signature contains `covenant_template: &str`
    - `grep -c '"/clear"' src/turn.rs` returns 0 (clear command now comes from profile)
    - src/turn.rs contains `turn_timeout_secs` and `fresh_mode`
    - src/turn.rs contains a golden test asserting build_prompt_body output equals the v1.0 byte string via `assert_eq!`
    - the golden test loads the profile via `load_agent_profile("claude"` and `Path::new("agents")`
    - `cargo test --lib turn` exits 0 (golden test + existing turn tests green)
  </acceptance_criteria>
  <done>build_prompt_body が covenant テンプレート駆動、process_job が fresh_mode 分岐 + profile turn_timeout 適用、ゴールデンテストが v1.0 バイト一致を固定。turn テスト全グリーン。</done>
</task>

<task type="auto">
  <name>Task 3: src/main.rs 配線 + D-16 turns サブディレクトリ + http.rs テスト修正 + README</name>
  <files>src/main.rs, src/http.rs, README.md</files>
  <read_first>
    - src/main.rs（全体: 行 6 import、14-20 起動シーケンス、23-25 turns_dir、19 Worker::new）
    - src/http.rs（行 218-233 build_test_state — from_parts の引数追加が必要、行 210-212 import）
    - src/profile.rs（load_agent_profile シグネチャ、AgentProfile 構築方法 — テストフィクスチャ用に最小プロファイルをどう作るか）
    - agents/claude.toml（テストフィクスチャで load_agent_profile("claude", Path::new("agents")) を使うか、最小 AgentProfile リテラルを作るか判断）
    - README.md（行 257-266 環境変数表、行 71-77 複数インスタンス運用、行 268- ターン成果物）
    - .planning/phases/04-agent-profile-abstraction/04-RESEARCH.md（§Pattern 5 D-16、§Code Examples main.rs、State of the Art TURNS_DIR breaking change）
    - .planning/phases/04-agent-profile-abstraction/04-PATTERNS.md（§src/main.rs 全 BEFORE/AFTER、Pitfall 4 turns_dir テスト影響）
  </read_first>
  <action>
    src/main.rs:
      - import（行 6）に load_agent_name, load_agents_dir を追加し、`use ht_webif::profile::load_agent_profile;` を追加。
      - 起動シーケンス（行 14-20、dotenvy の後・Worker::new の前）に挿入: `let agent_name = load_agent_name();`、`let agents_dir = load_agents_dir()?;`、`let profile = load_agent_profile(&agent_name, &agents_dir)?;`（不在/不正は ? で即終了 = Success Criteria 3）、`eprintln!("[profile] エージェント: {agent_name}");`。Worker::new 呼び出しを `Worker::new(ht_mcp_path, profile).await?` に変更。
      - turns_dir（行 23-25、D-16/PROF）: `let turns_base = load_turns_dir()?;` の後に `let turns_dir = turns_base.join(&agent_name);`（明示 TURNS_DIR でも常に <agent-name> を付与）。create_dir_all 後に `eprintln!("[profile] ターンディレクトリ: {}", turns_dir.display());`（D-16 の驚き軽減バナー）。
    src/http.rs:
      - build_test_state（行 218-233）の `Worker::<FakeMcp>::from_parts(...)` 呼び出しに profile 引数を追加。テスト用最小 AgentProfile を `load_agent_profile("claude", Path::new("agents")).unwrap()` で実 TOML からロードするか、AgentProfile リテラルを直接構築する（profile.rs のフィールドが pub なので構築可能）。Pitfall 4: turns_dir は build_test_state がそのまま受け取る（main.rs の <agent-name> 付与はここを通らないため既存テストは不変、turns_dir 構築ロジックを load_turns_dir に移さないこと）。
      - 必要な import（Path 等）を追加。
      - 全 http テスト（パストラバーサル系含む）がグリーンになること。
    README.md:
      - 環境変数表（行 259-264）に 2 行追加: `| `AGENT` | `claude` | 使用するエージェントプロファイル名。`agents/<name>.toml` を読む。未指定なら claude（後方互換） |` と `| `AGENTS_DIR` | `./agents` | プロファイル TOML の探索ディレクトリ |`。
      - TURNS_DIR 行（行 263）の説明を D-16 の挙動変更明記に更新: 実際の書き込み先が常に `<TURNS_DIR>/<agent-name>/` になること、これは v1.0 の明示 TURNS_DIR 運用と非互換であることを記載。
      - 複数インスタンス運用セクション（行 71-77）の例を、agent-name サブディレクトリが自動付与される旨に合わせて補足（AGENT + PORT 分離で衝突回避）。
    全タスク完了後 `cargo test` 全件グリーン（Success Criteria 5）であること。
  </action>
  <verify>
    <automated>grep -q 'load_agent_profile' src/main.rs && grep -q 'join(&agent_name)' src/main.rs && grep -q 'AGENT' README.md && grep -q 'AGENTS_DIR' README.md && cargo test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/main.rs contains `load_agent_profile` and passes profile to `Worker::new`
    - src/main.rs contains `turns_base.join(&agent_name)` (D-16 subdirectory)
    - src/main.rs logs the agent name and actual turns directory at startup
    - src/http.rs build_test_state passes a profile argument to `from_parts`
    - README.md environment-variable table contains rows for `AGENT` and `AGENTS_DIR`
    - README.md documents that the actual turns path is always `<TURNS_DIR>/<agent-name>/` (D-16 breaking change)
    - `cargo test` exits 0 with all tests green (no regressions, Success Criteria 5)
  </acceptance_criteria>
  <done>main.rs がプロファイルをロードして Worker に配線し turns に <agent-name> を付与、http テストが profile 引数で修正され全グリーン、README が AGENT/AGENTS_DIR と D-16 挙動変更を文書化。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 起動時設定 → 実行時挙動 | profile が spawn コマンド・ready 検知・テンプレート・fresh 分岐を駆動する。不正プロファイルは挙動破壊につながりうる |
| クライアント → TUI | POST /prompt の prompt が trigger_template 経由で TUI に渡る（v1.0 から不変の境界） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | build_prompt_body の covenant 置換（プロファイル由来テンプレート） | mitigate | ゴールデンテスト（D-12）が v1.0 とのバイト一致を固定。プレースホルダ欠落は 04-01 の validate_profile で起動時に排除済み |
| T-04-04 | Denial of Service | fresh_mode 未知値・不正 ready_pattern によるタイムアウト顕在化 | mitigate | fresh_mode の未知値は anyhow::bail! で即エラー（沈黙失敗にしない）。ready_pattern 欠落は profile デシリアライズで必須のため起動時に検出 |
| T-04-05 | Information Disclosure | 起動バナーに turns パス・エージェント名を出力 | accept | localhost 限定・運用者向けログ。新規機密露出なし（v1.0 セキュリティ姿勢を維持、RESEARCH §Security Domain） |
| T-04-SC | Tampering | crates 依存（toml は 04-01 で追加済み、本プランは新規追加なし） | accept | 本プランで新規 package install なし。04-01 の T-04-SC を参照 |
</threat_model>

<verification>
- `grep -c '"auto mode"' src/worker.rs` = 0、`grep -c create_claude_session src/mcp.rs src/worker.rs` = 0、`grep -c '"/clear"' src/turn.rs` = 0（全ハードコード抽出完了）
- ゴールデンテスト build_prompt_body_covenant_matches_v1_output が v1.0 バイト一致でグリーン（PROF-03）
- `cargo test` 全件グリーン（既存 + 新規、Success Criteria 5 = リファクタ前回帰なし）
- README に AGENT/AGENTS_DIR + D-16 TURNS_DIR 挙動変更が記載
</verification>

<success_criteria>
- AGENT=claude が v1.0 挙動等価（4 エンドポイント・ready 検知・fresh:true・タイムアウト、Success Criteria 1 / PROF-03）
- agents/claude.toml がハードコードを完全置換（ready_pattern/clear_command/fresh_mode/output_covenant/timeout、Success Criteria 2）
- AGENT=<unknown> で明確なエラー即終了（Success Criteria 3 / main.rs の ? 伝播）
- fresh_mode 分岐が command/respawn で動作（PROF-04）
- profile タイムアウトが適用される（PROF-05）
- cargo test 全グリーン（Success Criteria 5）
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 04-02)

新規/変更シンボル（drift 検証対象外 = この phase で新規作成またはシグネチャ変更）:
- `async fn create_session(&mut self, cmd: &[String]) -> Result<String>`（src/mcp.rs trait Mcp / McpClient / FakeMcp、`create_claude_session` をリネーム）
- `Worker<M>` の新フィールド `pub profile: crate::profile::AgentProfile`（src/worker.rs）
- `Worker::new(ht_mcp_path: String, profile: AgentProfile)` の新シグネチャ（profile 引数追加）
- `Worker::boot(ht_mcp_path: &str, profile: &AgentProfile)` の新シグネチャ
- `Worker::spawn_session(client: &mut McpClient, profile: &AgentProfile)` の新シグネチャ
- `Worker::from_parts(client, session_id, ht_mcp_path, profile)` の新シグネチャ（#[cfg(test)]）
- `build_prompt_body(task, result_path, status_path, covenant_template: &str)` の新シグネチャ（src/turn.rs、第 4 引数追加）
- `build_prompt_body_covenant_matches_v1_output`（src/turn.rs 新規ゴールデンテスト）
- 環境変数 `AGENT` / `AGENTS_DIR`（README.md に文書化、main.rs で消費）
- turns ディレクトリ構造 `<TURNS_DIR>/<agent-name>/`（D-16、main.rs）
</artifacts_produced>

<output>
Create `.planning/phases/04-agent-profile-abstraction/04-02-SUMMARY.md` when done
</output>
